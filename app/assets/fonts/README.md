# Bundled subtitle fonts

This directory is the single render-time source for curated OFL font files.
Only static `.ttf`/`.otf` cuts with their license files belong here. The
renderer validates family names from each file's OpenType `name` table.
