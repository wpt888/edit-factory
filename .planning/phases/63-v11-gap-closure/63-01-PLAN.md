---
phase: 63-v11-gap-closure
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - frontend/src/app/librarie/page.tsx
  - frontend/src/app/pipeline/page.tsx
  - frontend/src/app/segments/page.tsx
  - frontend/src/app/settings/page.tsx
  - frontend/src/app/global-error.tsx
  - frontend/src/app/products/page.tsx
  - frontend/src/app/product-video/page.tsx
  - frontend/src/app/batch-generate/page.tsx
  - frontend/src/components/create-feed-dialog.tsx
  - frontend/src/components/create-profile-dialog.tsx
  - frontend/src/components/PublishDialog.tsx
  - frontend/src/components/video-processing/tts-panel.tsx
  - frontend/src/components/video-processing/variant-triage.tsx
  - frontend/src/components/video-segment-player.tsx
  - frontend/src/contexts/profile-context.tsx
  - frontend/src/hooks/use-batch-polling.ts
  - frontend/src/hooks/use-job-polling.ts
  - frontend/src/hooks/use-local-storage-config.ts
  - frontend/src/hooks/use-subtitle-settings.ts
  - frontend/src/lib/api-error.ts
autonomous: true
requirements: [UX-04]

must_haves:
  truths:
    - "Every user-visible string in librarie/page.tsx is in English — no Romanian text remains"
    - "Every user-visible string in pipeline/page.tsx is in English — no Romanian text remains"
    - "Every user-visible string in segments/page.tsx is in English — no Romanian text remains"
    - "All other frontend pages (settings, global-error, products, product-video, batch-generate) have no Romanian strings"
    - "All components, hooks, contexts, and lib files (12 non-page files) have no Romanian strings"
  artifacts:
    - path: "frontend/src/app/librarie/page.tsx"
      provides: "English-only error messages and UI strings"
    - path: "frontend/src/app/pipeline/page.tsx"
      provides: "English-only error messages and UI strings"
    - path: "frontend/src/app/segments/page.tsx"
      provides: "English-only error messages and UI strings"
  key_links:
    - from: "handleApiError calls"
      to: "toast error messages"
      via: "string parameter"
      pattern: "handleApiError.*Error"
---

<objective>
Translate all remaining Romanian error strings to English across all frontend pages.

Purpose: The v11 audit found 32+ Romanian error strings without diacritics that escaped the Phase 62-01 localization scan (which only grepped for diacritics). These are user-visible toast error messages and inline UI text. UX-04 requires consistent English across the entire UI.

Output: All 20 affected frontend files (8 pages + 12 components/hooks/contexts/lib) with every Romanian string replaced by its English equivalent.
</objective>

<execution_context>
@/home/ukfdb/.claude/get-shit-done/workflows/execute-plan.md
@/home/ukfdb/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/v11-MILESTONE-AUDIT.md
@frontend/src/app/librarie/page.tsx
@frontend/src/app/pipeline/page.tsx
@frontend/src/app/segments/page.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Translate Romanian strings in primary audit targets (librarie, pipeline, segments)</name>
  <files>
    frontend/src/app/librarie/page.tsx
    frontend/src/app/pipeline/page.tsx
    frontend/src/app/segments/page.tsx
  </files>
  <action>
Find and replace ALL Romanian strings in these 3 files. The exact translations:

**librarie/page.tsx** (3 strings):
- `"Eroare la incarcarea clipurilor"` → `"Error loading clips"`
- `"Se incarca mai multe clipuri..."` → `"Loading more clips..."`
- `"Toate clipurile au fost incarcate ({clips.length} total)"` → keep the JSX expression but translate surrounding text: `"All clips loaded ({clips.length} total)"`

**pipeline/page.tsx** (2 strings):
- `"Eroare la actualizarea statusului pipeline"` → `"Error updating pipeline status"`
- `"Publicat cu succes din pipeline!"` → `"Published successfully from pipeline!"`

**segments/page.tsx** (17 strings):
- `"Eroare la incarcarea video-urilor sursa"` → `"Error loading source videos"`
- `"Eroare la incarcarea segmentelor"` → `"Error loading segments"`
- `"Eroare la incarcarea tuturor segmentelor"` → `"Error loading all segments"`
- `"Eroare la stergerea segmentului"` → `"Error deleting segment"`
- `"Eroare la restaurarea segmentului"` → `"Error restoring segment"`
- `"Eroare la incarcarea video-ului"` → `"Error loading video"`
- `"Eroare la stergerea video-ului"` → `"Error deleting video"`
- `"Eroare la stergerea segmentului in unire"` → `"Error deleting segment during merge"`
- `"Eroare la crearea segmentului"` → `"Error creating segment"`
- `"Eroare la actualizarea segmentului"` → `"Error updating segment"`
- `"Eroare la salvarea transformarilor"` → `"Error saving transformations"`
- `"Eroare la modificarea favoritului"` → `"Error toggling favorite"`
- `"Eroare la crearea grupului de produse"` → `"Error creating product group"`
- `"Eroare la actualizarea grupului"` → `"Error updating group"`
- `"Eroare la stergerea grupului"` → `"Error deleting group"`
- `"Se incarca..."` (in upload button) → `"Uploading..."`
- `"Selecteaza un video mai intai."` → `"Select a video first."`

Use the Edit tool for each replacement. Do NOT change any logic, imports, or code structure — only translate the string literals.
  </action>
  <verify>
    <automated>cd frontend && grep -rn "Eroare\|eroare\|Se incarca\|incarca mai multe\|au fost incarcate\|Publicat cu succes\|Selecteaza un video\|stergerea\|restaurarea\|actualizarea\|crearea\|salvarea\|modificarea" src/app/librarie/page.tsx src/app/pipeline/page.tsx src/app/segments/page.tsx | grep -v "node_modules" | grep -v ".next" ; echo "EXIT:$?"</automated>
  </verify>
  <done>Zero Romanian strings found in librarie/page.tsx, pipeline/page.tsx, and segments/page.tsx by the grep command above (empty output + EXIT:1 meaning no matches)</done>
</task>

<task type="auto">
  <name>Task 2: Translate Romanian strings in remaining pages (settings, global-error, products, product-video, batch-generate)</name>
  <files>
    frontend/src/app/settings/page.tsx
    frontend/src/app/global-error.tsx
    frontend/src/app/products/page.tsx
    frontend/src/app/product-video/page.tsx
    frontend/src/app/batch-generate/page.tsx
  </files>
  <action>
Find and replace ALL Romanian strings in these 5 files:

**settings/page.tsx** (5 strings):
- `"Eroare la incarcarea conturilor ElevenLabs"` → `"Error loading ElevenLabs accounts"`
- `"Eroare la incarcarea setarilor"` → `"Error loading settings"`
- `"Eroare la incarcarea dashboard-ului"` → `"Error loading dashboard"`
- `"Eroare la incarcarea vocilor"` → `"Error loading voices"`
- `"Domain-ul Postiz (ex: https://postiz.nortia.ro). API paths sunt adaugate automat."` → `"Postiz domain (e.g.: https://postiz.nortia.ro). API paths are added automatically."`

**global-error.tsx** (2 strings):
- `"A aparut o eroare neasteptata. Poti incerca sa reiei actiunea sau sa te intorci acasa."` → `"An unexpected error occurred. You can try the action again or return home."`
- `"Detalii eroare"` → `"Error details"`

**products/page.tsx** (2 strings):
- `"Importa produse dintr-un feed sau adauga manual."` (appears twice) → `"Import products from a feed or add manually."`
- `"Adauga Feed"` → `"Add Feed"`

**product-video/page.tsx** (1 string):
- `"Selecteaza un produs din catalog"` → `"Select a product from the catalog"`

**batch-generate/page.tsx** (1 string):
- `"Selecteaza produse si configureaza o generare batch."` → `"Select products and configure a batch generation."`

Use the Edit tool for each replacement. Do NOT change any logic, imports, or code structure — only translate the string literals.
  </action>
  <verify>
    <automated>cd frontend && grep -rn "Eroare\|eroare\|aparut o eroare\|Detalii eroare\|Domain-ul\|Importa produse\|Adauga Feed\|Selecteaza un produs\|Selecteaza produse\|sunt adaugate" src/app/settings/page.tsx src/app/global-error.tsx src/app/products/page.tsx src/app/product-video/page.tsx src/app/batch-generate/page.tsx | grep -v "node_modules" ; echo "EXIT:$?"</automated>
  </verify>
  <done>Zero Romanian strings found in settings/page.tsx, global-error.tsx, products/page.tsx, product-video/page.tsx, and batch-generate/page.tsx (empty output + EXIT:1 meaning no matches)</done>
</task>

<task type="auto">
  <name>Task 3: Translate Romanian strings in components, hooks, contexts, and lib files</name>
  <files>
    frontend/src/components/create-feed-dialog.tsx
    frontend/src/components/create-profile-dialog.tsx
    frontend/src/components/PublishDialog.tsx
    frontend/src/components/video-processing/tts-panel.tsx
    frontend/src/components/video-processing/variant-triage.tsx
    frontend/src/components/video-segment-player.tsx
    frontend/src/contexts/profile-context.tsx
    frontend/src/hooks/use-batch-polling.ts
    frontend/src/hooks/use-job-polling.ts
    frontend/src/hooks/use-local-storage-config.ts
    frontend/src/hooks/use-subtitle-settings.ts
    frontend/src/lib/api-error.ts
  </files>
  <action>
Find and replace ALL Romanian strings in these 12 files:

**components/create-feed-dialog.tsx** (1 string):
- `"Eroare la crearea feed-ului"` → `"Error creating feed"`

**components/create-profile-dialog.tsx** (1 string):
- `"Eroare la crearea profilului"` → `"Error creating profile"`

**components/PublishDialog.tsx** (4 strings):
- `"Nu s-au putut incarca platformele Postiz"` → `"Could not load Postiz platforms"`
- `"Selecteaza cel putin o platforma"` → `"Select at least one platform"`
- `"Eroare la publicare"` → `"Error publishing"`
- `"Postare programata!"` → `"Post scheduled!"`

**components/video-processing/tts-panel.tsx** (1 string):
- `"Voice-over-ul va fi generat separat dupa crearea variantelor."` → `"The voice-over will be generated separately after creating variants."`

**components/video-processing/variant-triage.tsx** (4 strings):
- `"Selecteaza toate"` → `"Select all"`
- `"Deselecteaza"` → `"Deselect"`
- `"Adauga Voice-over"` → `"Add Voice-over"`
- `"Selecteaza cel putin o varianta"` → `"Select at least one variant"`

**components/video-segment-player.tsx** (1 string):
- `"Eroare la activarea fullscreen"` → `"Error activating fullscreen"`

**contexts/profile-context.tsx** (2 strings):
- `"Eroare la incarcarea profilului"` → `"Error loading profile"`
- `"Eroare la context profil"` → `"Profile context error"`

**hooks/use-batch-polling.ts** (1 string):
- `"Eroare la actualizarea statusului"` → `"Error updating status"`

**hooks/use-job-polling.ts** (1 string):
- `"Eroare la actualizarea statusului"` → `"Error updating status"`

**hooks/use-local-storage-config.ts** (1 string):
- `"Eroare la setari"` → `"Settings error"`

**hooks/use-subtitle-settings.ts** (1 string):
- `"Eroare la setari"` → `"Settings error"`

**lib/api-error.ts** (2 strings):
- `"Eroare de server. Incearca mai tarziu."` → `"Server error. Try again later."`
- `"A aparut o eroare neasteptata."` → `"An unexpected error occurred."`

Use the Edit tool for each replacement. Do NOT change any logic, imports, or code structure — only translate the string literals.
  </action>
  <verify>
    <automated>cd frontend && grep -rn "Eroare\|eroare\|Selecteaza toate\|Deselecteaza\|Adauga Voice\|Voice-over-ul\|Postare programata\|Nu s-au putut\|Incearca mai tarziu\|aparut o eroare" src/components/ src/contexts/ src/hooks/ src/lib/ --include="*.tsx" --include="*.ts" | grep -v "node_modules" | grep -v ".next" ; echo "EXIT:$?"</automated>
  </verify>
  <done>Zero Romanian strings found in any of the 12 component/hook/context/lib files (empty output + EXIT:1 meaning no matches)</done>
</task>

</tasks>

<verification>
Run a comprehensive grep across ALL frontend pages for any remaining Romanian strings:

```bash
cd frontend && grep -rn "Eroare\|eroare\|incarca\|stergere\|restaurar\|actualizar\|Selecteaz\|selecteaz\|Publicat\|Domain-ul\|Importa\|Adauga\|aparut o\|Detalii\|Postare programata\|Voice-over-ul\|Deselecteaza\|Nu s-au putut\|Incearca mai tarziu" src/ --include="*.tsx" --include="*.ts" | grep -v "node_modules" | grep -v ".next" | grep -v "// " | grep -v "import"
```

Expected: Zero matches. Any hit indicates a missed Romanian string.
</verification>

<success_criteria>
1. Every handleApiError() call in librarie/page.tsx, pipeline/page.tsx, and segments/page.tsx passes an English error message
2. Every toast message and inline UI text in all 8 files is in English
3. The comprehensive grep finds zero remaining Romanian strings across the entire frontend/src/ directory (including components, hooks, contexts, and lib)
4. No code logic, imports, or structure has changed — only string literals were modified
</success_criteria>

<output>
After completion, create `.planning/phases/63-v11-gap-closure/63-01-SUMMARY.md`
</output>
