---
phase: 62-ux-polish-organization
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - frontend/src/app/layout.tsx
  - frontend/src/app/librarie/page.tsx
  - frontend/src/app/pipeline/page.tsx
  - frontend/src/app/login/page.tsx
  - frontend/src/app/signup/page.tsx
  - frontend/src/app/usage/page.tsx
  - frontend/src/components/video-processing/subtitle-editor.tsx
  - frontend/src/components/video-processing/secondary-videos-form.tsx
  - frontend/src/components/video-processing/progress-tracker.tsx
  - frontend/src/app/statsai/page.tsx
  - frontend/src/app/preturi/page.tsx
  - frontend/src/app/functionalitati/page.tsx
  - frontend/src/app/cum-functioneaza/page.tsx
  - frontend/src/app/contact/page.tsx
  - frontend/src/app/testimoniale/page.tsx
autonomous: true
requirements: [UX-04, UX-05]

must_haves:
  truths:
    - Every label, button, tooltip, and error message in the UI uses English — no Romanian strings on any page
    - Navigating to /statsai, /preturi, /functionalitati, /cum-functioneaza, /contact, or /testimoniale returns a 404
    - The html lang attribute is set to "en"
    - All metadata (page titles, descriptions) is in English
  artifacts:
    - frontend/src/app/layout.tsx (lang="en", English metadata)
    - Dead page directories deleted (6 directories removed)
  key_links:
    - layout.tsx metadata drives SEO and browser tab titles
    - navbar.tsx already uses English labels — no changes needed there
---

<objective>
Standardize all UI text to English and remove dead marketing pages from the routing tree.

Purpose: Eliminate mixed Romanian/English strings that confuse users, and remove vestigial marketing pages that serve no function in the production app.
Output: All UI text in English, six dead route directories deleted, layout metadata updated.
</objective>

<execution_context>
@/home/ukfdb/.claude/get-shit-done/workflows/execute-plan.md
@/home/ukfdb/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

<interfaces>
<!-- navbar.tsx is already fully English — no changes needed -->
<!-- Dead pages to remove: statsai, preturi, functionalitati, cum-functioneaza, contact, testimoniale -->
<!-- Each dead page is a single page.tsx file in its own directory under frontend/src/app/ -->

From frontend/src/app/layout.tsx (current Romanian metadata):
```typescript
export const metadata: Metadata = {
  title: "EditAI - Editare Video Inteligenta",
  description: "Platforma de analiza si automatizare video bazata pe inteligenta artificiala",
};
// html lang="ro"
```

Files with Romanian diacritics/strings (grep for [ăîâșțĂÎÂȘȚ]):
- frontend/src/app/librarie/page.tsx
- frontend/src/app/pipeline/page.tsx
- frontend/src/app/login/page.tsx
- frontend/src/app/signup/page.tsx
- frontend/src/app/usage/page.tsx
- frontend/src/components/video-processing/subtitle-editor.tsx

Files referencing Romanian route names in links/content:
- frontend/src/app/statsai/page.tsx (references other dead pages)
- frontend/src/app/functionalitati/page.tsx
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Translate all Romanian strings to English across the frontend</name>
  <files>
    frontend/src/app/layout.tsx
    frontend/src/app/librarie/page.tsx
    frontend/src/app/pipeline/page.tsx
    frontend/src/app/login/page.tsx
    frontend/src/app/signup/page.tsx
    frontend/src/app/usage/page.tsx
    frontend/src/components/video-processing/subtitle-editor.tsx
    frontend/src/components/video-processing/secondary-videos-form.tsx
    frontend/src/components/video-processing/progress-tracker.tsx
  </files>
  <action>
  1. In `frontend/src/app/layout.tsx`:
     - Change `lang="ro"` to `lang="en"` on the `<html>` element
     - Change metadata title to: `"EditAI - Smart Video Editing"`
     - Change metadata description to: `"AI-powered video analysis and automation platform"`
     - Remove or translate any Romanian comments (e.g., `// Fonturi pentru subtitrari` → `// Subtitle fonts`)

  2. Search ALL .tsx/.ts files under `frontend/src/` for Romanian strings. Use these patterns to find them:
     - Romanian diacritics: ă, î, â, ș, ț, Ă, Î, Â, Ș, Ț
     - Common Romanian words in UI: "Librărie", "Proiecte", "Încarcă", "Procesare", "Descarcă", "Șterge", "Actualizare", "Listează", "Selectează", "Nu s-a", "Eroare", "Succes", "Anulează"
     - Romanian comments starting with `//` containing Romanian text

  3. For each Romanian string found, translate to natural English:
     - UI labels/buttons: Use standard English UI conventions (e.g., "Delete" not "Remove permanently")
     - Error messages: Keep them clear and actionable
     - Comments: Translate inline comments to English
     - Toast messages: Translate any Romanian toast text
     - Docstrings in JSDoc: Translate to English

  4. Do NOT change:
     - The route path `/librarie` — it stays as-is (it is a URL, not a UI label)
     - Variable names or function names (only user-facing strings)
     - Backend Python files (separate concern)
     - Any strings that are already in English

  5. Verify no Romanian strings remain by grepping for diacritics and common Romanian words across all frontend/src files.
  </action>
  <verify>
    <automated>cd frontend && grep -rP '[ăîâșțĂÎÂȘȚ]' src/ --include='*.tsx' --include='*.ts' | grep -v 'node_modules' | grep -v '.next' | wc -l</automated>
    The count should be 0 (no Romanian diacritics in any frontend source file).
  </verify>
  <done>
  - Every user-facing string in the frontend is in English
  - html lang="en" in layout.tsx
  - Metadata title and description are in English
  - No Romanian diacritics remain in any .tsx/.ts file under frontend/src/
  </done>
</task>

<task type="auto">
  <name>Task 2: Delete dead marketing page directories</name>
  <files>
    frontend/src/app/statsai/ (DELETE)
    frontend/src/app/preturi/ (DELETE)
    frontend/src/app/functionalitati/ (DELETE)
    frontend/src/app/cum-functioneaza/ (DELETE)
    frontend/src/app/contact/ (DELETE)
    frontend/src/app/testimoniale/ (DELETE)
  </files>
  <action>
  1. Delete these six directories and all their contents:
     ```bash
     rm -rf frontend/src/app/statsai
     rm -rf frontend/src/app/preturi
     rm -rf frontend/src/app/functionalitati
     rm -rf frontend/src/app/cum-functioneaza
     rm -rf frontend/src/app/contact
     rm -rf frontend/src/app/testimoniale
     ```

  2. Search the entire frontend codebase for any remaining references to these routes:
     - `grep -r "statsai\|preturi\|functionalitati\|cum-functioneaza\|testimoniale" frontend/src/`
     - If any links or imports reference these pages, remove those references
     - The navbar.tsx does NOT reference these pages (already confirmed), so no navbar changes needed

  3. Verify that navigating to any of these routes would result in Next.js returning its default 404 page (since the page.tsx files no longer exist, Next.js App Router automatically 404s).

  4. Do NOT create a custom 404 page — Next.js default 404 handling is sufficient.
  </action>
  <verify>
    <automated>cd frontend && for route in statsai preturi functionalitati cum-functioneaza contact testimoniale; do test -d "src/app/$route" && echo "FAIL: $route still exists" || echo "OK: $route deleted"; done</automated>
    All six should print "OK: {route} deleted".
  </verify>
  <done>
  - All six dead marketing page directories are deleted
  - No remaining references to these routes in the frontend codebase
  - Navigating to any of the six URLs returns a 404
  </done>
</task>

</tasks>

<verification>
1. `grep -rP '[ăîâșțĂÎÂȘȚ]' frontend/src/ --include='*.tsx' --include='*.ts'` returns no matches
2. None of the six dead page directories exist under frontend/src/app/
3. `grep -r 'lang="ro"' frontend/src/app/layout.tsx` returns no matches
4. `grep -r 'lang="en"' frontend/src/app/layout.tsx` returns a match
5. `npm run build` in frontend/ succeeds without errors (no broken imports from deleted pages)
</verification>

<success_criteria>
- Zero Romanian strings in any frontend source file
- Six dead marketing page directories deleted
- Layout metadata is in English with lang="en"
- Frontend builds successfully
</success_criteria>

<output>
After completion, create `.planning/phases/62-ux-polish-organization/62-01-SUMMARY.md`
</output>
