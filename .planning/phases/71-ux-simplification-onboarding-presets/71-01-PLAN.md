---
phase: 71-ux-simplification-onboarding-presets
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - frontend/src/app/setup/page.tsx
  - app/api/desktop_routes.py
autonomous: true
requirements: [UX-03]

must_haves:
  truths:
    - "Setup wizard has a Free TTS preset button that skips ElevenLabs key entry"
    - "API keys show green checkmark on success and red error on failure inline"
    - "User can proceed through setup without entering an ElevenLabs key"
    - "Gemini and ElevenLabs keys are validated inline before proceeding"
  artifacts:
    - path: "frontend/src/app/setup/page.tsx"
      provides: "Enhanced setup wizard with Free TTS preset and inline validation"
      contains: "Free TTS"
    - path: "app/api/desktop_routes.py"
      provides: "Test-connection endpoint already exists"
  key_links:
    - from: "frontend/src/app/setup/page.tsx"
      to: "/api/v1/desktop/test-connection"
      via: "apiPost call for inline validation"
      pattern: "test-connection"
---

<objective>
Enhance the setup wizard (Step 2: API Keys) with a "Free TTS" preset button and inline API key validation indicators.

Purpose: First-time users can start producing videos in under 60 seconds by clicking "Free TTS" to skip ElevenLabs configuration entirely, using Edge TTS instead. API keys that are entered get validated inline with visual feedback (green checkmark / red error) before the user proceeds.

Output: Updated setup wizard page with preset button and enhanced validation UX.
</objective>

<execution_context>
@/home/ukfdb/.claude/get-shit-done/workflows/execute-plan.md
@/home/ukfdb/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@frontend/src/app/setup/page.tsx
@app/api/desktop_routes.py
</context>

<interfaces>
<!-- Existing setup wizard structure -->
From frontend/src/app/setup/page.tsx:
- 3-step wizard: Step 1 (License), Step 2 (API Keys), Step 3 (Preferences)
- Step 2 currently has: Supabase URL/Key (required), Gemini (optional), ElevenLabs (optional)
- Each service has testConnection() calling POST /api/v1/desktop/test-connection
- Status states: "idle" | "testing" | "ok" | "error" with icon indicators
- handleFinish() saves all settings via POST /desktop/settings

From app/api/desktop_routes.py:
- POST /test-connection accepts {service, url, key} — already validates supabase, gemini, elevenlabs
- POST /desktop/settings accepts {crash_reporting_enabled, supabase_url, supabase_key, gemini_api_key, elevenlabs_api_key}
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Add Free TTS preset and auto-validation to setup wizard</name>
  <files>frontend/src/app/setup/page.tsx</files>
  <action>
Modify Step 2 (API Configuration) of the setup wizard:

1. **Add "Free TTS" preset button** at the top of Step 2, before API key inputs:
   - A prominent card/button labeled "Use Free TTS (Edge TTS)" with a description like "No API key needed — use Microsoft's free text-to-speech engine"
   - When clicked: set a new state `useFreeeTts` to true, clear the elevenlabsKey field, set elevenlabsStatus to "ok" (skip validation), and show a green indicator "Edge TTS selected — no API key needed"
   - The ElevenLabs API key input section should be visually dimmed/collapsed when Free TTS is selected, with a small "Or enter ElevenLabs key instead" toggle to expand it back
   - Add state: `const [useFreeTts, setUseFreeTts] = useState(false)`

2. **Add auto-validation on blur** for Gemini and ElevenLabs key fields:
   - When the user tabs out of the Gemini key input (onBlur) and the key is non-empty, automatically call `testConnection("gemini")` — no need to click "Test" button
   - Same for ElevenLabs key input (onBlur) — auto-call `testConnection("elevenlabs")`
   - Keep the manual "Test" button as well for explicit re-testing
   - The existing status icon indicators (CheckCircle green, AlertCircle red, Loader2 spinning) already handle visual feedback — no changes needed there

3. **Update "Next" button gating** on Step 2:
   - The "Next" button to Step 3 should be enabled if EITHER:
     - (a) Free TTS is selected (no ElevenLabs key required, Gemini optional), OR
     - (b) ElevenLabs key is entered and validated (status "ok")
   - Supabase remains required — keep existing validation
   - Gemini remains optional — no gating on it
   - Add disabled state to the Next button: `disabled={supabaseStatus !== "ok" && !supabaseHint}` (allow proceeding if Supabase was previously configured in edit mode via hint)

4. **Update handleFinish** to include tts_provider preference:
   - When Free TTS is selected, include `tts_provider: "edge"` in the settings payload sent to POST /desktop/settings
   - When ElevenLabs key is provided, include `tts_provider: "elevenlabs"`
   - This allows the backend to know which TTS to use by default

Do NOT change Step 1 (License) or Step 3 (Preferences) structure.
  </action>
  <verify>
    <automated>cd "/mnt/c/OBSID SRL/n8n/edit_factory/frontend" && npx tsc --noEmit --pretty 2>&1 | head -30</automated>
  </verify>
  <done>
    - Setup wizard Step 2 shows a "Free TTS" preset button at the top
    - Clicking Free TTS dims/hides the ElevenLabs key field and shows green indicator
    - API key fields auto-validate on blur (not just manual "Test" button)
    - Status icons show green checkmark on success, red error on failure
    - User can proceed to Step 3 without entering an ElevenLabs key when Free TTS is selected
  </done>
</task>

<task type="auto">
  <name>Task 2: Add tts_provider to desktop settings endpoint</name>
  <files>app/api/desktop_routes.py</files>
  <action>
Update the POST /desktop/settings endpoint to accept and store `tts_provider` field:

1. Find the settings endpoint handler and its request model (or the dict it accepts)
2. Add `tts_provider: Optional[str] = None` to the accepted fields — valid values: "edge", "elevenlabs"
3. When `tts_provider` is provided, store it via the same mechanism used for other settings (KeyVault or config)
4. In the GET /desktop/settings response, include `tts_provider` in the returned data so the setup wizard can pre-fill in edit mode

This is a minimal backend change — the field is just stored/retrieved alongside existing settings. The actual TTS routing logic already exists from Phase 69 (fallback to Edge TTS when no ElevenLabs key).
  </action>
  <verify>
    <automated>cd "/mnt/c/OBSID SRL/n8n/edit_factory" && python -c "from app.api.desktop_routes import router; print('desktop_routes imports OK')"</automated>
  </verify>
  <done>
    - POST /desktop/settings accepts tts_provider field
    - GET /desktop/settings returns tts_provider in response
    - Existing settings behavior unchanged
  </done>
</task>

</tasks>

<verification>
- TypeScript compiles without errors
- Setup wizard renders Step 2 with Free TTS button visible
- Backend settings endpoint accepts tts_provider
</verification>

<success_criteria>
- First-time user can click "Free TTS" and proceed through wizard in under 60 seconds without entering an ElevenLabs key
- API key fields show green/red inline validation indicators
- tts_provider preference is persisted to backend settings
</success_criteria>

<output>
After completion, create `.planning/phases/71-ux-simplification-onboarding-presets/71-01-SUMMARY.md`
</output>
