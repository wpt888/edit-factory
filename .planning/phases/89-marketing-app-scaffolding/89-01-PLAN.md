---
phase: 89-marketing-app-scaffolding
plan: "01"
type: execute
wave: 3
depends_on: []
files_modified:
  - marketing/package.json
  - marketing/tsconfig.json
  - marketing/next.config.ts
  - marketing/postcss.config.mjs
  - marketing/eslint.config.mjs
  - marketing/next-env.d.ts
  - marketing/.env.example
  - marketing/app/layout.tsx
  - marketing/app/page.tsx
  - marketing/app/globals.css
  - marketing/lib/utils.ts
  - marketing/lib/supabase.ts
  - marketing/components/ui/button.tsx
  - marketing/components/ui/card.tsx
  - marketing/playwright.config.ts
  - marketing/tests/scaffold-smoke.spec.ts
autonomous: true
requirements: [MARK-01, MARK-06]
must_haves:
  truths:
    - "`marketing/package.json` declares Next.js ^16.1.1, React 19.2.1, Tailwind ^4, @supabase/ssr, @supabase/supabase-js, sonner, lucide-react, @radix-ui/react-slot, class-variance-authority, clsx, tailwind-merge as dependencies (per D-01 / D-02 / D-03 / D-04)"
    - "`marketing/package.json` `scripts.dev` is exactly `next dev --port 3001` (per D-06) so the port is enforced at the command line, not via env vars"
    - "Running `cd marketing && npm install && npm run dev` starts a Next.js dev server bound to http://localhost:3001/ that returns HTTP 200 on `/`"
    - "Visiting http://localhost:3001/ renders a server-rendered placeholder containing the literal text `Edit Factory` AND the literal text `Coming soon` (per D-09) — `marketing/app/page.tsx` contains NO `'use client'` directive"
    - "`marketing/lib/supabase.ts` exports a `getMarketingSupabase()` server-side helper that reads `process.env.MARKETING_SUPABASE_URL` + `process.env.MARKETING_SUPABASE_KEY` and throws a clear error if either is missing (per D-05 / D-12 / D-14)"
    - "`marketing/lib/supabase.ts` ALSO exports a `getMarketingBrowserClient()` helper consuming `NEXT_PUBLIC_MARKETING_SUPABASE_URL` + `NEXT_PUBLIC_MARKETING_SUPABASE_ANON_KEY` (per D-05 dual-env-var pattern documented for Phase 91 inheritance)"
    - "`marketing/.env.example` documents all four marketing env vars (MARKETING_SUPABASE_URL, MARKETING_SUPABASE_KEY, NEXT_PUBLIC_MARKETING_SUPABASE_URL, NEXT_PUBLIC_MARKETING_SUPABASE_ANON_KEY) as placeholders + commented-out future-phase keys (LEMON_SQUEEZY_*, OAUTH_*) per D-10"
    - "`cd marketing && npx tsc --noEmit` exits with code 0 — strict mode TypeScript passes across all marketing files (per D-07)"
    - "`cd marketing && npx playwright test` exits with code 0 — the scaffold-smoke spec asserts HTTP 200 on `/`, asserts page contains `Edit Factory`, and produces a screenshot at `marketing/screenshots/phase-89-scaffold.png` (per D-11 + CLAUDE.md MANDATORY rule)"
    - "Zero files outside `marketing/` are created or modified — `git diff --name-only` shows only `marketing/**` paths (root `.gitignore` already covers `node_modules/`, `.next/`, `.env.local`, `screenshots/` globally — no .gitignore changes needed)"
  artifacts:
    - path: "marketing/package.json"
      provides: "Independent Next.js 16 + Tailwind 4 + Shadcn-derived app skeleton with port-3001-locked dev script"
      contains: '"dev": "next dev --port 3001"'
    - path: "marketing/tsconfig.json"
      provides: "TypeScript 5 strict-mode config with `@/*` path alias mirroring frontend/tsconfig.json patterns (per D-07)"
      contains: '"strict": true'
    - path: "marketing/next.config.ts"
      provides: "Next.js 16 App Router config (output: standalone for future Vercel deploy compatibility; minimal — no rewrites or custom headers since this is a scaffold)"
      contains: 'output: "standalone"'
    - path: "marketing/postcss.config.mjs"
      provides: "Tailwind v4 PostCSS plugin wiring (`@tailwindcss/postcss`) — identical to frontend/postcss.config.mjs pattern"
      contains: '"@tailwindcss/postcss"'
    - path: "marketing/app/layout.tsx"
      provides: "Root server-side layout wiring globals.css + Sonner Toaster (per D-04 — Sonner is an npm dep, not a copied file; mount at root with `position=\"top-right\"`)"
      contains: 'import { Toaster } from "sonner"'
    - path: "marketing/app/page.tsx"
      provides: "Server-rendered placeholder home page (no 'use client' — per D-09) rendering `Edit Factory` hero + `Coming soon: full landing page in Phase 90` in a Card layout from copied Shadcn primitives"
      contains: "Coming soon"
    - path: "marketing/app/globals.css"
      provides: "Tailwind v4 imports + same OKLCH design tokens / `--background` / `--foreground` / etc. as frontend/src/app/globals.css for visual consistency (per D-03)"
      contains: '@import "tailwindcss"'
    - path: "marketing/lib/utils.ts"
      provides: "`cn()` helper using clsx + tailwind-merge — identical to frontend/src/lib/utils.ts core cn() (formatTime helpers omitted; not used by scaffold)"
      contains: "twMerge(clsx(inputs))"
    - path: "marketing/lib/supabase.ts"
      provides: "`getMarketingSupabase()` server-side factory (service role) + `getMarketingBrowserClient()` (anon, NEXT_PUBLIC_-prefixed); both throw on missing env vars per D-12 (no live Supabase project yet)"
      contains: "MARKETING_SUPABASE_URL"
      also_contains: "MARKETING_SUPABASE_KEY"
    - path: "marketing/components/ui/button.tsx"
      provides: "Shadcn Button primitive copied byte-for-byte from frontend/src/components/ui/button.tsx (per D-04 — copy, do NOT depend on)"
      contains: "buttonVariants"
    - path: "marketing/components/ui/card.tsx"
      provides: "Shadcn Card / CardHeader / CardTitle / CardContent / CardDescription / CardFooter / CardAction primitives copied byte-for-byte from frontend/src/components/ui/card.tsx"
      contains: "CardContent"
    - path: "marketing/playwright.config.ts"
      provides: "Playwright config with `webServer.command = npm run dev` + `url = http://localhost:3001` + `use.baseURL = http://localhost:3001` so the test runner self-manages the dev server (no manual background-process management) per D-11"
      contains: "http://localhost:3001"
    - path: "marketing/tests/scaffold-smoke.spec.ts"
      provides: "Single smoke test — navigates to `/`, asserts response status === 200, asserts page contains text `Edit Factory`, takes fullPage screenshot to `marketing/screenshots/phase-89-scaffold.png` (per CLAUDE.md MANDATORY rule)"
      contains: "Edit Factory"
    - path: "marketing/.env.example"
      provides: "Placeholder env vars for Phase 89 (MARKETING_SUPABASE_*) + commented-out future env vars (LEMON_SQUEEZY_*, OAUTH_JWT_SIGNING_KEY) for Phases 91/93 forward compatibility per D-10"
      contains: "MARKETING_SUPABASE_URL="
  key_links:
    - from: "marketing/app/page.tsx"
      to: "marketing/components/ui/card.tsx"
      via: '`import { Card, CardHeader, CardTitle, CardDescription, CardContent } from "@/components/ui/card"` — uses @/* path alias to consume copied Shadcn primitive'
      pattern: 'from "@/components/ui/card"'
    - from: "marketing/app/layout.tsx"
      to: "sonner npm package"
      via: '`import { Toaster } from "sonner"` and `<Toaster position="top-right" />` rendered after `{children}` in the body'
      pattern: 'from "sonner"'
    - from: "marketing/lib/supabase.ts"
      to: "process.env.MARKETING_SUPABASE_URL + process.env.MARKETING_SUPABASE_KEY"
      via: "`getMarketingSupabase()` reads both env vars at call time (NOT at module load) so missing-env-var error surfaces clearly via a thrown Error with the missing var name"
      pattern: "process\\.env\\.MARKETING_SUPABASE_(URL|KEY)"
    - from: "marketing/playwright.config.ts"
      to: "marketing/app/page.tsx (rendered at http://localhost:3001/)"
      via: "`webServer: { command: 'npm run dev', url: 'http://localhost:3001', reuseExistingServer: !process.env.CI, timeout: 120000 }` — Playwright spawns dev server on port 3001, waits for healthy URL, then runs spec"
      pattern: "webServer"
    - from: "marketing/package.json scripts.dev"
      to: "Next.js dev server port binding"
      via: "`next dev --port 3001` — the `--port 3001` CLI flag is the SINGLE source of truth enforcing MARK-01's port-3001 requirement (NOT an env var) per D-06"
      pattern: "next dev --port 3001"
---

<objective>
Create a new `marketing/` subfolder containing a Next.js 16 App Router app, fully independent of the existing `frontend/`, with: locked port 3001 (per D-06), Tailwind v4 + Shadcn-derived UI primitives copied from frontend/src/components/ui/ (Button + Card per D-04), a server-rendered placeholder home page (per D-09), a wired Supabase client that reads NEW env vars `MARKETING_SUPABASE_URL` + `MARKETING_SUPABASE_KEY` per MARK-06 (the actual Supabase project provisioning is a DOCUMENTED manual follow-up per D-12 / D-15 — the autonomous loop cannot navigate Supabase Web UI), and a single Playwright smoke test that asserts the placeholder page renders correctly + produces a screenshot per CLAUDE.md MANDATORY rule.

Purpose: Establish the marketing-app Track C scaffolding (per .planning/v13-desktop-production/SCOPE.md §C1 + ARCHITECTURE.md §6) so Phases 90 (landing page), 91 (Lemon Squeezy checkout + webhook), 92 (account dashboard), and 93 (OAuth endpoints) can each build on top without re-scaffolding. Closes MARK-01 + MARK-06.

Output:
- 16 new files in `marketing/` (zero files modified outside `marketing/`)
- Working `npm run dev` on port 3001 with placeholder home page
- Wired Supabase client (server + browser helpers) ready for Phase 91 consumption
- Passing Playwright smoke test with screenshot artifact
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/milestones/v13-ROADMAP.md
@.planning/milestones/v13-REQUIREMENTS.md
@.planning/v13-desktop-production/SCOPE.md
@.planning/v13-desktop-production/ARCHITECTURE.md
@CLAUDE.md
@frontend/package.json
@frontend/tsconfig.json
@frontend/next.config.ts
@frontend/postcss.config.mjs
@frontend/eslint.config.mjs
@frontend/playwright.config.ts
@frontend/components.json
@frontend/src/app/globals.css
@frontend/src/app/layout.tsx
@frontend/src/lib/utils.ts
@frontend/src/components/ui/button.tsx
@frontend/src/components/ui/card.tsx
@.gitignore

<interfaces>
<!-- Key external-package shapes the executor will consume. Extracted from existing frontend usage. -->
<!-- Executor should use these directly — no need to re-explore the codebase. -->

From `frontend/src/lib/utils.ts` (to mirror as `marketing/lib/utils.ts`):
```ts
import { clsx, type ClassValue } from "clsx"
import { twMerge } from "tailwind-merge"

export function cn(...inputs: ClassValue[]) {
  return twMerge(clsx(inputs))
}
```
(Only `cn()` needed for scaffold — drop the formatTime helpers.)

From `frontend/postcss.config.mjs` (to mirror as `marketing/postcss.config.mjs`):
```js
const config = {
  plugins: {
    "@tailwindcss/postcss": {},
  },
};

export default config;
```

From `frontend/src/components/ui/button.tsx` and `card.tsx` — these are COPIED VERBATIM into `marketing/components/ui/` (per D-04). Their only relative imports are `from "@/lib/utils"` (the `cn` helper) and external deps `@radix-ui/react-slot` (Button only) + `class-variance-authority` (Button only). No further internal imports.

From `@supabase/ssr` (for `marketing/lib/supabase.ts`):
```ts
// Server-side (service role) — for Route Handlers, server components
import { createServerClient } from "@supabase/ssr"
// Browser-side (anon role) — for client components
import { createBrowserClient } from "@supabase/ssr"
```
Both helpers take `(url: string, key: string, options)` — see https://supabase.com/docs/guides/auth/server-side/nextjs for the App Router pattern. For Phase 89's scaffold scope (no cookies yet, no auth UI per D-14), we keep the wiring minimal — just construct the client and return it. Cookie wiring lands in Phase 92 (account dashboard) and Phase 93 (OAuth).

From `sonner` (for `marketing/app/layout.tsx`):
```ts
import { Toaster } from "sonner"
// In layout body, after {children}:
<Toaster position="top-right" />
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create marketing/ root config files + install dependencies</name>
  <files>marketing/package.json, marketing/tsconfig.json, marketing/next.config.ts, marketing/postcss.config.mjs, marketing/eslint.config.mjs, marketing/next-env.d.ts</files>
  <read_first>
    - frontend/package.json (canonical stack pin: Next.js ^16.1.1, React 19.2.1, Tailwind ^4, @radix-ui/react-slot ^1.2.4, class-variance-authority ^0.7.1, clsx ^2.1.1, tailwind-merge ^3.4.0, sonner ^2.0.7, lucide-react ^0.556.0, @supabase/ssr ^0.8.0, @supabase/supabase-js ^2.89.0, @tailwindcss/postcss ^4, tw-animate-css ^1.4.0, typescript ^5, @types/node ^20, @types/react ^19, @types/react-dom ^19, eslint ^9, eslint-config-next ^16.1.1, @playwright/test ^1.57.0)
    - frontend/tsconfig.json (mirror compilerOptions exactly except: drop the plugins.next entry only if it errors — Next.js 16 + bundler moduleResolution + paths `@/*`)
    - frontend/next.config.ts (minimal — only retain `output: "standalone"`, drop images.remotePatterns and headers since scaffold has no remote images yet)
    - frontend/postcss.config.mjs (copy verbatim — Tailwind v4 single-plugin pattern)
    - frontend/eslint.config.mjs (copy verbatim — defineConfig + nextVitals + nextTs + globalIgnores list)
  </read_first>
  <action>
Create `marketing/package.json` with the following EXACT JSON (note: `"dev": "next dev --port 3001"` is the SINGLE source of truth enforcing MARK-01's port — per D-06; do NOT put the port in an .env var):
```json
{
  "name": "marketing",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "dev": "next dev --port 3001",
    "build": "next build",
    "start": "next start --port 3001",
    "lint": "eslint",
    "typecheck": "tsc --noEmit",
    "test": "npx playwright test"
  },
  "dependencies": {
    "@radix-ui/react-slot": "^1.2.4",
    "@supabase/ssr": "^0.8.0",
    "@supabase/supabase-js": "^2.89.0",
    "class-variance-authority": "^0.7.1",
    "clsx": "^2.1.1",
    "lucide-react": "^0.556.0",
    "next": "^16.1.1",
    "react": "19.2.1",
    "react-dom": "19.2.1",
    "sonner": "^2.0.7",
    "tailwind-merge": "^3.4.0"
  },
  "devDependencies": {
    "@playwright/test": "^1.57.0",
    "@tailwindcss/postcss": "^4",
    "@types/node": "^20",
    "@types/react": "^19",
    "@types/react-dom": "^19",
    "eslint": "^9",
    "eslint-config-next": "^16.1.1",
    "tailwindcss": "^4",
    "tw-animate-css": "^1.4.0",
    "typescript": "^5"
  }
}
```

Create `marketing/tsconfig.json` with the following EXACT JSON (mirrors frontend/tsconfig.json strict-mode + `@/*` path alias per D-07; the `paths` value is `./*` instead of `./src/*` because marketing/ does NOT use a `src/` directory per D-08):
```json
{
  "compilerOptions": {
    "target": "ES2017",
    "lib": ["dom", "dom.iterable", "esnext"],
    "allowJs": true,
    "skipLibCheck": true,
    "strict": true,
    "noEmit": true,
    "esModuleInterop": true,
    "module": "esnext",
    "moduleResolution": "bundler",
    "resolveJsonModule": true,
    "isolatedModules": true,
    "jsx": "react-jsx",
    "incremental": true,
    "plugins": [
      {
        "name": "next"
      }
    ],
    "paths": {
      "@/*": ["./*"]
    }
  },
  "include": [
    "next-env.d.ts",
    "**/*.ts",
    "**/*.tsx",
    ".next/types/**/*.ts",
    ".next/dev/types/**/*.ts"
  ],
  "exclude": ["node_modules"]
}
```

Create `marketing/next-env.d.ts` (Next.js auto-generates this on first build, but pre-creating prevents a 1st-build error AND lets `tsc --noEmit` pass before any build runs):
```ts
/// <reference types="next" />
/// <reference types="next/image-types/global" />

// NOTE: This file should not be edited
// see https://nextjs.org/docs/app/api-reference/config/typescript for more information.
```

Create `marketing/next.config.ts` (minimal — Phase 89 is a scaffold; rewrites/headers belong in Phase 90+ when real pages exist):
```ts
import type { NextConfig } from "next";

const nextConfig: NextConfig = {
  // Standalone output for future Vercel / Docker deploys (Phase 96+).
  output: "standalone",
};

export default nextConfig;
```

Create `marketing/postcss.config.mjs` BYTE-IDENTICAL to frontend/postcss.config.mjs:
```js
const config = {
  plugins: {
    "@tailwindcss/postcss": {},
  },
};

export default config;
```

Create `marketing/eslint.config.mjs` BYTE-IDENTICAL to frontend/eslint.config.mjs.

After all 6 files are written, run `npm install` inside `marketing/`. IMPORTANT: this is a cold install of Next.js 16 + Radix + Tailwind 4 + Playwright on Windows and will take 3–5 minutes — the Bash tool's default 120000ms timeout WILL kill it. Use `timeout: 600000` (10 minutes) when invoking the Bash tool for this step.

Command: `cd marketing && npm install`

Do NOT run `npm install` from the repo root — it would pick up the root has no package.json and fail confusingly. Always `cd marketing` first.
  </action>
  <verify>
    <automated>cd marketing && test -f package.json && test -f tsconfig.json && test -f next.config.ts && test -f postcss.config.mjs && test -f eslint.config.mjs && test -f next-env.d.ts && test -d node_modules && grep -q '"dev": "next dev --port 3001"' package.json && grep -q '"next": "^16.1.1"' package.json && grep -q '"react": "19.2.1"' package.json && grep -q '"tailwindcss": "^4"' package.json && grep -q '"@supabase/ssr"' package.json && grep -q '"sonner"' package.json && grep -q '"strict": true' tsconfig.json && grep -q '"@/\*": \["\./\*"\]' tsconfig.json</automated>
  </verify>
  <acceptance_criteria>
    - `test -f marketing/package.json` → exit 0
    - `test -f marketing/tsconfig.json` → exit 0
    - `test -f marketing/next.config.ts` → exit 0
    - `test -f marketing/postcss.config.mjs` → exit 0
    - `test -f marketing/eslint.config.mjs` → exit 0
    - `test -f marketing/next-env.d.ts` → exit 0
    - `test -d marketing/node_modules` → exit 0 (npm install ran)
    - `test -f marketing/node_modules/next/package.json` → exit 0 (Next.js installed, not just a manifest)
    - `test -f marketing/node_modules/@supabase/ssr/package.json` → exit 0
    - `test -f marketing/node_modules/sonner/package.json` → exit 0
    - `test -f marketing/node_modules/@radix-ui/react-slot/package.json` → exit 0
    - `grep -q '"dev": "next dev --port 3001"' marketing/package.json` → exit 0 (per D-06 — port locked at CLI)
    - `grep -q '"next": "\^16.1.1"' marketing/package.json` → exit 0 (per D-01, NOT Next.js 15)
    - `grep -q '"react": "19.2.1"' marketing/package.json` → exit 0 (per D-02 — exact pin)
    - `grep -q '"tailwindcss": "\^4"' marketing/package.json` → exit 0 (per D-03)
    - `grep -q '"@supabase/ssr"' marketing/package.json` → exit 0
    - `grep -q '"strict": true' marketing/tsconfig.json` → exit 0 (per D-07)
    - `grep -q '"@/\*": \["\./\*"\]' marketing/tsconfig.json` → exit 0 (per D-08 — no src/ dir)
    - NO files modified outside `marketing/` — verify via `git status --porcelain | grep -v "^?? marketing/" | wc -l` returns 0
  </acceptance_criteria>
  <done>
    All 6 config files exist with the exact content above; `npm install` has populated `marketing/node_modules/`; the port lock is asserted by grep at the JSON-string level; no files outside `marketing/` have been touched.
  </done>
</task>

<task type="auto">
  <name>Task 2: Create App Router shell + globals.css + lib/utils.ts</name>
  <files>marketing/app/layout.tsx, marketing/app/page.tsx, marketing/app/globals.css, marketing/lib/utils.ts</files>
  <read_first>
    - frontend/src/app/layout.tsx (reference — Sonner Toaster import + mount pattern; ignore ProfileProvider/AuthProvider/LicenseGuard/NavBarWrapper — they are existing-app-specific and do NOT exist in marketing/)
    - frontend/src/app/globals.css (copy ENTIRE file byte-for-byte — design tokens, `@theme inline`, `@layer base` — per D-03 visual consistency)
    - frontend/src/lib/utils.ts (copy only the `cn()` helper — drop formatTime / formatTimeShort, not needed for scaffold)
    - .planning/phases/89-marketing-app-scaffolding/89-01-PLAN.md `<interfaces>` block (page.tsx imports + layout.tsx Toaster usage)
  </read_first>
  <action>
Create `marketing/app/globals.css` byte-for-byte identical to frontend/src/app/globals.css (full OKLCH design tokens, `:root` + `.dark`, `@theme inline`, `@layer base` block — per D-03). This file is ~186 lines; copy via `cat frontend/src/app/globals.css > marketing/app/globals.css` semantics (use the Write tool with the full content).

Create `marketing/lib/utils.ts`:
```ts
import { clsx, type ClassValue } from "clsx"
import { twMerge } from "tailwind-merge"

export function cn(...inputs: ClassValue[]) {
  return twMerge(clsx(inputs))
}
```

Create `marketing/app/layout.tsx` (server component, minimal — NO ProfileProvider/AuthProvider/LicenseGuard/NavBarWrapper since they are existing-app-specific; the marketing app gets its own provider stack starting in Phase 92):
```tsx
import type { Metadata } from "next";
import { Geist, Geist_Mono } from "next/font/google";
import "./globals.css";
import { Toaster } from "sonner";

const geistSans = Geist({
  variable: "--font-geist-sans",
  subsets: ["latin"],
});

const geistMono = Geist_Mono({
  variable: "--font-geist-mono",
  subsets: ["latin"],
});

export const metadata: Metadata = {
  title: "Edit Factory — Production-Ready Video Tools",
  description: "Marketing site for Edit Factory desktop video production app.",
};

export default function RootLayout({
  children,
}: Readonly<{
  children: React.ReactNode;
}>) {
  return (
    <html lang="en" className="dark" suppressHydrationWarning>
      <body
        className={`${geistSans.variable} ${geistMono.variable} antialiased`}
      >
        {children}
        <Toaster position="top-right" theme="dark" />
      </body>
    </html>
  );
}
```

Create `marketing/app/page.tsx` (server-rendered — NO `'use client'` directive per D-09; the page MUST contain both literal strings `Edit Factory` and `Coming soon` for downstream Playwright assertion):
```tsx
import { Card, CardContent, CardDescription, CardHeader, CardTitle } from "@/components/ui/card";

export default function HomePage() {
  return (
    <main className="min-h-screen flex items-center justify-center p-8 bg-background text-foreground">
      <Card className="max-w-2xl w-full">
        <CardHeader>
          <CardTitle className="text-3xl">Edit Factory — Production-Ready Video Tools</CardTitle>
          <CardDescription className="text-base mt-2">
            Automated video production for social media creators. Scripts, voiceovers, and assembly — all on your desktop.
          </CardDescription>
        </CardHeader>
        <CardContent>
          <p className="text-muted-foreground">
            Coming soon: full landing page in Phase 90.
          </p>
        </CardContent>
      </Card>
    </main>
  );
}
```

IMPORTANT: `marketing/app/page.tsx` must NOT contain `'use client'` anywhere — D-09 requires server-render. Verify with grep before declaring task done.

Note: `marketing/components/ui/card.tsx` does not yet exist at this task's runtime — it's created in Task 3. TypeScript will surface a "Cannot find module" error temporarily. This is expected and resolves after Task 3. Run `tsc --noEmit` only as part of Task 4's acceptance, not Task 2's.
  </action>
  <verify>
    <automated>cd marketing && test -f app/layout.tsx && test -f app/page.tsx && test -f app/globals.css && test -f lib/utils.ts && grep -q "Edit Factory" app/page.tsx && grep -q "Coming soon" app/page.tsx && ! grep -q "'use client'" app/page.tsx && grep -q 'from "sonner"' app/layout.tsx && grep -q "@import \"tailwindcss\"" app/globals.css && grep -q "twMerge(clsx(inputs))" lib/utils.ts</automated>
  </verify>
  <acceptance_criteria>
    - `test -f marketing/app/layout.tsx` → exit 0
    - `test -f marketing/app/page.tsx` → exit 0
    - `test -f marketing/app/globals.css` → exit 0
    - `test -f marketing/lib/utils.ts` → exit 0
    - `grep -q "Edit Factory" marketing/app/page.tsx` → exit 0 (per D-09 placeholder content)
    - `grep -q "Coming soon" marketing/app/page.tsx` → exit 0 (per D-09 placeholder content)
    - `grep -c "'use client'" marketing/app/page.tsx` returns 0 (per D-09 — server-rendered)
    - `grep -q 'from "sonner"' marketing/app/layout.tsx` → exit 0 (per D-04 — Sonner wired)
    - `grep -q '<Toaster' marketing/app/layout.tsx` → exit 0 (per D-04 — Toaster mounted)
    - `grep -q '@import "tailwindcss"' marketing/app/globals.css` → exit 0 (per D-03 — Tailwind v4 import)
    - `grep -q "@theme inline" marketing/app/globals.css` → exit 0 (design tokens block present)
    - `grep -q "twMerge(clsx(inputs))" marketing/lib/utils.ts` → exit 0 (per cn() helper)
    - `wc -l marketing/app/globals.css` is at least 180 (full design-token block — not a stub)
  </acceptance_criteria>
  <done>
    Layout + page render with the placeholder text; globals.css carries the full design-token block (visual consistency with desktop frontend per D-03); cn() helper is wired; Sonner Toaster is mounted at root. The page is server-rendered (no `'use client'`). Task 2 leaves a temporary TS import error for `@/components/ui/card` that Task 3 resolves.
  </done>
</task>

<task type="auto">
  <name>Task 3: Copy Shadcn primitives (Button + Card) into marketing/components/ui/</name>
  <files>marketing/components/ui/button.tsx, marketing/components/ui/card.tsx</files>
  <read_first>
    - frontend/src/components/ui/button.tsx (copy VERBATIM — only relative import is `@/lib/utils`, external deps `@radix-ui/react-slot` + `class-variance-authority` are already declared in marketing/package.json from Task 1)
    - frontend/src/components/ui/card.tsx (copy VERBATIM — only relative import is `@/lib/utils`)
  </read_first>
  <action>
Copy `frontend/src/components/ui/button.tsx` BYTE-IDENTICAL into `marketing/components/ui/button.tsx`. The file uses `import { cn } from "@/lib/utils"` — this resolves correctly because `marketing/tsconfig.json` `paths."@/*"` points to `./*` (marketing root) and `marketing/lib/utils.ts` was created in Task 2.

Copy `frontend/src/components/ui/card.tsx` BYTE-IDENTICAL into `marketing/components/ui/card.tsx`. Same `@/lib/utils` import resolution.

Per D-04, only `Button` + `Card` are copied for Phase 89's placeholder home page. Phase 90 will copy additional primitives (Input, Label, etc.) as the landing page needs them. Do NOT copy any other Shadcn primitives in Phase 89 — that would expand scope.

Per D-04, Sonner's `Toaster` is NOT a copied primitive (sonner is a separate npm package, not a Shadcn-recipe file in frontend/src/components/ui/) — it's already wired in `marketing/app/layout.tsx` via `import { Toaster } from "sonner"` (Task 2).

After both files are written, run `cd marketing && npx tsc --noEmit` and verify exit code 0 — this confirms the @/* path alias resolves AND there are no type errors across page.tsx + layout.tsx + button.tsx + card.tsx + utils.ts.

If tsc fails on `@/lib/utils` resolution, check that `marketing/tsconfig.json` has `"paths": { "@/*": ["./*"] }` (NOT `["./src/*"]` — marketing has no src dir per D-08).
  </action>
  <verify>
    <automated>cd marketing && test -f components/ui/button.tsx && test -f components/ui/card.tsx && grep -q "buttonVariants" components/ui/button.tsx && grep -q "CardContent" components/ui/card.tsx && grep -q "@radix-ui/react-slot" components/ui/button.tsx && grep -q '@/lib/utils' components/ui/button.tsx && grep -q '@/lib/utils' components/ui/card.tsx && npx tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - `test -f marketing/components/ui/button.tsx` → exit 0
    - `test -f marketing/components/ui/card.tsx` → exit 0
    - `grep -q "buttonVariants" marketing/components/ui/button.tsx` → exit 0
    - `grep -q "@radix-ui/react-slot" marketing/components/ui/button.tsx` → exit 0
    - `grep -q "CardContent" marketing/components/ui/card.tsx` → exit 0
    - `grep -q "@/lib/utils" marketing/components/ui/button.tsx` → exit 0
    - `grep -q "@/lib/utils" marketing/components/ui/card.tsx` → exit 0
    - `cd marketing && npx tsc --noEmit` exits 0 — strict-mode TS passes across ALL files (per D-07)
    - `diff frontend/src/components/ui/button.tsx marketing/components/ui/button.tsx` returns empty (per D-04 — byte-for-byte copy)
    - `diff frontend/src/components/ui/card.tsx marketing/components/ui/card.tsx` returns empty
    - No additional Shadcn primitives copied (per D-04 — only Button + Card for Phase 89; Phase 90 expands)
    - `ls marketing/components/ui/ | wc -l` returns exactly 2 (only button.tsx + card.tsx)
  </acceptance_criteria>
  <done>
    Button + Card are byte-identical copies of the frontend primitives, importing `@/lib/utils` (resolves via tsconfig paths to `marketing/lib/utils.ts`). `tsc --noEmit` passes cleanly — the entire marketing TypeScript graph (layout → page → card → utils) compiles in strict mode.
  </done>
</task>

<task type="auto">
  <name>Task 4: Create Supabase client wiring + .env.example</name>
  <files>marketing/lib/supabase.ts, marketing/.env.example</files>
  <read_first>
    - .planning/milestones/v13-REQUIREMENTS.md line 34 (MARK-06: separate Supabase project, zero shared users with existing web app)
    - .planning/phases/89-marketing-app-scaffolding/89-01-PLAN.md frontmatter D-05 / D-10 / D-12 / D-14 / D-15 (env var naming, dual-helper pattern, no actual project provisioning yet)
    - frontend/src/lib/utils.ts (existing util import style)
  </read_first>
  <action>
Create `marketing/lib/supabase.ts` with TWO factory functions — both throw clear errors when env vars are missing (per D-12: the autonomous loop cannot create a live Supabase project; missing-env behavior is by design and surfaces as a thrown Error, NOT silent undefined):

```ts
import { createBrowserClient, createServerClient, type CookieOptions } from "@supabase/ssr";

/**
 * Server-side Supabase client for the Marketing app.
 *
 * Uses the SERVICE-ROLE key (MARKETING_SUPABASE_KEY — NO NEXT_PUBLIC_ prefix,
 * so it is NEVER bundled into the browser). Use this for:
 *   - Route Handlers (e.g., marketing/app/api/lemon-squeezy/webhook/route.ts in Phase 91)
 *   - Server Components reading privileged data
 *   - Server actions
 *
 * Phase 89 scope: scaffold only. Cookie wiring is intentionally minimal —
 * Phase 92 (account dashboard) and Phase 93 (OAuth) will extend this with
 * proper cookie read/write callbacks.
 *
 * MARK-06: this connects to a SEPARATE Supabase project from the existing
 * `frontend/` app — zero shared users.
 *
 * Per D-12: the Supabase project does NOT yet exist. Missing env vars
 * surface as a thrown Error with the specific missing var name, NOT a
 * silent undefined that would create a 500 at request time without context.
 */
export function getMarketingSupabase() {
  const url = process.env.MARKETING_SUPABASE_URL;
  const key = process.env.MARKETING_SUPABASE_KEY;

  if (!url) {
    throw new Error(
      "MARKETING_SUPABASE_URL is not set. " +
        "Provision the marketing Supabase project and add the URL to marketing/.env.local. " +
        "See marketing/.env.example for the full env contract."
    );
  }
  if (!key) {
    throw new Error(
      "MARKETING_SUPABASE_KEY is not set. " +
        "Add the service-role key from the Supabase dashboard to marketing/.env.local. " +
        "Do NOT prefix with NEXT_PUBLIC_ — this is a server-only secret."
    );
  }

  // Phase 89: scaffold-mode cookie callbacks. Real cookie wiring lands in Phase 92/93.
  return createServerClient(url, key, {
    cookies: {
      get() {
        return undefined;
      },
      set(_name: string, _value: string, _options: CookieOptions) {
        // no-op in scaffold; Phase 92 wires this to next/headers cookies()
      },
      remove(_name: string, _options: CookieOptions) {
        // no-op in scaffold
      },
    },
  });
}

/**
 * Browser-side Supabase client for the Marketing app.
 *
 * Uses the ANON key (NEXT_PUBLIC_MARKETING_SUPABASE_ANON_KEY — safe to ship
 * to the browser; row-level security on the Supabase side gates access).
 *
 * Use this for:
 *   - Client components that need auth state
 *   - Phase 92 `/account` UI reading the user's own subscription
 *
 * Per D-05 dual-env-var pattern: server-side uses MARKETING_SUPABASE_URL +
 * MARKETING_SUPABASE_KEY (service role); browser-side uses NEXT_PUBLIC-prefixed
 * vars (URL is duplicated so the prefix difference is the only flag — Next.js's
 * NEXT_PUBLIC_ mechanism only exposes prefixed vars to the client bundle).
 */
export function getMarketingBrowserClient() {
  const url = process.env.NEXT_PUBLIC_MARKETING_SUPABASE_URL;
  const anonKey = process.env.NEXT_PUBLIC_MARKETING_SUPABASE_ANON_KEY;

  if (!url) {
    throw new Error(
      "NEXT_PUBLIC_MARKETING_SUPABASE_URL is not set. " +
        "Add it to marketing/.env.local (and the Vercel project env in Phase 96 deploy). " +
        "See marketing/.env.example."
    );
  }
  if (!anonKey) {
    throw new Error(
      "NEXT_PUBLIC_MARKETING_SUPABASE_ANON_KEY is not set. " +
        "Add the anon key from the Supabase dashboard to marketing/.env.local."
    );
  }

  return createBrowserClient(url, anonKey);
}
```

Create `marketing/.env.example` documenting ALL marketing env vars including future-phase placeholders (per D-10):
```
# ─── Marketing App env vars ───
# Copy this file to marketing/.env.local and fill in real values.
# marketing/.env.local is gitignored (root .gitignore line 4).

# ─── Supabase (Phase 89 — MARK-06) ───
# A SEPARATE Supabase project from the existing app.
# Service-role key — SERVER-ONLY (do NOT prefix with NEXT_PUBLIC_).
MARKETING_SUPABASE_URL=
MARKETING_SUPABASE_KEY=

# Anon key — browser-safe (NEXT_PUBLIC_ prefix exposes to client bundle).
NEXT_PUBLIC_MARKETING_SUPABASE_URL=
NEXT_PUBLIC_MARKETING_SUPABASE_ANON_KEY=

# ─── Lemon Squeezy (Phase 91 — MARK-03 / MARK-04) ───
# Uncomment + populate when Phase 91 starts.
# LEMON_SQUEEZY_API_KEY=
# LEMON_SQUEEZY_WEBHOOK_SECRET=
# LEMON_SQUEEZY_STORE_ID=
# LEMON_SQUEEZY_VARIANT_STARTER=
# LEMON_SQUEEZY_VARIANT_PRO=
# LEMON_SQUEEZY_VARIANT_CLOUD_SYNC=

# ─── Resend (Phase 91 — license-key email delivery) ───
# RESEND_API_KEY=
# RESEND_FROM_EMAIL=

# ─── OAuth signing (Phase 93 — OAUTH-02) ───
# JWT signing key for marketing-app-issued tokens.
# Generate with: openssl rand -base64 64
# OAUTH_JWT_SIGNING_KEY=
```

After both files exist, re-run `cd marketing && npx tsc --noEmit` to confirm the Supabase import + types resolve cleanly (no `Cannot find module '@supabase/ssr'` — it's installed via Task 1).
  </action>
  <verify>
    <automated>cd marketing && test -f lib/supabase.ts && test -f .env.example && grep -q "getMarketingSupabase" lib/supabase.ts && grep -q "getMarketingBrowserClient" lib/supabase.ts && grep -q "MARKETING_SUPABASE_URL" lib/supabase.ts && grep -q "MARKETING_SUPABASE_KEY" lib/supabase.ts && grep -q "NEXT_PUBLIC_MARKETING_SUPABASE_URL" lib/supabase.ts && grep -q "NEXT_PUBLIC_MARKETING_SUPABASE_ANON_KEY" lib/supabase.ts && grep -q "createServerClient" lib/supabase.ts && grep -q "createBrowserClient" lib/supabase.ts && grep -q "MARKETING_SUPABASE_URL=" .env.example && grep -q "MARKETING_SUPABASE_KEY=" .env.example && grep -q "LEMON_SQUEEZY" .env.example && grep -q "OAUTH_JWT_SIGNING_KEY" .env.example && npx tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - `test -f marketing/lib/supabase.ts` → exit 0
    - `test -f marketing/.env.example` → exit 0
    - `grep -q "export function getMarketingSupabase" marketing/lib/supabase.ts` → exit 0 (server helper exported)
    - `grep -q "export function getMarketingBrowserClient" marketing/lib/supabase.ts` → exit 0 (browser helper exported per D-05 dual-pattern)
    - `grep -q "process\.env\.MARKETING_SUPABASE_URL" marketing/lib/supabase.ts` → exit 0 (MARK-06 — exact env var name)
    - `grep -q "process\.env\.MARKETING_SUPABASE_KEY" marketing/lib/supabase.ts` → exit 0 (MARK-06 — exact env var name)
    - `grep -q "process\.env\.NEXT_PUBLIC_MARKETING_SUPABASE_URL" marketing/lib/supabase.ts` → exit 0 (per D-05)
    - `grep -q "process\.env\.NEXT_PUBLIC_MARKETING_SUPABASE_ANON_KEY" marketing/lib/supabase.ts` → exit 0 (per D-05)
    - `grep -q "createServerClient" marketing/lib/supabase.ts` → exit 0 (uses @supabase/ssr)
    - `grep -q "createBrowserClient" marketing/lib/supabase.ts` → exit 0
    - `grep -q "throw new Error" marketing/lib/supabase.ts` → exit 0 (missing-env throws, not silent undefined — per D-12 / T-89-04)
    - `! grep -q "NEXT_PUBLIC_SUPABASE_URL" marketing/lib/supabase.ts` (existing app's env-var name MUST NOT leak — per MARK-06)
    - `grep -q "MARKETING_SUPABASE_URL=" marketing/.env.example` → exit 0 (per D-10)
    - `grep -q "LEMON_SQUEEZY" marketing/.env.example` → exit 0 (future-phase placeholders documented)
    - `grep -q "OAUTH_JWT_SIGNING_KEY" marketing/.env.example` → exit 0 (Phase 93 forward compat)
    - `grep -q "^MARKETING_SUPABASE_URL=$" marketing/.env.example` → exit 0 (value is empty — placeholder only, per T-89-04 mitigation)
    - `cd marketing && npx tsc --noEmit` exits 0 — Supabase types resolve cleanly across the project
  </acceptance_criteria>
  <done>
    Both Supabase helpers (server + browser) exist with the exact env var names from MARK-06 + D-05; missing env vars throw clear errors (per D-12, no live Supabase project yet); `.env.example` documents Phase 89 vars + commented-out future-phase vars per D-10; TypeScript compiles cleanly across the whole marketing/ tree.
  </done>
</task>

<task type="auto">
  <name>Task 5: Playwright smoke test (asserts page + takes screenshot per CLAUDE.md MANDATORY rule)</name>
  <files>marketing/playwright.config.ts, marketing/tests/scaffold-smoke.spec.ts</files>
  <read_first>
    - frontend/playwright.config.ts (canonical baseline: defineConfig + devices.Desktop Chrome + webServer block with reuseExistingServer)
    - frontend/tests/smoke/quick-screenshot.spec.ts (existing screenshot-only smoke pattern — page.goto + waitForLoadState + page.screenshot fullPage)
    - CLAUDE.md (MANDATORY Playwright screenshot rule for frontend UI changes — phase 89 IS a frontend UI scaffold)
  </read_first>
  <action>
Create `marketing/playwright.config.ts` — configured so Playwright self-manages the dev server on port 3001 (NO manual `npm run dev &` + `kill` background-process dance):
```ts
import { defineConfig, devices } from '@playwright/test';

export default defineConfig({
  testDir: './tests',
  fullyParallel: false,
  forbidOnly: !!process.env.CI,
  retries: process.env.CI ? 2 : 0,
  workers: 1,
  reporter: 'list',
  timeout: 60000,
  use: {
    baseURL: process.env.PLAYWRIGHT_BASE_URL || 'http://localhost:3001',
    trace: 'on-first-retry',
    screenshot: 'only-on-failure',
  },
  projects: [
    {
      name: 'chromium',
      use: { ...devices['Desktop Chrome'] },
    },
  ],
  webServer: {
    command: 'npm run dev',
    url: 'http://localhost:3001',
    reuseExistingServer: !process.env.CI,
    timeout: 120000,
  },
});
```

Create `marketing/tests/scaffold-smoke.spec.ts` — single test that asserts HTTP 200 + literal-text presence + writes a screenshot to `marketing/screenshots/phase-89-scaffold.png` (per CLAUDE.md MANDATORY rule for frontend changes):
```ts
import { test, expect } from '@playwright/test';

test.describe('Phase 89: marketing app scaffold smoke', () => {
  test('renders placeholder home page on port 3001 with required text', async ({ page }) => {
    // Step 1: navigate to the placeholder page (Playwright auto-starts `npm run dev` via webServer config).
    const response = await page.goto('/');

    // Step 2: assert HTTP 200 — MARK-01 success criterion #2.
    expect(response, 'page.goto should return a Response').not.toBeNull();
    expect(response?.status(), 'home page should return HTTP 200').toBe(200);

    // Step 3: wait for the page to settle (Next.js hydration + Tailwind class application).
    await page.waitForLoadState('networkidle');

    // Step 4: assert the literal placeholder text is present (per D-09).
    await expect(page.getByText('Edit Factory', { exact: false })).toBeVisible();
    await expect(page.getByText('Coming soon', { exact: false })).toBeVisible();

    // Step 5: full-page screenshot per CLAUDE.md MANDATORY Playwright rule.
    await page.screenshot({
      path: 'screenshots/phase-89-scaffold.png',
      fullPage: true,
    });
  });
});
```

Run the test:
- Command: `cd marketing && npx playwright install --with-deps chromium` (one-time browser-binary download — needed on first run; subsequent runs cached)
- Command: `cd marketing && npx playwright test`
- Playwright's `webServer` block auto-starts `npm run dev` (port 3001), waits for `http://localhost:3001` to become healthy (timeout 120000ms), runs the spec, then tears down the server.
- The browser-binary download step may take 1–2 minutes. Use `timeout: 300000` (5 min) on the install command. The test itself runs in <30 seconds.

After the test passes:
- Verify `marketing/screenshots/phase-89-scaffold.png` exists.
- Per .gitignore line 114 (`screenshots/`), this PNG is gitignored — that's intentional. The CLAUDE.md rule requires the screenshot exist for human review during the task, not that it be committed.
- Do NOT add the screenshot to git.

If the test fails with `expect(response?.status()).toBe(200)`, the dev server failed to start — check for port-3001 collision (T-89-02). The fix is to stop whatever process is bound to 3001, then re-run.

If the test fails with `Cannot find module '@/components/ui/card'`, Task 3 was skipped or the tsconfig paths alias is wrong — re-verify Task 3 acceptance criteria.
  </action>
  <verify>
    <automated>cd marketing && test -f playwright.config.ts && test -f tests/scaffold-smoke.spec.ts && grep -q "http://localhost:3001" playwright.config.ts && grep -q "webServer" playwright.config.ts && grep -q "Edit Factory" tests/scaffold-smoke.spec.ts && grep -q "Coming soon" tests/scaffold-smoke.spec.ts && grep -q "page-89-scaffold.png\|phase-89-scaffold.png" tests/scaffold-smoke.spec.ts && npx playwright test --reporter=list && test -f screenshots/phase-89-scaffold.png</automated>
  </verify>
  <acceptance_criteria>
    - `test -f marketing/playwright.config.ts` → exit 0
    - `test -f marketing/tests/scaffold-smoke.spec.ts` → exit 0
    - `grep -q "http://localhost:3001" marketing/playwright.config.ts` → exit 0 (port locked at test-config level)
    - `grep -q "webServer" marketing/playwright.config.ts` → exit 0 (Playwright self-manages dev server)
    - `grep -q "reuseExistingServer" marketing/playwright.config.ts` → exit 0 (CI vs local toggle)
    - `grep -q "expect(response\?.status()).toBe(200)" marketing/tests/scaffold-smoke.spec.ts` → exit 0 (HTTP 200 assertion — MARK-01 SC2)
    - `grep -q "Edit Factory" marketing/tests/scaffold-smoke.spec.ts` → exit 0 (text assertion — D-09)
    - `grep -q "Coming soon" marketing/tests/scaffold-smoke.spec.ts` → exit 0 (text assertion — D-09)
    - `grep -q "phase-89-scaffold.png" marketing/tests/scaffold-smoke.spec.ts` → exit 0 (screenshot path locked)
    - `cd marketing && npx playwright test` exits 0 — smoke test passes
    - `test -f marketing/screenshots/phase-89-scaffold.png` → exit 0 (screenshot artifact created per CLAUDE.md MANDATORY rule)
    - Screenshot file size > 1000 bytes (sanity check — empty PNG header would be ~70 bytes; a real rendered page is typically 50KB+)
    - Screenshot file is NOT committed to git (gitignored via root .gitignore line 114 `screenshots/`)
  </acceptance_criteria>
  <done>
    Playwright config locks port 3001 + self-manages dev server (no manual background-process management); smoke spec asserts HTTP 200 + both literal placeholder strings + writes the mandatory screenshot; `npx playwright test` exits 0; the screenshot PNG exists locally but is gitignored. All three MARK-01 success criteria (package.json exists, npm run dev port 3001, placeholder home page) are now empirically verified by the smoke test.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| `marketing/lib/supabase.ts` ↔ Supabase service-role API | Service-role key in `MARKETING_SUPABASE_KEY` grants full DB access — must NEVER reach the browser bundle |
| `marketing/lib/supabase.ts` ↔ Supabase anon API (browser) | Anon key in `NEXT_PUBLIC_MARKETING_SUPABASE_ANON_KEY` is browser-safe — RLS on Supabase side gates access |
| `marketing/.env.example` ↔ git history | Example file MUST contain only empty placeholders — accidental real-key commit pollutes history permanently |
| Local port 3001 ↔ other dev processes | Single OS-wide TCP port; collision with any other process bound to 3001 fails loudly via `EADDRINUSE` |
| `marketing/` ↔ existing `frontend/` | Visual-design drift over time (Shadcn primitives copied, not shared) — accepted v13 tradeoff |
| Next.js 16 ↔ Next.js 15 expectations | Requirement text says Next.js 15 but actual install is 16.1.1 (per D-01) — verifier/audit MUST treat the version bump as a locked decision, not a deviation |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-89-01 | Information Disclosure | `marketing/lib/supabase.ts` service-role key | mitigate | `MARKETING_SUPABASE_KEY` is read ONLY in `getMarketingSupabase()` (server helper) and NEVER referenced with the `NEXT_PUBLIC_` prefix. The browser helper `getMarketingBrowserClient()` uses a separate `NEXT_PUBLIC_MARKETING_SUPABASE_ANON_KEY`. Next.js's `NEXT_PUBLIC_` mechanism is the boundary — only prefixed vars enter the client bundle. Acceptance criterion in Task 4 grep-asserts the absence of `NEXT_PUBLIC_MARKETING_SUPABASE_KEY` (no anon-key-in-service-role-slot inversion). |
| T-89-02 | Denial of Service | Port 3001 collision | accept | Accepted v1 risk — the `--port 3001` flag in `package.json scripts.dev` AND the Playwright `webServer.url` both fail loud with `EADDRINUSE` if 3001 is occupied. No silent fallback to another port. Documented manual recovery: stop the colliding process. |
| T-89-03 | Tampering | Shadcn drift between desktop and marketing UI | accept | Accepted per .planning/v13-desktop-production/SCOPE.md "don't touch existing app" constraint. Phase 89 copies Button + Card byte-for-byte from `frontend/src/components/ui/`. Future drift is a Phase 100+ refactor candidate (extract `@editfactory/ui` workspace package). Diff-assertion in Task 3 AC catches deliberate drift at Phase 89 time. |
| T-89-04 | Information Disclosure | `marketing/.env.example` accidentally committed with real keys | mitigate | `.env.example` template has ONLY empty placeholder values (`MARKETING_SUPABASE_URL=` with nothing after the equals sign). Real values go in `marketing/.env.local`, which is gitignored via root `.gitignore` line 4 (`.env.local`) AND line 6 (`.env*.local`). Task 4 AC grep-asserts the empty-placeholder pattern `^MARKETING_SUPABASE_URL=$`. |
| T-89-05 | Tampering | Next.js 16.1.1 vs requirement-stated Next.js 15 breaking changes | accept | Accepted per D-01. The existing `frontend/` has already validated this stack (Next.js 16 + React 19.2.1 + Tailwind 4 + 17 Radix primitives + Supabase SSR) in production. SUMMARY.md will explicitly document the version bump so the verifier/audit treats it as locked, not a deviation from MARK-01's literal text. |
| T-89-06 | Spoofing / Tampering | TypeScript strict-mode regressions in scaffolded code | mitigate | `tsc --noEmit` is asserted exit 0 in BOTH Task 3 AC (after Shadcn copy) AND Task 4 AC (after Supabase wire) AND embedded in Playwright `webServer.command = npm run dev` (Next.js build invokes typecheck implicitly during dev start). Three independent type-check gates before the Playwright smoke runs. Strict mode (`"strict": true` in tsconfig.json) is grep-asserted in Task 1 AC. |

**STRIDE distribution**: 0 high, 3 mitigate, 3 accept. All mitigations point to specific acceptance criteria, not generic advice.
</threat_model>

<verification>
After all 5 tasks complete, run these end-to-end gates from the repo root:

1. **File presence**: `cd marketing && ls -la package.json tsconfig.json next.config.ts postcss.config.mjs eslint.config.mjs next-env.d.ts .env.example app/layout.tsx app/page.tsx app/globals.css lib/utils.ts lib/supabase.ts components/ui/button.tsx components/ui/card.tsx playwright.config.ts tests/scaffold-smoke.spec.ts` — all 16 files exist.

2. **TypeScript clean**: `cd marketing && npx tsc --noEmit` exits 0.

3. **Lint clean**: `cd marketing && npx eslint .` exits 0 (or only emits warnings — no errors).

4. **Smoke test passes**: `cd marketing && npx playwright test` exits 0 + `marketing/screenshots/phase-89-scaffold.png` exists.

5. **Port locked**: `grep -q '"dev": "next dev --port 3001"' marketing/package.json` exits 0.

6. **MARK-06 env vars present**: `grep -q "MARKETING_SUPABASE_URL" marketing/lib/supabase.ts && grep -q "MARKETING_SUPABASE_KEY" marketing/lib/supabase.ts` exits 0.

7. **Zero existing-frontend bleed**: `grep -r "NEXT_PUBLIC_SUPABASE_URL" marketing/ | wc -l` returns 0 (the existing-app env var name does NOT appear in marketing/ — different Supabase project per MARK-06).

8. **No files outside marketing/**: `git status --porcelain | grep -v "^?? marketing/" | grep -v "^?? \.planning/phases/89-" | wc -l` returns 0 (only marketing/ AND the planning artifact directory are touched).

9. **Decision traceability**: Each of D-01 through D-15 is honored by at least one acceptance criterion across Tasks 1–5 (verifier should cross-reference SUMMARY.md against this list).
</verification>

<success_criteria>
**Phase 89 success criteria** (from .planning/milestones/v13-ROADMAP.md Phase 89 detail):

1. `marketing/package.json` exists with Next.js 16 (per D-01 — bumped from requirement-text Next.js 15), Tailwind 4, Shadcn-derived dependencies. — Satisfied by Task 1 + Task 3 acceptance criteria.

2. `npm run dev` in `marketing/` starts a server on port 3001 with a placeholder home page. — Satisfied by Task 1 (`scripts.dev = next dev --port 3001`) + Task 2 (page renders) + Task 5 (Playwright proves it works end-to-end).

3. Supabase client in `marketing/lib/supabase.ts` uses env vars `MARKETING_SUPABASE_URL` and `MARKETING_SUPABASE_KEY` (distinct from existing app). — Satisfied by Task 4 acceptance criteria (both env var names grep-asserted; existing-app env var name `NEXT_PUBLIC_SUPABASE_URL` grep-asserted absent).

**Planner-discovered must-haves**:

4. `tsc --noEmit` clean across the entire marketing/ TypeScript graph (strict mode per D-07). — Asserted in Tasks 3 + 4 AC.

5. Playwright smoke test passes with screenshot artifact per CLAUDE.md MANDATORY rule. — Asserted in Task 5 AC.

6. Zero files modified outside `marketing/` — clean greenfield scaffold with no side effects on existing `app/`, `frontend/`, `electron/`. — Asserted in verification gate #8.

7. All 15 locked decisions (D-01 through D-15) honored — verifier cross-references SUMMARY.md.
</success_criteria>

<output>
After completion, create `.planning/phases/89-marketing-app-scaffolding/89-01-SUMMARY.md` documenting:

1. **Files created** (the 16 marketing/ files listed in `files_modified` frontmatter).
2. **Decisions honored**: explicit table mapping D-01 → Task 1 (Next.js 16 not 15), D-02 → Task 1 (React 19.2.1), D-03 → Task 2 (Tailwind 4 imports), D-04 → Task 3 (Button + Card copy + Sonner via npm), D-05 → Task 4 (dual env var pattern), D-06 → Task 1 (--port 3001 CLI flag), D-07 → Task 1 (strict TS), D-08 → Task 1 + Task 2 (App Router, no src/), D-09 → Task 2 (server-rendered placeholder), D-10 → Task 4 (.env.example with future-phase placeholders), D-11 → Task 5 (Playwright + screenshot), D-12 → Task 4 (throw on missing env), D-13 → not in scope (no Vercel deploy in 89), D-14 → Task 4 (client exported, no UI), D-15 → SUMMARY follow-ups section.
3. **Acceptance-criteria evidence**: for each task, paste actual grep/test output proving the ACs pass.
4. **Manual follow-ups (DOCUMENTED, NOT executed — per D-15)**:
   - (a) Provision the marketing Supabase project (Supabase Web UI navigation — autonomous loop cannot perform).
   - (b) Wire `MARKETING_SUPABASE_URL` / `MARKETING_SUPABASE_KEY` / `NEXT_PUBLIC_MARKETING_SUPABASE_URL` / `NEXT_PUBLIC_MARKETING_SUPABASE_ANON_KEY` into `marketing/.env.local` (gitignored).
   - (c) Add `marketing/` to Vercel project (Phase 96 territory).
   - These follow-ups are NOT autonomous-loop blockers — they're required before Phase 91 (Lemon Squeezy webhook handler) starts.
5. **Version-bump justification**: explicit paragraph documenting that the requirement text says "Next.js 15" but Phase 89 ships Next.js 16.1.1 per D-01 — rationale: SCOPE.md §C1 "matching the desktop's design system" + existing frontend already on Next.js 16 + ARCHITECTURE.md §6 "matches existing repo conventions". Verifier/audit MUST NOT flag this as a MARK-01 deviation.
6. **MARK-01 + MARK-06 closure**: explicit statement that both requirements are satisfied by Phase 89 + flip to `[x]` in `.planning/milestones/v13-REQUIREMENTS.md` line 29 (MARK-01) + line 34 (MARK-06) is documented as a post-execution follow-up.
</output>
