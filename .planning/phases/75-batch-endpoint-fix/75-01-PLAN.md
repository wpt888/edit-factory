---
phase: 75-batch-endpoint-fix
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - frontend/src/components/batch-upload-queue.tsx
autonomous: true
requirements:
  - UX-05
gap_closure: true
must_haves:
  truths:
    - "BatchUploadQueue calls /api/v1/library/projects/{id}/generate (not /generate-raw)"
    - "Queueing a video for batch processing does not produce a 404 error"
  artifacts:
    - path: "frontend/src/components/batch-upload-queue.tsx"
      provides: "Batch upload queue with correct endpoint URL"
      contains: "/generate"
  key_links:
    - from: "frontend/src/components/batch-upload-queue.tsx"
      to: "app/api/library_routes.py"
      via: "apiFetch POST to /library/projects/{id}/generate"
      pattern: "library/projects.*generate[^-]"
---

<objective>
Fix the BatchUploadQueue component so batch video processing works end-to-end by correcting the backend endpoint URL from `/generate-raw` (which does not exist) to `/generate` (the actual route at library_routes.py:761).

Purpose: Close INT-03 and FLOW-02 from the v12 milestone audit — every batch job currently fails with a 404 at the clip generation step.
Output: Working batch upload flow that processes queued videos sequentially without errors.
</objective>

<execution_context>
@/home/ukfdb/.claude/get-shit-done/workflows/execute-plan.md
@/home/ukfdb/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md

@frontend/src/components/batch-upload-queue.tsx
@app/api/library_routes.py
</context>

<interfaces>
<!-- The backend endpoint the batch queue must call -->
From app/api/library_routes.py (line 761):
```python
@router.post("/projects/{project_id}/generate")
async def generate_raw_clips(
    request: Request,
    background_tasks: BackgroundTasks,
    project_id: str,
    video: UploadFile = File(default=None),
    video_path: str = Form(default=None),
    variant_count: int = Form(default=3),
    profile: ProfileContext = Depends(get_profile_context)
):
```
Router is mounted at `/api/v1/library`, so full path is `/api/v1/library/projects/{project_id}/generate`.

From frontend/src/components/batch-upload-queue.tsx (line 233-234, the bug):
```typescript
const genRes = await apiFetch(
  `/library/projects/${projectId}/generate-raw`,  // BUG: should be /generate
```
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Fix batch endpoint URL and update comment</name>
  <files>frontend/src/components/batch-upload-queue.tsx</files>
  <action>
    In batch-upload-queue.tsx, change line 234 from:
      `/library/projects/${projectId}/generate-raw`
    to:
      `/library/projects/${projectId}/generate`

    Also update the comment on line 228 from "Upload + generate raw clips" to "Upload + generate clips" to match the corrected endpoint name.

    Do NOT change any other code in this file. This is a single-line URL fix.
  </action>
  <verify>
    <automated>cd "/mnt/c/OBSID SRL/n8n/edit_factory" && grep -n "generate-raw" frontend/src/components/batch-upload-queue.tsx | wc -l | grep -q "^0$" && grep -n "/generate" frontend/src/components/batch-upload-queue.tsx | head -3 && echo "PASS: no generate-raw references remain"</automated>
  </verify>
  <done>The string "generate-raw" does not appear anywhere in batch-upload-queue.tsx. The apiFetch call on line ~234 uses `/library/projects/${projectId}/generate`.</done>
</task>

<task type="auto">
  <name>Task 2: Verify no other references to generate-raw in frontend</name>
  <files>frontend/src/components/batch-upload-queue.tsx</files>
  <action>
    Search the entire frontend directory for any remaining references to "generate-raw" to ensure no other component uses the non-existent endpoint. If any are found, fix them to use "/generate" instead.

    Expected: Only planning/documentation files should reference "generate-raw" (describing the bug). No source code files should.
  </action>
  <verify>
    <automated>cd "/mnt/c/OBSID SRL/n8n/edit_factory" && grep -r "generate-raw" frontend/src/ | wc -l | grep -q "^0$" && echo "PASS: no generate-raw in frontend source"</automated>
  </verify>
  <done>Zero occurrences of "generate-raw" in any file under frontend/src/. The batch upload flow will call the correct endpoint.</done>
</task>

</tasks>

<verification>
1. `grep -rn "generate-raw" frontend/src/` returns zero results
2. `grep -n "generate" frontend/src/components/batch-upload-queue.tsx` shows the corrected `/generate` URL (not `/generate-raw`)
3. The backend route `POST /projects/{project_id}/generate` exists at library_routes.py:761 and accepts the same FormData (video + variant_count) that the batch queue sends
</verification>

<success_criteria>
- BatchUploadQueue calls `/api/v1/library/projects/{id}/generate` — verified by grep
- No references to `/generate-raw` remain in any frontend source file
- The batch flow's FormData payload (video file + variant_count) matches the backend endpoint's expected parameters
</success_criteria>

<output>
After completion, create `.planning/phases/75-batch-endpoint-fix/75-01-SUMMARY.md`
</output>
