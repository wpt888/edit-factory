---
phase: 79-v12-tech-debt-cleanup
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/phases/75-batch-endpoint-fix/75-VERIFICATION.md
  - app/main.py
  - app/api/routes.py
  - app/api/library_routes.py
  - app/api/pipeline_routes.py
  - app/services/video_processor.py
  - app/services/voice_detector.py
  - app/services/silence_remover.py
  - app/services/srt_validator.py
  - app/services/keyword_matcher.py
  - app/services/gemini_analyzer.py
  - app/services/edge_tts_service.py
autonomous: true
requirements:
  - UX-07

must_haves:
  truths:
    - "Phase 75 has a formal VERIFICATION.md confirming the batch endpoint fix"
    - "No Romanian comments remain in the three primary backend files (library_routes.py, routes.py, video_processor.py)"
    - "No Romanian comments remain in any other backend Python file"
    - "The orphaned GET /pipeline/presets endpoint is removed"
  artifacts:
    - path: ".planning/phases/75-batch-endpoint-fix/75-VERIFICATION.md"
      provides: "Formal phase verification for batch endpoint fix"
      contains: "status: passed"
    - path: "app/api/pipeline_routes.py"
      provides: "Pipeline routes without orphaned presets endpoint"
  key_links: []
---

<objective>
Close remaining low-priority tech debt from the v12 milestone audit: add a formal VERIFICATION.md for Phase 75, translate all Romanian comments in backend Python files to English, and remove the orphaned GET /pipeline/presets endpoint that the frontend no longer calls.

Purpose: Clean codebase for maintainability — no dead endpoints, no foreign-language comments, complete phase documentation.
Output: VERIFICATION.md for Phase 75, clean English-only backend comments, removed orphaned endpoint.
</objective>

<execution_context>
@/home/ukfdb/.claude/get-shit-done/workflows/execute-plan.md
@/home/ukfdb/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/v12-MILESTONE-AUDIT.md
@.planning/phases/75-batch-endpoint-fix/75-01-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create Phase 75 VERIFICATION.md and remove orphaned presets endpoint</name>
  <files>.planning/phases/75-batch-endpoint-fix/75-VERIFICATION.md, app/api/pipeline_routes.py</files>
  <action>
1. Create `.planning/phases/75-batch-endpoint-fix/75-VERIFICATION.md` with status: passed. Content based on the inline verification from 75-01-SUMMARY.md:
   - Phase: 75-batch-endpoint-fix
   - Requirements verified: UX-05
   - Checks: (a) grep for "generate-raw" in frontend/src/ returns 0 results, (b) npm run build succeeds, (c) backend route POST /projects/{project_id}/generate exists at library_routes.py
   - Result: All checks passed, batch endpoint URL corrected from /generate-raw to /generate
   - Include the commit hash 5b8e272 from the summary

2. Remove the orphaned GET /pipeline/presets endpoint from `app/api/pipeline_routes.py`. The endpoint is at line ~83-87 (the `@router.get("/presets")` and `async def list_presets()` function). Remove the entire function and its import. The frontend uses local STYLE_PRESETS TypeScript constant instead. Do NOT remove `app/services/pipeline_presets.py` — it may be used by other backend code for applying presets during rendering.
  </action>
  <verify>
    <automated>test -f ".planning/phases/75-batch-endpoint-fix/75-VERIFICATION.md" && echo "VERIFICATION exists" && grep -c "presets" app/api/pipeline_routes.py | grep -q "0" && echo "presets endpoint removed" || grep -c "@router.get.*presets" app/api/pipeline_routes.py | grep -q "0" && echo "presets route removed"</automated>
  </verify>
  <done>75-VERIFICATION.md exists with passed status; GET /pipeline/presets endpoint no longer exists in pipeline_routes.py</done>
</task>

<task type="auto">
  <name>Task 2: Translate all Romanian comments to English in backend Python files</name>
  <files>app/main.py, app/api/routes.py, app/api/library_routes.py, app/services/video_processor.py, app/services/voice_detector.py, app/services/silence_remover.py, app/services/srt_validator.py, app/services/keyword_matcher.py, app/services/gemini_analyzer.py, app/services/edge_tts_service.py</files>
  <action>
Translate ALL Romanian comments to English in the following files. Each file has Romanian comments that need translation — replace the Romanian text with equivalent English while preserving the comment structure and any code references within the comment.

Files and approximate Romanian comment counts:
- `app/main.py` — 3 comments (CORS config, production note, static files)
- `app/api/routes.py` — 8 comments (ffprobe, rotation, frame rate, subtitle, voice muting, file cleanup)
- `app/api/library_routes.py` — ~80 comments (project verification, video info, segment processing, voice detection, concat, rendering, thumbnails, DB updates, ASS color format)
- `app/services/video_processor.py` — ~80 comments (env loading, Gemini import, scores, rotation, variance, thresholds, hashing, filtering, segments, voice detection, muting, FFmpeg filters, subtitle positioning, font handling, ASS format)
- `app/services/voice_detector.py` — ~15 comments (sample rate, resampling, WAV conversion, audio extraction, voice detection, silence duration, audio filter, conditions)
- `app/services/silence_remover.py` — ~20 comments (min silence, padding, threshold, sorting, gaps, segments, concat, VAD)
- `app/services/srt_validator.py` — 3 comments (regex timestamp, regex arrow, limits check)
- `app/services/keyword_matcher.py` — ~15 comments (matched text, confidence, format, normalize, diacritics, punctuation, prefix/suffix match, fuzzy, sorting, duplicates, centering, source marker)
- `app/services/gemini_analyzer.py` — ~15 comments (score, resize, content prep, prompt, frames, timestamps, images, JSON parse, batches, filter, sort, select, overlap, chronological)
- `app/services/edge_tts_service.py` — ~10 comments (popular voices, language labels, SRT building, saving, voice assignment, fallback, listing, test)

Translation rules:
- Keep comments concise — translate meaning, not word-for-word
- Preserve inline code references (variable names, values, etc.)
- Keep the `#` comment marker and indentation identical
- Do NOT change any code, only comment text
- For language labels in edge_tts_service.py (e.g., "# Romana", "# Engleza US"), translate to English equivalents ("# Romanian", "# English US", etc.)
  </action>
  <verify>
    <automated>python3 -c "
import re, sys
files = ['app/main.py', 'app/api/routes.py', 'app/api/library_routes.py', 'app/services/video_processor.py', 'app/services/voice_detector.py', 'app/services/silence_remover.py', 'app/services/srt_validator.py', 'app/services/keyword_matcher.py', 'app/services/gemini_analyzer.py', 'app/services/edge_tts_service.py']
ro_words = re.compile(r'#.*\b(pentru|este|sau|din|care|sunt|poate|acest|toate|doar|daca|fara|sterge|adauga|verificare|procesare|rezultat|imagine|fisier|generat|incarcare|descarca|folosim|verificam|calculam|sortam|eliminam|construim|luam|adaugam|obtinem|lansam|salvam|cream|extragem|detectam|aplicam|concatenam|actualizam|pregatim|colectam|asiguram|selectam|parsam|limitam|trimitem|gasim|combinam|convertim|inseram)\b', re.IGNORECASE)
ro_chars = re.compile(r'#.*[ăîâșțĂÎÂȘȚ]')
total = 0
for f in files:
    with open(f, 'r', encoding='utf-8') as fh:
        for i, line in enumerate(fh, 1):
            if line.strip().startswith('#') or '  #' in line or '\t#' in line:
                if ro_words.search(line) or ro_chars.search(line):
                    print(f'{f}:{i}: {line.rstrip()}')
                    total += 1
if total == 0:
    print('PASS: No Romanian comments found')
else:
    print(f'FAIL: {total} Romanian comments remaining')
    sys.exit(1)
"</automated>
  </verify>
  <done>All Romanian comments in the 10 backend Python files are translated to English; the verification script finds zero Romanian comments</done>
</task>

</tasks>

<verification>
1. `test -f .planning/phases/75-batch-endpoint-fix/75-VERIFICATION.md` — VERIFICATION.md exists
2. `grep -c "@router.get.*presets" app/api/pipeline_routes.py` returns 0 — orphaned endpoint removed
3. Romanian comment verification script (from Task 2) returns PASS
4. `python -c "import app.main"` — backend still imports without errors after changes
</verification>

<success_criteria>
- Phase 75 has a formal VERIFICATION.md with status: passed
- Zero Romanian comments in library_routes.py, routes.py, and video_processor.py (primary success criteria)
- Zero Romanian comments in all other backend Python files (extended cleanup)
- GET /pipeline/presets endpoint removed from pipeline_routes.py
- Backend still starts and imports correctly after all changes
</success_criteria>

<output>
After completion, create `.planning/phases/79-v12-tech-debt-cleanup/79-01-SUMMARY.md`
</output>
