---
phase: 66-local-file-storage-offline-mode
plan: 03
type: execute
wave: 1
depends_on: ["66-02"]
files_modified: ["app/api/library_routes.py"]
autonomous: true
gap_closure: true
requirements: [DATA-03, DATA-04]

must_haves:
  truths:
    - "generate_raw_clips endpoint works with SQLite backend (no 503 error)"
    - "Background task _generate_raw_clips_task creates clips via repository, not raw supabase.table()"
    - "Project status updates during generation use repo.update_project(), not supabase.table()"
    - "With DATA_BACKEND=sqlite, uploading a video and generating clips completes without network errors"
  artifacts:
    - path: "app/api/library_routes.py"
      provides: "generate_raw_clips and _generate_raw_clips_task using repository methods"
      contains: "repo.create_clip"
  key_links:
    - from: "app/api/library_routes.py"
      to: "app/repositories/base.py"
      via: "repo.create_clip(), repo.update_project()"
      pattern: "repo\\.(create_clip|update_project)"
---

<objective>
Migrate the generate_raw_clips endpoint and its _generate_raw_clips_task background task from raw supabase.table() calls to repository methods, closing the gap that blocks offline clip generation with SQLite backend.

Purpose: Without this migration, the core video processing workflow (upload + generate clips) returns a 503 error when using SQLite backend because generate_raw_clips calls repo.get_client() which returns None for SQLite. This blocks ROADMAP Success Criterion 3.
Output: generate_raw_clips and _generate_raw_clips_task fully using repository pattern
</objective>

<execution_context>
@/home/ukfdb/.claude/get-shit-done/workflows/execute-plan.md
@/home/ukfdb/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/66-local-file-storage-offline-mode/66-02-SUMMARY.md

<interfaces>
<!-- Repository methods available for migration (from app/repositories/base.py) -->

repo.update_project(project_id: str, data: Dict[str, Any]) -> Dict[str, Any]
repo.create_clip(data: Dict[str, Any]) -> Dict[str, Any]
repo.get_project(project_id: str) -> Optional[Dict[str, Any]]

<!-- Migration pattern established in 66-02 (from SUMMARY): -->
<!-- Replace get_client()+raw query with repo.method(), remove 503 guard -->
<!-- Use get_repository() at function start, call methods directly -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Migrate generate_raw_clips endpoint and _generate_raw_clips_task to repository methods</name>
  <files>app/api/library_routes.py</files>
  <action>
Apply the same migration pattern established in 66-02 (replace get_client() + raw supabase queries with repository method calls).

**In generate_raw_clips endpoint (line 734-737):**
- Remove the `supabase = repo.get_client()` and `if not supabase: raise HTTPException(503)` guard
- The endpoint already has `repo = get_repository()` — just remove the get_client() block
- The rest of the endpoint (file upload, video info, background task dispatch) does NOT use supabase, so no other changes needed in the endpoint itself

**In _generate_raw_clips_task background function (lines 815-976):**
1. Replace lines 837-843 (get_client() guard): Change to `repo = get_repository()` and guard on `if not repo:` instead of checking supabase client
2. Replace lines 859-866 (project status to "generating"): Use `repo.update_project(project_id, {"source_video_path": video_path, "source_video_duration": video_info.get("duration", 0), "source_video_width": video_info.get("width", 1080), "source_video_height": video_info.get("height", 1920), "status": "generating"})` — drop the `.eq("profile_id", profile_id)` filter since update_project updates by project_id. The updated_at timestamp is handled by the repository layer.
3. Replace lines 916-927 (clip insert): Use `repo.create_clip({"project_id": project_id, "profile_id": profile_id, "variant_index": variant["variant_index"], "variant_name": variant["variant_name"], "raw_video_path": str(video_file), "thumbnail_path": str(thumbnail_path) if thumbnail_path else None, "duration": duration, "is_selected": False, "is_deleted": False, "final_status": "pending"})`
4. Replace lines 933-937 (project status to "ready_for_triage"): Use `repo.update_project(project_id, {"status": "ready_for_triage", "variants_count": len(variants)})`
5. Replace lines 944-947 (project status to "failed" on process error): Use `repo.update_project(project_id, {"status": "failed"})`
6. Replace lines 955-958 (project status to "failed" in except block): Use `repo.update_project(project_id, {"status": "failed"})`

**Important:** Remove the `if not status_result.data:` warning checks after update calls — repository methods don't return PostgREST-style result objects. Instead, trust that exceptions are raised on failure (the try/except blocks already handle errors).

Remove the `supabase` variable entirely from the function — it should not appear anywhere in _generate_raw_clips_task after migration.
  </action>
  <verify>
    <automated>cd "/mnt/c/OBSID SRL/n8n/edit_factory" && python -c "
import ast, sys
with open('app/api/library_routes.py') as f:
    source = f.read()
# Verify no supabase.table calls remain in generate_raw_clips or _generate_raw_clips_task
# Find the function boundaries
lines = source.split('\n')
in_target = False
target_lines = []
for i, line in enumerate(lines):
    if 'async def generate_raw_clips(' in line or 'async def _generate_raw_clips_task(' in line:
        in_target = True
    elif in_target and (line.startswith('async def ') or line.startswith('def ') or line.startswith('class ') or line.startswith('@router')):
        if 'async def _generate_raw_clips_task(' not in line:
            in_target = False
    if in_target:
        target_lines.append((i+1, line))

supabase_calls = [(n, l) for n, l in target_lines if 'supabase.table(' in l or 'get_client()' in l]
if supabase_calls:
    print('FAIL: Found raw supabase calls:')
    for n, l in supabase_calls:
        print(f'  Line {n}: {l.strip()}')
    sys.exit(1)

repo_calls = [(n, l) for n, l in target_lines if 'repo.create_clip(' in l or 'repo.update_project(' in l]
if len(repo_calls) < 3:
    print(f'FAIL: Expected at least 3 repo method calls, found {len(repo_calls)}')
    sys.exit(1)

print(f'PASS: No raw supabase calls, found {len(repo_calls)} repo method calls')
"
    </automated>
  </verify>
  <done>generate_raw_clips endpoint and _generate_raw_clips_task have zero supabase.table() or get_client() calls. All database operations use repo.create_clip() and repo.update_project(). The function works with both Supabase and SQLite backends.</done>
</task>

<task type="auto">
  <name>Task 2: Verify offline clip generation end-to-end</name>
  <files>app/api/library_routes.py</files>
  <action>
Run a quick smoke test to verify the migration did not break the code structure:

1. Start the backend with `DATA_BACKEND=sqlite` and confirm it boots without import errors
2. Verify the generate_raw_clips endpoint is registered and accessible (check /docs or curl the endpoint existence)
3. Grep the entire _generate_raw_clips_task function to confirm zero remaining `supabase` variable references (not just supabase.table, but any `supabase` usage)

If any `supabase` references remain (e.g., in comments referencing old behavior), clean them up. Comments should reference "repository" or "repo" instead.
  </action>
  <verify>
    <automated>cd "/mnt/c/OBSID SRL/n8n/edit_factory" && DATA_BACKEND=sqlite python -c "
from app.api.library_routes import router
routes = [r.path for r in router.routes if hasattr(r, 'path')]
assert '/projects/{project_id}/generate' in routes, 'generate endpoint not found'
print('PASS: generate_raw_clips endpoint registered')

# Verify no supabase references in the task function
import inspect
from app.api.library_routes import _generate_raw_clips_task
source = inspect.getsource(_generate_raw_clips_task)
if 'supabase' in source.lower().replace('# supabase', '').replace('supabase-', ''):
    # Allow 'supabase' only in comments
    import re
    code_lines = [l for l in source.split('\n') if l.strip() and not l.strip().startswith('#')]
    supabase_in_code = [l.strip() for l in code_lines if 'supabase' in l.lower()]
    if supabase_in_code:
        print(f'FAIL: supabase references in code: {supabase_in_code}')
        import sys; sys.exit(1)
print('PASS: No supabase references in _generate_raw_clips_task code')
"
    </automated>
  </verify>
  <done>Backend boots with DATA_BACKEND=sqlite, generate_raw_clips endpoint is accessible, and _generate_raw_clips_task contains zero supabase references in executable code.</done>
</task>

</tasks>

<verification>
- Zero `supabase.table()` calls in generate_raw_clips or _generate_raw_clips_task
- Zero `get_client()` calls in generate_raw_clips or _generate_raw_clips_task
- At least 3 `repo.update_project()` calls (generating status, ready_for_triage status, two failed status paths)
- At least 1 `repo.create_clip()` call (clip insertion loop)
- Backend boots without errors with DATA_BACKEND=sqlite
- generate_raw_clips endpoint is accessible at /api/v1/projects/{id}/generate
</verification>

<success_criteria>
The generate_raw_clips endpoint and _generate_raw_clips_task background task use repository methods exclusively. With DATA_BACKEND=sqlite, the clip generation workflow no longer returns 503. ROADMAP Success Criterion 3 is unblocked.
</success_criteria>

<output>
After completion, create `.planning/phases/66-local-file-storage-offline-mode/66-03-SUMMARY.md`
</output>
