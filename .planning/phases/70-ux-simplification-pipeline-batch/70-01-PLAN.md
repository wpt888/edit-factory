---
phase: 70-ux-simplification-pipeline-batch
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - frontend/src/types/pipeline-presets.ts
  - app/api/pipeline_routes.py
  - app/services/pipeline_presets.py
autonomous: true
requirements: [UX-01, UX-02]
must_haves:
  truths:
    - "Style presets exist with human-readable names and auto-configured backend parameters"
    - "A backend endpoint returns available style presets"
    - "TypeScript types for presets and simple mode state are defined"
  artifacts:
    - path: "frontend/src/types/pipeline-presets.ts"
      provides: "StylePreset type, SimpleModeState type, STYLE_PRESETS constant"
      exports: ["StylePreset", "SimpleModeState", "STYLE_PRESETS", "PipelineMode"]
    - path: "app/services/pipeline_presets.py"
      provides: "Style preset definitions with backend parameter mappings"
      exports: ["STYLE_PRESETS", "get_preset_by_id", "get_all_presets"]
    - path: "app/api/pipeline_routes.py"
      provides: "GET /pipeline/presets endpoint"
  key_links:
    - from: "frontend/src/types/pipeline-presets.ts"
      to: "app/services/pipeline_presets.py"
      via: "Preset IDs match between frontend constants and backend definitions"
      pattern: "energetic_short|product_showcase|calm_narration"
---

<objective>
Define style presets and types for Simple Mode pipeline

Purpose: Create the data contracts and backend service that power the simplified 3-step pipeline mode. Style presets map user-friendly names ("Energetic Short", "Product Showcase") to backend parameters (variant count, voice settings, subtitle style, segment duration).

Output: TypeScript types for frontend, Python preset service, and a GET endpoint for preset listing.
</objective>

<execution_context>
@/home/ukfdb/.claude/get-shit-done/workflows/execute-plan.md
@/home/ukfdb/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@frontend/src/app/pipeline/page.tsx (lines 244-300 for current state variables)
@app/api/pipeline_routes.py (existing pipeline router)
@app/services/script_generator.py (existing AI script generation for reference)
</context>

<tasks>

<task type="auto">
  <name>Task 1: Define style presets and types</name>
  <files>frontend/src/types/pipeline-presets.ts, app/services/pipeline_presets.py</files>
  <action>
Create `frontend/src/types/pipeline-presets.ts` with:

1. `PipelineMode` type: `"simple" | "advanced"`
2. `StylePreset` interface with fields:
   - `id: string` (e.g., "energetic_short")
   - `name: string` (e.g., "Energetic Short")
   - `description: string` (1-line description for UI card)
   - `icon: string` (lucide icon name: "zap", "package", "mic", "film", "sparkles")
   - `params` object containing:
     - `variant_count: number`
     - `voice_speed: number`
     - `voice_stability: number`
     - `voice_similarity: number`
     - `words_per_subtitle: number`
     - `min_segment_duration: number`
     - `ultra_rapid_intro: boolean`
     - `elevenlabs_model: string`
     - `preset_name: string` (encoding preset: "TikTok", "YouTube_Shorts", etc.)

3. `SimpleModeState` interface with:
   - `mode: PipelineMode`
   - `selectedPreset: string | null` (preset ID)
   - `uploadedFile: File | null`

4. `STYLE_PRESETS` constant array of 5 presets:
   - "Energetic Short" — fast pace, high energy (speed 1.2, stability 0.4, rapid intro on, 2 words/sub, min segment 2s)
   - "Product Showcase" — steady, professional (speed 1.0, stability 0.6, rapid intro off, 3 words/sub, min segment 4s)
   - "Calm Narration" — slow, warm voice (speed 0.9, stability 0.7, rapid intro off, 4 words/sub, min segment 5s)
   - "Quick Demo" — punchy, tutorial-style (speed 1.1, stability 0.5, rapid intro on, 2 words/sub, min segment 3s)
   - "Cinematic" — dramatic, slow build (speed 0.85, stability 0.8, rapid intro off, 3 words/sub, min segment 6s)

Create `app/services/pipeline_presets.py` with matching Python preset definitions:
- `STYLE_PRESETS` list of dicts mirroring the TypeScript constants
- `get_preset_by_id(preset_id: str) -> dict | None`
- `get_all_presets() -> list[dict]`

Preset IDs MUST match between frontend and backend.
  </action>
  <verify>
    <automated>cd "/mnt/c/OBSID SRL/n8n/edit_factory" && python -c "from app.services.pipeline_presets import get_all_presets, get_preset_by_id; presets = get_all_presets(); assert len(presets) == 5; assert get_preset_by_id('energetic_short') is not None; assert get_preset_by_id('nonexistent') is None; print('OK:', [p['id'] for p in presets])"</automated>
  </verify>
  <done>5 style presets defined in both TypeScript and Python with matching IDs. Frontend has StylePreset type, PipelineMode type, SimpleModeState interface, and STYLE_PRESETS constant. Backend has get_all_presets() and get_preset_by_id().</done>
</task>

<task type="auto">
  <name>Task 2: Add GET /pipeline/presets endpoint</name>
  <files>app/api/pipeline_routes.py</files>
  <action>
Add a new GET endpoint to the existing pipeline router in `app/api/pipeline_routes.py`:

```python
@router.get("/pipeline/presets")
async def list_presets():
    """Return available style presets for Simple Mode."""
    from app.services.pipeline_presets import get_all_presets
    return {"presets": get_all_presets()}
```

This is a public endpoint (no auth dependency needed) since presets are static configuration.

Place it near the top of the router file, before the generate/preview endpoints.
  </action>
  <verify>
    <automated>cd "/mnt/c/OBSID SRL/n8n/edit_factory" && python -c "
from fastapi.testclient import TestClient
from app.main import app
client = TestClient(app)
resp = client.get('/api/v1/pipeline/presets')
assert resp.status_code == 200
data = resp.json()
assert 'presets' in data
assert len(data['presets']) == 5
print('OK: endpoint returns', len(data['presets']), 'presets')
"</automated>
  </verify>
  <done>GET /api/v1/pipeline/presets returns 200 with 5 presets. Each preset has id, name, description, icon, and params fields.</done>
</task>

</tasks>

<verification>
- `python -c "from app.services.pipeline_presets import get_all_presets; print(len(get_all_presets()))"` outputs 5
- `curl http://localhost:8000/api/v1/pipeline/presets` returns JSON with 5 presets
- TypeScript file has no syntax errors: `cd frontend && npx tsc --noEmit src/types/pipeline-presets.ts` (or broader check)
</verification>

<success_criteria>
- 5 style presets defined in both frontend types and backend service
- GET /pipeline/presets endpoint returns all presets
- Preset IDs are consistent between frontend and backend
- Types are exported for use by Plan 02 (Simple Mode UI)
</success_criteria>

<output>
After completion, create `.planning/phases/70-ux-simplification-pipeline-batch/70-01-SUMMARY.md`
</output>
