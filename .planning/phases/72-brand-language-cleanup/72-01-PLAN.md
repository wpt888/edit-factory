---
phase: 72-brand-language-cleanup
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - frontend/src/app/layout.tsx
  - frontend/src/app/login/page.tsx
  - frontend/src/app/login/reset-password/page.tsx
  - frontend/src/app/signup/page.tsx
  - frontend/src/app/settings/page.tsx
  - frontend/src/app/setup/page.tsx
  - frontend/src/components/navbar.tsx
  - frontend/src/components/PostDetailModal.tsx
  - frontend/src/components/video-processing/variant-triage.tsx
  - frontend/src/components/video-processing/tts-panel.tsx
autonomous: true
requirements: [UX-06, UX-07]
must_haves:
  truths:
    - "The product name 'Edit Factory' appears consistently in the window title, sidebar header, login page, signup page, reset-password page, setup wizard, and settings page"
    - "Zero matches for 'EditAI' (case-insensitive) across the entire frontend/src directory excluding localStorage keys"
    - "Zero matches for Romanian words 'adauga', 'videoclip', 'Se adauga' in any user-facing string"
  artifacts:
    - path: "frontend/src/app/layout.tsx"
      provides: "Window title with 'Edit Factory'"
      contains: "Edit Factory"
    - path: "frontend/src/components/navbar.tsx"
      provides: "Sidebar header brand name"
      contains: "Edit Factory"
    - path: "frontend/src/app/login/page.tsx"
      provides: "Login page brand name"
      contains: "Edit Factory"
    - path: "frontend/src/app/signup/page.tsx"
      provides: "Signup page brand name"
      contains: "Edit Factory"
    - path: "frontend/src/app/setup/page.tsx"
      provides: "Setup wizard brand name"
      contains: "Edit Factory"
    - path: "frontend/src/app/settings/page.tsx"
      provides: "Settings/about brand name"
      contains: "Edit Factory"
  key_links:
    - from: "frontend/src/app/layout.tsx"
      to: "browser tab"
      via: "metadata.title"
      pattern: "title.*Edit Factory"
---

<objective>
Unify the product brand name to "Edit Factory" across all frontend UI surfaces and replace all hardcoded Romanian text with English equivalents.

Purpose: Users currently see a confusing mix of "EditAI" and "Edit Factory" in different parts of the app, plus leftover Romanian strings from early development. This cleanup ensures a professional, consistent English-language product.

Output: All user-facing strings use "Edit Factory" as the product name and all text is in English.
</objective>

<execution_context>
@/home/ukfdb/.claude/get-shit-done/workflows/execute-plan.md
@/home/ukfdb/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Unify brand name from "EditAI" to "Edit Factory"</name>
  <files>
    frontend/src/app/layout.tsx,
    frontend/src/app/login/page.tsx,
    frontend/src/app/login/reset-password/page.tsx,
    frontend/src/app/signup/page.tsx,
    frontend/src/components/navbar.tsx
  </files>
  <action>
Replace all user-visible "EditAI" text with "Edit Factory" in these specific locations:

1. `frontend/src/app/layout.tsx` line 52: Change `title: "EditAI - Smart Video Editing"` to `title: "Edit Factory - Smart Video Editing"`
2. `frontend/src/app/login/page.tsx` line 109: Change `EditAI` span text to `Edit Factory`
3. `frontend/src/app/login/reset-password/page.tsx` line 78: Change `EditAI` span text to `Edit Factory`
4. `frontend/src/app/signup/page.tsx` line 115: Change `EditAI` span text to `Edit Factory`
5. `frontend/src/components/navbar.tsx` line 100: Change `EditAI` text to `Edit Factory`
6. `frontend/src/components/navbar.tsx` line 198: Change `SheetTitle` text from `EditAI` to `Edit Factory`

DO NOT rename localStorage keys (`editai_subtitle_`, `editai_current_profile_id`, `editai_profiles`, `editai_budget`). These are internal storage keys, not user-facing — renaming them would break existing user data. Only change text that appears visually in the UI.

DO NOT modify the electron/ directory — that already uses "Edit Factory" consistently.
DO NOT modify `frontend/src/app/settings/page.tsx` or `frontend/src/app/setup/page.tsx` — these already use "Edit Factory" correctly.
DO NOT modify `frontend/src/components/PostDetailModal.tsx` — already uses "Edit Factory".
  </action>
  <verify>
    <automated>cd "/mnt/c/OBSID SRL/n8n/edit_factory" && grep -rni "EditAI" frontend/src/ --include="*.tsx" --include="*.ts" | grep -v "editai_" | grep -v "node_modules" ; echo "EXIT:$?"</automated>
  </verify>
  <done>Running case-insensitive grep for "EditAI" across frontend/src (excluding localStorage key patterns like editai_*) returns zero matches. The window title, navbar, login, signup, and reset-password pages all display "Edit Factory".</done>
</task>

<task type="auto">
  <name>Task 2: Replace hardcoded Romanian text with English</name>
  <files>
    frontend/src/components/video-processing/variant-triage.tsx,
    frontend/src/components/video-processing/tts-panel.tsx
  </files>
  <action>
Replace Romanian strings with English equivalents:

1. `frontend/src/components/video-processing/variant-triage.tsx` line 206: Change `"Se adauga voice-over..."` to `"Adding voice-over..."` (the ttsStatus fallback text)
2. `frontend/src/components/video-processing/tts-panel.tsx` line 81: Change `"Voice-over adaugat direct la videoclip"` to `"Voice-over added directly to video"`

These are the only two files with Romanian text in user-facing strings (confirmed by searching for "proiect", "sterge", "adauga", "incarcare", "eroare", "incarca", "selecteaza", "descarca", "videoclip").
  </action>
  <verify>
    <automated>cd "/mnt/c/OBSID SRL/n8n/edit_factory" && grep -rni "adauga\|videoclip\|proiect\|sterge\|incarcare\|eroare\|incarca\|selecteaza\|descarca" frontend/src/ --include="*.tsx" --include="*.ts" | grep -v "node_modules" ; echo "EXIT:$?"</automated>
  </verify>
  <done>Running grep for common Romanian words across frontend/src returns zero matches. All user-facing strings are in English.</done>
</task>

</tasks>

<verification>
1. `grep -rni "EditAI" frontend/src/ --include="*.tsx" --include="*.ts" | grep -v "editai_"` returns no matches
2. `grep -rni "adauga\|videoclip\|proiect\|sterge\|incarcare\|eroare" frontend/src/ --include="*.tsx" --include="*.ts"` returns no matches
3. `cd frontend && npx next build` completes without errors (no broken imports or syntax)
</verification>

<success_criteria>
- UX-06: The brand name "Edit Factory" appears in window title, navbar (desktop + mobile sheet), login page, signup page, and reset-password page. No "EditAI" appears in user-visible text.
- UX-07: Zero Romanian words in any frontend source file. All status messages, labels, and descriptions are in English.
</success_criteria>

<output>
After completion, create `.planning/phases/72-brand-language-cleanup/72-01-SUMMARY.md`
</output>
