---
phase: 64-data-abstraction-layer
plan: 03
type: execute
wave: 3
depends_on: [64-01, 64-02]
files_modified:
  - app/services/cost_tracker.py
  - app/services/job_storage.py
  - app/services/elevenlabs_account_manager.py
  - app/services/assembly_service.py
  - app/services/postiz_service.py
  - app/services/telegram_service.py
  - app/services/tts_library_service.py
  - app/services/schedule_service.py
  - app/services/file_storage.py
  - app/api/library_routes.py
  - app/api/pipeline_routes.py
  - app/api/segments_routes.py
  - app/api/postiz_routes.py
  - app/api/assembly_routes.py
  - app/api/schedule_routes.py
  - app/api/profile_routes.py
  - app/api/tts_library_routes.py
  - app/api/tts_routes.py
  - app/api/feed_routes.py
  - app/api/product_routes.py
  - app/api/catalog_routes.py
  - app/api/image_generate_routes.py
  - app/api/product_generate_routes.py
  - app/api/routes.py
  - app/api/auth.py
  - app/main.py
  - app/cleanup.py
autonomous: true
requirements: [DATA-02]

must_haves:
  truths:
    - "No service file imports get_supabase() directly — all database access goes through repository methods"
    - "No route file calls get_supabase() directly — all database access goes through repository methods"
    - "app/main.py startup cleanup uses repository instead of direct Supabase"
    - "The application starts and serves requests identically to before the migration"
  artifacts:
    - path: "app/api/library_routes.py"
      provides: "Library routes using repository pattern"
      contains: "get_repository"
    - path: "app/services/job_storage.py"
      provides: "Job storage using repository pattern"
      contains: "get_repository"
    - path: "app/services/cost_tracker.py"
      provides: "Cost tracker using repository pattern"
      contains: "get_repository"
  key_links:
    - from: "app/api/library_routes.py"
      to: "app/repositories/factory.py"
      via: "imports get_repository"
      pattern: "from app.repositories.factory import get_repository"
    - from: "app/services/job_storage.py"
      to: "app/repositories/factory.py"
      via: "imports get_repository"
      pattern: "from app.repositories"
    - from: "app/main.py"
      to: "app/repositories/factory.py"
      via: "uses repository for startup cleanup"
      pattern: "get_repository"
---

<objective>
Migrate all route and service files from direct Supabase calls to repository methods.

Purpose: After this plan, no business logic file imports or calls `get_supabase()` directly. All database access goes through `get_repository()` which returns a `DataRepository` implementation. This is the critical wiring step that makes the backend swappable.
Output: All 27 files updated to use repository pattern instead of direct Supabase calls.
</objective>

<execution_context>
@/home/ukfdb/.claude/get-shit-done/workflows/execute-plan.md
@/home/ukfdb/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/64-data-abstraction-layer/64-01-SUMMARY.md
@.planning/phases/64-data-abstraction-layer/64-02-SUMMARY.md

@app/repositories/base.py
@app/repositories/supabase_repo.py
@app/repositories/factory.py
@app/db.py
</context>

<interfaces>
<!-- From Plans 01 and 02 -->
From app/repositories/factory.py:
```python
def get_repository() -> DataRepository: ...
def close_repository() -> None: ...
```

From app/repositories/base.py:
```python
class DataRepository(ABC):
    # All repository methods — see base.py for complete interface
    def list_projects(self, profile_id, filters=None) -> QueryResult: ...
    def get_project(self, project_id) -> Optional[Dict]: ...
    def create_project(self, data) -> Dict: ...
    def update_project(self, project_id, data) -> Dict: ...
    # ... etc for all tables
    def table_query(self, table_name, operation, **kwargs) -> QueryResult: ...
```
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Migrate services from direct Supabase to repository</name>
  <files>app/services/cost_tracker.py, app/services/job_storage.py, app/services/elevenlabs_account_manager.py, app/services/assembly_service.py, app/services/postiz_service.py, app/services/telegram_service.py, app/services/tts_library_service.py, app/services/schedule_service.py, app/services/file_storage.py</files>
  <action>
For each service file that imports `get_supabase`:

1. Replace `from app.db import get_supabase` with `from app.repositories.factory import get_repository`
2. Replace every `supabase = get_supabase()` / `self._supabase = get_supabase()` with `repo = get_repository()`
3. Replace every `supabase.table("X").select(...).eq(...).execute()` chain with the equivalent repository method call

**File-by-file migration guide:**

**cost_tracker.py** (~3 call sites):
- `self._supabase = get_supabase()` -> `self._repo = get_repository()`
- `self._supabase.table("api_costs").insert(...)` -> `self._repo.log_cost(data)`
- `_get_summary_from_supabase()` -> refactor to use `self._repo.get_cost_summary(profile_id, filters)`

**job_storage.py** (~10 call sites):
- Replace all `self._supabase.table("jobs")...` calls with corresponding repository methods:
  - `.insert(...)` -> `self._repo.create_job(data)`
  - `.select("*").eq("id", job_id)` -> `self._repo.get_job(job_id)`
  - `.update(data).eq("id", job_id)` -> `self._repo.update_job(job_id, data)`
  - `.delete().eq("id", job_id)` -> `self._repo.delete_job(job_id)`
  - `.select("*").order(...)` -> `self._repo.list_jobs(limit, profile_id)`
  - `.select("*").eq("data->>project_id", pid)` -> `self._repo.list_jobs_by_project(project_id)`
  - `.delete().in_("status", [...]).lt("created_at", cutoff)` -> `self._repo.cleanup_old_jobs(cutoff)`

**elevenlabs_account_manager.py** (~12 call sites):
- `self._get_supabase()` -> `get_repository()`
- Map all `.table("elevenlabs_accounts")...` to repository account methods

**assembly_service.py** (~2 call sites):
- `.table("editai_segments")...` -> `repo.list_segments(...)` or `repo.get_segment(...)`
- `.table("editai_source_videos")...` -> `repo.list_source_videos(...)`

**postiz_service.py** (~2 call sites):
- `.table("profiles")...` -> `repo.get_profile(...)` or `repo.list_profiles(...)`

**telegram_service.py** (~1 call site):
- `.table("profiles")...` -> `repo.list_profiles(...)`

**tts_library_service.py** (~1 call site):
- `.table("editai_tts_assets")...` -> `repo.create_tts_asset(...)` or `repo.update_tts_asset(...)`

**schedule_service.py** (~3 call sites):
- `.table("editai_projects")...` -> `repo.list_projects(...)`
- `.table("editai_clips")...` -> `repo.list_clips(...)`
- `.table("editai_schedule_items")...` -> `repo.list_schedule_items(...)`

**file_storage.py** (~1 call site):
- `.table(...)` usage -> use `repo.table_query(...)` if it's a storage-specific query, or the appropriate typed method

**CRITICAL rules:**
- Do NOT change function signatures or return values — callers must see identical behavior
- Keep the in-memory fallback logic in job_storage.py and cost_tracker.py (the try/except that falls back to dict storage when Supabase is unavailable)
- If a query pattern doesn't map cleanly to a repository method, use `repo.table_query(table_name, operation, **kwargs)` as an escape hatch
- Preserve all error handling and logging
- Do NOT remove `app/db.py` — it's still needed by `app/repositories/supabase_repo.py`
  </action>
  <verify>
    <automated>cd "/mnt/c/OBSID SRL/n8n/edit_factory" && python -c "
import subprocess
result = subprocess.run(['grep', '-rn', 'from app.db import get_supabase', 'app/services/'], capture_output=True, text=True)
if result.stdout.strip():
    print(f'FAIL: Services still import get_supabase directly:')
    print(result.stdout)
    exit(1)
else:
    print('OK: No service file imports get_supabase directly')
"</automated>
  </verify>
  <done>All 9 service files use repository methods instead of direct Supabase calls. No service file imports get_supabase(). Existing behavior (including fallbacks) is preserved.</done>
</task>

<task type="auto">
  <name>Task 2: Migrate routes and core files from direct Supabase to repository</name>
  <files>app/api/library_routes.py, app/api/pipeline_routes.py, app/api/segments_routes.py, app/api/postiz_routes.py, app/api/assembly_routes.py, app/api/schedule_routes.py, app/api/profile_routes.py, app/api/tts_library_routes.py, app/api/tts_routes.py, app/api/feed_routes.py, app/api/product_routes.py, app/api/catalog_routes.py, app/api/image_generate_routes.py, app/api/product_generate_routes.py, app/api/routes.py, app/api/auth.py, app/main.py, app/cleanup.py</files>
  <action>
For each route file and core file that imports `get_supabase`:

1. Replace `from app.db import get_supabase` with `from app.repositories.factory import get_repository`
2. Replace every `supabase = get_supabase()` with `repo = get_repository()`
3. Replace every `supabase.table("X")...` chain with the equivalent repository method

**The largest files (migrate carefully):**

**library_routes.py** (~30 call sites): This is the biggest file. Key patterns:
- `supabase.table("editai_projects").select("*").eq("profile_id", pid)` -> `repo.list_projects(profile_id)`
- `supabase.table("editai_projects").insert({...})` -> `repo.create_project(data)`
- `supabase.table("editai_clips").select("*, editai_clip_content(*)").eq("project_id", pid)` -> `repo.list_clips(project_id, {"include_content": True})`
- `supabase.table("editai_clips").update({...}).eq("id", cid)` -> `repo.update_clip(clip_id, data)`
- Complex queries with `.is_("deleted_at", "null")`, `.neq("status", "x")`, `.order(...)` — map filter params to the filters dict

**pipeline_routes.py** (~15 call sites): Pipeline CRUD + segment queries
**segments_routes.py** (~25 call sites): Segment CRUD, source videos, project segments
**profile_routes.py** (~13 call sites): Profile CRUD with JSONB settings
**schedule_routes.py** (~12 call sites): Schedule plans and items
**image_generate_routes.py** (~18 call sites): Generated images, prompt templates

**app/main.py** (~8 call sites): Startup cleanup logic
- Replace all `from app.db import get_supabase` with `from app.repositories.factory import get_repository`
- Replace cleanup queries (reset stale statuses) with repository methods
- Keep the `from app.db import close_supabase` call in the shutdown handler — add `from app.repositories.factory import close_repository` alongside it and call both

**app/cleanup.py** (~1 call site):
- Replace with repository method

**app/api/auth.py** (special case):
- `auth.py` uses `get_supabase()` for profile lookups. Replace with `get_repository().get_profile(...)` and `get_repository().get_default_profile(user_id)`
- Keep the `_get_supabase` alias import if needed for the auth verification logic that uses `supabase.auth` (JWT verification is NOT a data query — it uses the Supabase Auth SDK, which is different from table queries)
- ONLY migrate `.table("profiles")...` calls. Leave any `supabase.auth.get_user(token)` calls as-is — those are auth SDK calls, not data queries

**CRITICAL rules:**
- Do NOT change any route endpoint signatures, return shapes, or error responses
- For complex chained queries (`.select().eq().eq().order().limit()`) that don't map to a single repository method, use `repo.table_query()` as escape hatch rather than creating fragile workarounds
- Preserve ALL background task behavior — just change the data access layer inside them
- If a variable is named `supabase` and used in multiple queries in the same function, rename it to `repo` and update all references in that function
- Keep `app/db.py` — it's still used by `supabase_repo.py`
- app/api/auth.py should keep using get_supabase() for `supabase.auth.*` calls (JWT verification) — only migrate `.table()` calls

After all migrations, verify with grep that the ONLY files importing `get_supabase` are:
- `app/db.py` (defines it)
- `app/repositories/supabase_repo.py` (uses it internally)
- `app/api/auth.py` (for `supabase.auth.*` JWT verification only)
  </action>
  <verify>
    <automated>cd "/mnt/c/OBSID SRL/n8n/edit_factory" && python -c "
import subprocess

# Check routes don't import get_supabase (except auth.py which needs it for supabase.auth)
result = subprocess.run(['grep', '-rn', 'from app.db import get_supabase', 'app/api/', 'app/main.py', 'app/cleanup.py'], capture_output=True, text=True)
lines = [l for l in result.stdout.strip().split('\n') if l and 'auth.py' not in l]
if lines:
    print('FAIL: Files still import get_supabase:')
    for l in lines:
        print(f'  {l}')
    exit(1)

# Check auth.py only uses get_supabase for auth SDK, not table queries
result2 = subprocess.run(['grep', '-n', '.table(', 'app/api/auth.py'], capture_output=True, text=True)
# If there are .table() calls in auth.py using supabase directly, that's a fail
auth_table_calls = [l for l in result2.stdout.strip().split('\n') if l and 'supabase' in l.lower() and 'repo' not in l.lower()]
# We allow this if auth.py migrated table calls to repo — just check no get_supabase table usage

print('OK: No route/service file (except auth.py for auth SDK) imports get_supabase directly')

# Verify the app can still import main
try:
    import app.main
    print('OK: app.main imports successfully')
except Exception as e:
    print(f'WARNING: app.main import issue (may need running server): {e}')
" 2>&1 | head -30</automated>
  </verify>
  <done>All 18 route/core files use repository methods. Only app/db.py, app/repositories/supabase_repo.py, and app/api/auth.py (for auth SDK only) still reference get_supabase(). The application starts and responds identically.</done>
</task>

</tasks>

<verification>
- `grep -rn "from app.db import get_supabase" app/api/ app/services/ app/main.py app/cleanup.py` returns only auth.py
- `grep -rn "get_repository" app/api/ app/services/` shows all files using the repository pattern
- `python -c "import app.main"` succeeds (no import errors from migration)
</verification>

<success_criteria>
1. Zero direct get_supabase() usage in any service file
2. Zero direct get_supabase() usage in any route file (except auth.py for JWT verification)
3. app/main.py uses repository for startup cleanup
4. Application starts without errors
5. All database operations use repository methods or table_query() escape hatch
</success_criteria>

<output>
After completion, create `.planning/phases/64-data-abstraction-layer/64-03-SUMMARY.md`
</output>
