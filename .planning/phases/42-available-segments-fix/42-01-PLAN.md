---
phase: 42-available-segments-fix
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - app/api/pipeline_routes.py
autonomous: true
gap_closure: true
requirements:
  - TIME-03
  - TIME-04

must_haves:
  truths:
    - "PipelinePreviewResponse includes available_segments in its JSON output"
    - "Frontend TimelineEditor receives non-empty availableSegments enabling swap and manual assignment dialogs"
    - "Segment swap button in TimelineEditor is enabled when segments are available"
    - "Unmatched phrase 'Select Segment' button opens dialog populated with available segments"
  artifacts:
    - path: "app/api/pipeline_routes.py"
      provides: "PipelinePreviewResponse with available_segments field"
      contains: "available_segments"
  key_links:
    - from: "app/services/assembly_service.py"
      to: "app/api/pipeline_routes.py"
      via: "preview_data dict passed to PipelinePreviewResponse constructor"
      pattern: "available_segments.*preview_data"
    - from: "app/api/pipeline_routes.py"
      to: "frontend/src/app/pipeline/page.tsx"
      via: "JSON response consumed by fetch in handlePreviewAll"
      pattern: "available_segments"
---

<objective>
Add the missing `available_segments` field to `PipelinePreviewResponse` Pydantic model and wire it into the response constructor so the frontend TimelineEditor can populate its segment swap and manual assignment dialogs.

Purpose: Closes the integration gap where assembly_service.preview_matches() produces available_segments but PipelinePreviewResponse silently drops it, leaving TIME-03 (segment swap) and TIME-04 (manual assignment) non-functional at runtime.

Output: Fixed pipeline_routes.py — one field added to model, one kwarg added to constructor.
</objective>

<execution_context>
@/home/ukfdb/.claude/get-shit-done/workflows/execute-plan.md
@/home/ukfdb/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/v8-MILESTONE-AUDIT.md
@app/api/pipeline_routes.py
@app/services/assembly_service.py
@frontend/src/app/pipeline/page.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add available_segments to PipelinePreviewResponse and wire in route handler</name>
  <files>app/api/pipeline_routes.py</files>
  <action>
Two changes in app/api/pipeline_routes.py:

1. **Add field to Pydantic model** (around line 199, after `unmatched_count: int`):
   Add `available_segments: List[dict] = []` to the `PipelinePreviewResponse` class.
   Import `List` and `dict` are already available (List is imported from typing at file top).

2. **Wire field in response constructor** (around line 853-860):
   In the `return PipelinePreviewResponse(...)` call, add:
   `available_segments=preview_data.get("available_segments", [])`

This is a 2-line fix. Do NOT modify assembly_service.py (it already produces available_segments correctly at line 844). Do NOT modify the frontend (page.tsx:559 already reads available_segments from the response).

Verify the field type matches what assembly_service produces: a list of dicts with keys {id, keywords, source_video_id, duration}.
  </action>
  <verify>
    <automated>cd "/mnt/c/OBSID SRL/n8n/edit_factory" && python -c "
from app.api.pipeline_routes import PipelinePreviewResponse
import json
# Verify the model accepts available_segments
resp = PipelinePreviewResponse(
    audio_duration=10.0,
    srt_content='test',
    matches=[],
    total_phrases=1,
    matched_count=0,
    unmatched_count=1,
    available_segments=[{'id': 'seg1', 'keywords': ['test'], 'source_video_id': 'vid1', 'duration': 5.0}]
)
data = json.loads(resp.model_dump_json())
assert 'available_segments' in data, 'available_segments missing from response'
assert len(data['available_segments']) == 1, 'available_segments not populated'
assert data['available_segments'][0]['id'] == 'seg1', 'segment data incorrect'
print('PASS: PipelinePreviewResponse includes available_segments')
"
    </automated>
    <manual>Start backend (python run.py), run a pipeline preview via the UI, and confirm the JSON response includes available_segments array with segment objects.</manual>
  </verify>
  <done>PipelinePreviewResponse model declares available_segments field. Route handler passes preview_data["available_segments"] to the constructor. Frontend receives non-empty availableSegments enabling swap and manual assignment in TimelineEditor.</done>
</task>

</tasks>

<verification>
1. Python import check: `from app.api.pipeline_routes import PipelinePreviewResponse` succeeds
2. Model validation: constructing PipelinePreviewResponse with available_segments=[{...}] succeeds and serializes correctly
3. Default value: constructing without available_segments defaults to empty list (backward compatible)
4. Route handler: the return statement at ~line 853 includes available_segments kwarg
</verification>

<success_criteria>
- PipelinePreviewResponse Pydantic model has `available_segments: List[dict] = []` field
- Route handler preview endpoint passes `available_segments=preview_data.get("available_segments", [])` to constructor
- No changes needed in assembly_service.py or frontend — they already handle available_segments correctly
- TIME-03 (segment swap) and TIME-04 (manual assignment) unblocked at runtime
</success_criteria>

<output>
After completion, create `.planning/phases/42-available-segments-fix/42-01-SUMMARY.md`
</output>
