== DIM: surfaces-web (5 confirmed) ==
[medium] app/(app)/automations/import-dialog.tsx:69 :: Modal scrim color/opacity is hand-rolled per file instead of a shared overlay token, and the value drifts across the app: bg-black/60 here vs /50, /55, /70 elsewhere for the exact same 'dim page behind modal' role.
    EV: <div className="absolute inset-0 bg-black/60 backdrop-blur-[2px]" onClick={onClose} />
    FIX: Introduce one shared Overlay/scrim class (e.g. a `.modal-scrim` utility or a components/ui/dialog.tsx primitive) at a single opacity, and point every ad-hoc modal at it instead of re-typing bg-black/NN.
[medium] app/(app)/schedule/post-sheet.tsx:34 :: Same scrim role uses a different opacity (55) than the /60 used in automations dialogs and the /70 used for native <dialog> backdrops, with no design-token backing any of the values.
    EV: className="absolute inset-0 bg-black/55"
    FIX: Standardize on one scrim opacity (see recommendation on import-dialog.tsx) so all sheets/modals dim the background identically.
[medium] app/(app)/media/media-card.tsx:172 :: Native <dialog> backdrops use bg-black/70, a third distinct opacity from the /60 and /55 used by custom overlay divs elsewhere for the identical purpose (repeated at line 259 in the same file, and at connect-tile.tsx:158/198 and block-editor.tsx:211 with /60 instead).
    EV: className="m-auto w-full max-w-lg rounded-lg border bg-card p-5 text-foreground backdrop:bg-black/70"
    FIX: Fold all backdrop:bg-black/NN and bg-black/NN overlay usages (9 files found: import-dialog.tsx, new-automation.tsx, template-wizard.tsx, studio-canvas.tsx, workflow-editor.tsx, connect-tile.tsx x2, media-card.tsx x2, post-sheet.tsx, block-editor.tsx) onto one constant.
[medium] app/(app)/create/composer.tsx:301 :: The main post-composer textarea fills with bg-background, while the overwhelming majority of text inputs across the app (35 occurrences across post-form.tsx, settings/page.tsx, media-card.tsx, clips/new-run.tsx, review-highlights.tsx, connect-tile.tsx, admin/users pages, auth field.tsx) use border-input bg-card for the identical 'text input' role — a visible surface mismatch on the app's most-used screen.
    EV: className="mt-4 w-full resize-none rounded-lg border border-input bg-background p-3 text-sm outline-none focus-visible:border-ring focus-visible:ring-3 focus-visible:ring-ring/40"
    FIX: Standardize input fill to bg-card (the dominant convention) or, better, extract a shared Input/Textarea component in components/ui/ so the fill token can't drift per file again.
[low] app/(app)/automations/template-wizard.tsx:20 :: Same input-fill inconsistency repeated in the template wizard modal's fields (bg-background instead of the app-wide bg-card convention for border-input fields).
    EV: "w-full rounded-lg border border-input bg-background px-3 py-2 text-sm outline-none focus-visible:border-ring focus-visible:ring-3 focus-visible:ring-ring/40"
    FIX: Align to border-input bg-card, matching settings/page.tsx and post-form.tsx conventions.
  OBS: The web app has no shared UI primitives for the two surfaces this dimension covers: there is no Dialog/Overlay component (components/ui/ contains only button.tsx) and no shared Input/Textarea component, so every file that needs a modal scrim or a text-input fill re-types Tailwind classes from memory, and the exact shade drifts (bg-black/50 vs /55 vs /60 vs /70 for scrims; bg-card vs bg-background for inputs, 35-to-3 in favor of bg-card but never fixed). The one clear surface-hierarchy break is localized: the automations workflow editor (studio-header.tsx, cost-strip.tsx, workflow-editor.tsx's run-history dock) borrows bg-sidebar/NN — a token that should be exclusive to components/app-sidebar.tsx — for its in-canvas header/footer bars, and pairs it with border-border instead of border-sidebar-border, so the same feature mixes two different surface systems. Everywhere else (dashboard, clip

== DIM: surfaces-studio (5 confirmed) ==
[high] app/pipeline/components/pipeline-history-sidebar.tsx:86 :: Root cause (widest-reaching instance) of the reported black-toolbar/gray-content edge. The wrapper div is explicitly `bg-background` (near-black, oklch 0.145) but the Card immediately inside it keeps the component default `bg-card` (oklch 0.19, visibly lighter) while `rounded-none border-0 shadow-none` strip away the only things (radius, border, shadow) that would soften or delineate the seam. This history panel rend
    EV: line 86: `className={`w-full min-w-0 bg-background ${isEditingWorkspace ? "min-[1280px]:h-full min-[1280px]:min-h-0" : ""}`}` — line 89-91: `<Card className={isEditingWorkspace ? "
    FIX: When stripping a Card's border/radius/shadow for the flush workspace layout, also override its background (`bg-background` or `bg-transparent`) to match the ancestor that already paints the surface — or, if the lighter card tone is meant to read as a distinct panel, keep a 1px `b
[high] app/pipeline/components/source-videos-card.tsx:50 :: This is the exact panel a user sees first: Step 1 is the pipeline's default landing step and 'workspace' is DEFAULT_PIPELINE_LAYOUT, so SourceVideosCard renders directly below the sticky toolbar inside an `aside` that is `bg-background`. The Card itself has its border/radius/shadow stripped for workspace mode but keeps the default `bg-card`, producing the identical near-black-to-gray seam as finding #1, reproducing t
    EV: step1-script.tsx:154-161: `<aside className={workspaceLayout ? "min-w-0 bg-background min-[1280px]:h-full ..." : "min-w-0"}> <SourceVideosCard ctx={ctx} workspace={workspaceLayout}
    FIX: Same fix as finding #1: make the stripped Card's background match its `bg-background` parent, applied consistently via a shared workspace-Card utility class instead of re-deriving the same ternary string per component (it is currently duplicated near-verbatim in 4+ files).
[high] app/pipeline/components/pipeline-stepper.tsx:133 :: Precise trace of the reported mismatch: the toolbar uses `bg-background/95` with `backdrop-blur`, i.e. a near-opaque near-black background (oklch 0.145). Content immediately below it (both step Cards and the history sidebar Card, see findings above) uses the un-overridden `bg-card` token (oklch 0.19). `--card` is defined as visibly lighter than `--background` in globals.css, and no border/shadow separates the two reg
    EV: pipeline-stepper.tsx:133: `className="sticky top-0 z-20 flex h-14 shrink-0 items-center gap-4 border-b bg-background/95 px-4 backdrop-blur supports-[backdrop-filter]:bg-background/
    FIX: Treat this as one design decision, not a per-file fix: either give workspace-mode panels the page's own background (so the toolbar's border-b is the only visible seam, by design) or intentionally theme the toolbar to `bg-card` to match the panels beneath it. Do not leave the two 
[medium] app/pipeline/components/step2-tts.tsx:598 :: Self-contradicting within the same file: the `<section>` wrapper is explicitly `bg-background` (line 592), but its direct child — a sticky sub-header playing the same role as a section title bar — is `bg-card`, one shade lighter, creating a small-scale repeat of the toolbar/content mismatch nested inside Step 2 specifically. The structurally identical sub-header in Step 3 (same h-*, sticky, border-b role) correctly u
    EV: step2-tts.tsx:592: `className={workspaceLayout ? "min-w-0 bg-background min-[1280px]:flex ..." : "min-w-0"}` then line 598: `className={workspaceLayout ? "hidden min-[1280px]:flex 
    FIX: Change step2-tts.tsx:598's `bg-card` to `bg-background` to match its own section wrapper and to be consistent with the equivalent Step 3 header.
[medium] app/pipeline/components/step4-render.tsx:1 :: Step 4 never adopts the 'flush workspace panel' treatment (bg-card retained, border/radius/shadow stripped) used throughout Steps 1-3 under the same persistent toolbar: grepping `rounded-none.*border-0.*shadow-none` across the pipeline finds 7 hits in step1-script.tsx, step2-tts.tsx, step3-preview.tsx (x3), source-videos-card.tsx, and pipeline-history-sidebar.tsx, but 0 in step4-render.tsx. Its Cards keep the default
    EV: grep `rounded-none.*border-0.*shadow-none` over app/pipeline: 7 matches in step1-script.tsx:172, step2-tts.tsx:334, step3-preview.tsx:279/372/559, source-videos-card.tsx:52, pipeli
    FIX: Either bring Step 4's Cards into the same workspace-flush treatment as Steps 1-3 for consistency, or — if Step 4 is deliberately a simpler, non-workspace review screen — say so explicitly (e.g. a code comment) so the asymmetry reads as intentional rather than an oversight.
  OBS: The reported bug is real and traceable exactly: the pipeline toolbar and its page root both use --background (oklch 0.145, near-black), while the panels immediately beneath it — SourceVideosCard on Step 1, the Assembly/Script Cards on Steps 1-3, and the History sidebar Card on every step — are ordinary shadcn `Card` components whose default `bg-card` (oklch 0.19, visibly lighter) was never overridden when a `workspaceLayout` variant strips their border/radius/shadow for the dense "flush panel" editing layout. The border/radius/shadow removal was clearly deliberate (it appears 7 times, hand-written per component), but each site forgot the accompanying background swap, so what should read as one continuous bg-background canvas instead shows a hard, un-bordered seam wherever a Card starts — most visibly right under the sticky toolbar. This is a copy-paste-pattern bug, not a one-off: the sam

== DIM: spacing-web (8 confirmed) ==
[high] app/(app)/schedule/page.tsx:378 :: Schedule is the only (app) page with no horizontal gutter and no max-width: px-1 puts the PageHeader, status alerts, and detail card ~4px from the viewport edge, and gap-4/py-4 halves the vertical rhythm (every sibling page uses px-4 sm:px-6 with py-6..8 and gap-6..8). Moving Dashboard -> Schedule visibly snaps all content to the left edge.
    EV: <main className="flex w-full flex-col gap-4 px-1 py-4">
    FIX: Keep the standard container (px-4 py-6 sm:px-6, gap-6) on the page; let only the calendar grid be full-bleed via a nested wrapper with negative margins (-mx-4 sm:-mx-6) if the calendar needs the width.
[high] app/(app)/create/page.tsx:116 :: Create and Automations (automations/page.tsx:104, identical class string) introduce a second container system that diverges from the incumbent `px-4 py-8 sm:px-6` at every breakpoint: mobile py-4 vs py-8, sm-md px-4 vs px-6, md+ px-8 vs px-6. These are the two newest screens, so the most-trafficked launch pages (Create hub) sit in a different spatial system than Media/Channels/Billing.
    EV: <div className="mx-auto w-full max-w-5xl space-y-8 p-4 md:p-8">
    FIX: Change both to the dominant recipe: `mx-auto flex w-full max-w-5xl flex-col gap-8 px-4 py-8 sm:px-6` (space-y-8 -> flex gap-8 for parity with siblings).
[medium] app/(app)/clips/page.tsx:103 :: Clips uses gap-6/py-6 while its direct peer library pages Media (media/page.tsx:67), Channels (:93), Analytics (:94), Billing (:51), Settings (:113) all use gap-8/py-8 — the PageHeader-to-content gap shrinks from 2rem to 1.5rem when tabbing between Media and Clips. Dashboard (dashboard/page.tsx:214 `gap-6 px-4 py-6`) shares the minority value, making 3 rhythm systems total in (app).
    EV: <main className="mx-auto flex w-full max-w-6xl flex-col gap-6 px-4 py-6 sm:px-6">
    FIX: Pick one rhythm for the (app) group — gap-8/py-8 is the majority (5 pages) — and apply it to clips and dashboard, or explicitly standardize everything down to gap-6/py-6; either way one value.
[medium] app/(app)/billing/page.tsx:51 :: Content max-width has no rule: settings 3xl, billing/create/automations 5xl, clips/media/channels/analytics/affiliate 6xl, dashboard 7xl, schedule unbounded. Settings-narrow and dashboard-wide are defensible, but Billing (5xl) vs Channels (6xl, channels/page.tsx:93) vs Analytics (6xl) are peer full-page lists whose content edges jump ~128px at >=1280px viewports.
    EV: <main className="mx-auto flex w-full max-w-5xl flex-col gap-8 px-4 py-8 sm:px-6">
    FIX: Define 2-3 named widths (e.g. narrow=3xl for settings-style forms, default=6xl, wide=7xl for dashboard) and map every page to one; move billing/create/automations from 5xl to the default 6xl.
[medium] app/(app)/settings/page.tsx:539 :: Peer section cards have no shared padding token — grep shows 31 `bg-card p-*` occurrences across 16 files spanning p-4/p-5/p-6 plus ad-hoc responsive escalation: settings Section p-6 (:539), billing p-6 (:166), analytics account card p-5 (:121), dashboard Clip Pipeline `p-5 shadow-sm sm:p-6` (:277), clips job card `p-5 sm:p-6` (:147), schedule detail p-4 (:393), create composer `p-4 sm:p-5` (composer.tsx:263). channe
    EV: <section className="rounded-lg border bg-card p-6 shadow-sm">
    FIX: Extract one Card primitive (or a `card` class) with a single padding (p-5 sm:p-6 is the closest to majority) and sweep the 16 files onto it; keep p-4 only for compact tiles (dashboard quick actions, connect tiles) as an explicit `compact` variant.
[medium] app/(app)/clips/page.tsx:108 :: The dashed empty-state card — the same visual pattern on four main screens — has four different paddings: clips px-6 py-14 (:108), media p-10 (media/page.tsx:113), dashboard px-6 py-12 (dashboard/page.tsx:417), analytics px-4 py-8 (analytics/page.tsx:101), composer p-10 (create/composer.tsx:548). Empty states are exactly what a new user sees first, and each screen's version is a different height.
    EV: <div className="flex flex-col items-center gap-3 rounded-lg border border-dashed bg-card px-6 py-14 text-center">
    FIX: One EmptyState component (icon + title + hint) with a single padding (p-10 or py-12) reused on clips, media, dashboard, analytics, and the create composer.
[low] app/(app)/affiliate/page.tsx:210 :: gap-7 is the only occurrence in the entire app/ tree (grep: 1 hit) — a one-off rhythm value between the siblings' gap-6 and gap-8. Same page's pending state (line 64) uses `max-w-3xl ... gap-6`, so the page also changes width and rhythm depending on approval state. Low user impact since Affiliate is hidden from nav for launch.
    EV: <main className="mx-auto flex w-full max-w-6xl flex-col gap-7 px-4 py-8 sm:px-6">
    FIX: Change gap-7 to gap-8 to match the (app) majority; keep the pending state's narrow width if intentional but align its gap to the same value.
[low] app/(admin)/admin/users/page.tsx:114 :: Admin is otherwise the most disciplined group — 11 of 13 pages use identical `gap-6 px-4 py-6 sm:px-6` — but the two users pages (here and users/[userId]/page.tsx:281) use gap-5, the only gap-5 containers in the console.
    EV: <div className="mx-auto flex w-full max-w-6xl flex-col gap-5 px-4 py-6 sm:px-6">
    FIX: Change both users pages to gap-6 to make the admin group 13/13 uniform.
  OBS: The root cause is that the spatial system lives only as a memorized class string, not as code: there is no PageContainer/Card primitive anywhere (no components/ui/card.tsx exists), so all 13 app pages and 13 admin pages hand-roll `mx-auto flex w-full max-w-* flex-col gap-* px-4 py-* sm:px-6` from memory. Where a primitive does exist — PageHeader — usage is perfectly consistent across every page, which proves extraction works in this codebase; where it doesn't, each value (gap, py, max-w, card padding) drifts independently. Drift correlates with recency: the two newest screens (Create hub and the reworked Automations, from the 2026-07-17 perception pack) invented a wholly new `space-y-8 p-4 md:p-8` recipe instead of copying a sibling, while the admin group — built in one pass — is 11/13 identical. Schedule shows the second failure mode: a legitimate need (full-width calendar) was solved b

== DIM: spacing-studio (9 confirmed) ==
[high] app/automations/page.tsx, app/clipping/page.tsx, app/media-library/page.tsx, app/batch-generate/page.tsx, app/products/page.tsx, app/product-library/page.tsx, app/librarie/page.tsx, app/calendar/page.tsx, app/batch/page.tsx, app/schedule/page.tsx, app/tts-library/page.tsx, app/create-video/page.tsx, app/create-image/page.tsx, app/product-video/page.tsx, app/wiki/page.tsx, app/settings/page.tsx, app/usage/page.tsx, app/pipeline/page.tsx:208 :: No shared page-shell/container component exists (confirmed in app/layout.tsx, which renders NavBarWrapper{children} with zero content-width wrapper), so every route hand-rolls its own top-level max-width. Six incompatible width scales are in simultaneous use across sibling routes reachable from the same nav: max-w-7xl/1280px (automations:208, clipping:202, media-library:93, batch-generate:250, products:457, product-l
    EV: automations/page.tsx:208 `mx-auto w-full max-w-7xl space-y-6 p-4 md:p-8` vs calendar/page.tsx:18 `max-w-[1400px] mx-auto p-6` vs product-video/page.tsx:248 `max-w-3xl mx-auto px-4 
    FIX: Introduce one PageShell/PageContainer component (single max-width token, e.g. max-w-7xl, plus a standard responsive padding recipe) and have every app/*/page.tsx route wrap its content in it instead of inlining ad hoc classes.
[high] app/automations/page.tsx:208, app/clipping/page.tsx:202, app/media-library/page.tsx:93, app/batch-generate/page.tsx:250, app/products/page.tsx:457, app/product-library/page.tsx:298:208 :: Even the 6 files that agree on max-w-7xl split into two incompatible padding recipes: `p-4 md:p-8` (automations, clipping, media-library) vs `px-4 sm:px-6 lg:px-8 py-8` (batch-generate, products, product-library). Below the md breakpoint the first group gives 16px on all sides while the second always forces py-8 (32px) top/bottom regardless of breakpoint — mobile/narrow-window vertical spacing differs by 2x between p
    EV: clipping/page.tsx:202 `mx-auto w-full max-w-7xl space-y-6 p-4 md:p-8` vs products/page.tsx:457 `max-w-7xl mx-auto px-4 sm:px-6 lg:px-8 py-8`
    FIX: Fold both groups into the single PageShell padding recipe from finding #1; there is no content-driven reason for two paddings on the same width class.
[high] app/pipeline/components/pipeline-stepper.tsx:133, app/pipeline/components/step2-tts.tsx:598, app/pipeline/components/step3-preview.tsx:543:133 :: The pipeline flow alone contains three different in-content toolbar treatments: the top PipelineStepper bar is h-14 with `bg-background/95` + `backdrop-blur`; the Step-2 workspace sub-header reuses h-14 but with a flat `bg-card` and no blur; the Step-3 workspace sub-header drops to h-10 (a different height entirely) with plain `bg-background`. Outside of /pipeline, no other route (products, schedule, settings, batch,
    EV: pipeline-stepper.tsx:133 `sticky top-0 z-20 flex h-14 shrink-0 items-center gap-4 border-b bg-background/95 px-4 backdrop-blur ...` vs step2-tts.tsx:598 `... min-[1280px]:flex min-
    FIX: Standardize on one toolbar height (h-14) and one background treatment for every sticky sub-header inside the pipeline workspace; Step-3's h-10 header should match Step-2's h-14 unless there's a deliberate density reason worth documenting.
[medium] app/librarie/page.tsx:1285, app/products/page.tsx:459, app/product-library/page.tsx:300, app/tts-library/page.tsx:394, app/usage/page.tsx:218, app/automations/page.tsx:208-209, app/media-library/page.tsx:93-94:218 :: Header-to-content spacing is encoded two incompatible ways. The newer trio (automations, clipping, media-library) puts the header row as a plain child of a `space-y-6` (24px) parent, so gap-to-content is implicit and consistent. The older pages instead hardcode a margin directly on the header div — `mb-6` in librarie/products/product-library/tts-library, but `mb-8` (32px) in usage — so usage's header-to-content gap i
    EV: usage/page.tsx:218 `<div className="flex items-center justify-between mb-8">` vs products/page.tsx:459 `<div className="flex items-center justify-between mb-6">` vs automations/pag
    FIX: Pick one mechanism (prefer the parent space-y-6 pattern used by the newer pages) and apply it everywhere; fix usage/page.tsx:218's mb-8 outlier to mb-6 at minimum.
[medium] app/product-video/page.tsx:281, app/product-video/page.tsx:312, app/product-video/page.tsx:355, app/product-video/page.tsx:377:281 :: Within one page, CardContent padding is overridden to a tighter `p-4` for the info/footage-mode cards but left at the shadcn default (~24px via the Card wrapper's `py-6` + CardContent's `px-6`) for the Generation Settings card immediately below it, with no content-driven reason for the switch — it reads as an unintentional copy-paste drift rather than a deliberate density choice.
    EV: product-video/page.tsx:281 `<CardContent className="p-4">` (product info card) and :312/:355 same, vs product-video/page.tsx:377 `<CardContent className="space-y-6">` (Generation S
    FIX: Pick one CardContent padding for stacked cards on this page (either p-4 throughout or default throughout) so the vertical rhythm down the page doesn't step in and out.
[medium] app/wiki/page.tsx:162, app/settings/page.tsx:868, app/settings/page.tsx:878, app/settings/page.tsx:891, app/usage/page.tsx:216:216 :: These three routes use Tailwind's built-in `container` utility instead of an explicit max-w-* token. In this codebase (no tailwind.config.js found; Tailwind v4 CSS-first config) `container` resolves to the current breakpoint width itself (up to 1536px at 2xl), which doesn't line up with any of the max-w-7xl (1280px), max-w-[1400px], or max-w-[1600px] scales used elsewhere — so it is a fourth/fifth de facto width scal
    EV: usage/page.tsx:216 `<div className="container mx-auto p-6">` — no equivalent max-w-* token anywhere else in the app
    FIX: Replace `container mx-auto p-6` with the same PageShell max-width used by the rest of the app (finding #1) instead of Tailwind's default responsive container.
[medium] app/create-video/page.tsx:83, app/create-image/page.tsx:848:83 :: The two sibling AI-generation pages (Create Video, Create Image — both reachable from the same Create hub) agree on max-w-5xl but disagree on horizontal padding: create-video uses `px-6`, create-image uses `px-4`, a 33% difference in side gutter between two pages meant to feel like tabs of one flow.
    EV: create-video/page.tsx:83 `mx-auto max-w-5xl space-y-6 px-6 py-8` vs create-image/page.tsx:848 `w-full max-w-5xl mx-auto py-8 px-4 space-y-6`
    FIX: Align both Create-* pages on identical container classes since they're presented as tabs of one feature.
[medium] app/calendar/page.tsx:18, app/librarie/page.tsx:1265, app/batch/page.tsx:378, app/schedule/page.tsx:480, app/tts-library/page.tsx:392:18 :: All five pages share the max-w-[1400px] scale but use three different padding recipes: calendar uses flat `p-6`; librarie uses `px-6 py-8`; batch/schedule/tts-library use `px-6 md:px-10 lg:px-16 py-8` (a responsive ramp up to 64px side gutters at lg). Calendar and Librarie never widen their side gutter past 24px even on very wide screens, while Batch/Schedule/TTS-Library triple it — visibly different edge margins on 
    EV: calendar/page.tsx:18 `max-w-[1400px] mx-auto p-6` vs batch/page.tsx:378 `w-full max-w-[1400px] mx-auto px-6 md:px-10 lg:px-16 py-8`
    FIX: Standardize the padding ramp for every max-w-[1400px] page; if the wider lg:px-16 gutter is intentional for data-dense pages, document it as the deliberate exception rather than leaving 2 of 5 pages on a flat p-6/px-6.
[low] app/automations/page.tsx:216, app/media-library/page.tsx:97, app/clipping/page.tsx:210:216 :: Only 3 of ~17 in-scope pages apply `font-heading` (Bricolage Grotesque, the token's designated heading font) to their page-title h1; the remaining pages (calendar:19, librarie:1287, create-image:852, create-video:87, product-video:263, products:461, product-library:302, tts-library:396, batch:383, schedule:485, settings:895, usage:226, wiki:165, batch-generate:259) render the h1 in the default UI sans font at inconsi
    EV: media-library/page.tsx:97 `<h1 className="font-heading text-3xl font-bold tracking-tight">` vs calendar/page.tsx:19 `<h1 className="text-2xl font-bold mb-6">Calendar</h1>` (no font
    FIX: Apply font-heading + one consistent size (text-3xl tracking-tight, matching the 3 newer pages) to every page-title h1 for a uniform header block height across routes.
  OBS: There is no shared page-shell/layout primitive anywhere in the Studio app — app/layout.tsx wraps children directly in NavBarWrapper with zero content-width wrapper, so all 20+ routes independently reinvent the outer container div (max-width, mx-auto, padding, space-y). The result is at least six distinct width scales in concurrent use (max-w-7xl, max-w-[1400px], max-w-5xl, max-w-3xl, max-w-[1600px], and Tailwind's bare `container`) and at least four distinct padding recipes layered on top of them, with no correlation between "feels like the same product area" (e.g. Products/Product Library/Product Video, or Create Video/Create Image) and which scale a page picked. A visible three-file cluster (automations, clipping, media-library) shares one clean, modern recipe (`max-w-7xl space-y-6 p-4 md:p-8` + `font-heading` h1) that looks like a recent, coordinated rewrite; nearly every other route 

== DIM: typography (7 confirmed) ==
[high] C:\obSID SRL\n8n\edit_factory\frontend\src\app (multiple):226 :: 14 of 20 top-level Studio pages render their h1 page title without font-heading, so the page falls back to the UI body font (Instrument Sans) instead of Bricolage Grotesque for the single most prominent element on the screen. Only 6/20 (clipping, automations, media-library, setup, login, auth-callback) apply font-heading; the rest (usage, create-video, batch-generate, product-library, settings, products, calendar, sc
    EV: app/usage/page.tsx:226 `<h1 className="text-3xl font-bold text-foreground">Usage & Costs</h1>` (no font-heading) vs app/clipping/page.tsx:210 `<h1 className="font-heading text-3xl 
    FIX: Extract a shared PageTitle/PageHeader component (mirroring web's components/page-header.tsx: `font-heading text-3xl font-bold tracking-tight`) and swap all 14 offending h1s to it in one pass; grep for `<h1 className="text-[23]xl font-bold` to find every remaining site.
[high] C:\obSID SRL\n8n\edit_factory\frontend\src\app\schedule\page.tsx:485 :: Even among the pages sharing the same 'top-level page title' role, the size itself is split roughly evenly between text-3xl and text-2xl with no structural reason (not nested, not a sub-page) — same role, two different scales.
    EV: schedule/page.tsx:485 `<h1 className="text-2xl font-bold tracking-tight">Smart Schedule</h1>` and calendar/page.tsx:19 `<h1 className="text-2xl font-bold mb-6">Calendar</h1>` vs cl
    FIX: Standardize every top-level page title on text-3xl (matching web's PageHeader) once the shared component from the previous finding is introduced; text-2xl should be reserved for dialog/section headers only.
[medium] C:\obSID SRL\n8n\social-scheduler\app\(admin)\admin\page.tsx:85 :: Web app's (app) routes all funnel page titles through components/page-header.tsx (font-heading text-3xl font-bold tracking-tight), but every (admin) page hardcodes its own h1 at text-2xl instead of reusing PageHeader, so admin screens read one visual step smaller than the rest of the app for the same 'page title' role.
    EV: app/(admin)/admin/page.tsx:85 `<h1 className="font-heading text-2xl font-bold tracking-tight">Overview</h1>` — same pattern repeated verbatim (just swapping the word) in admin/sett
    FIX: Either add an AdminPageHeader wrapping PageHeader with text-2xl as a deliberate, named admin scale, or reuse PageHeader directly if the smaller admin scale wasn't an intentional decision — right now it looks like drift from copy-pasting rather than a design choice.
[medium] C:\obSID SRL\n8n\social-scheduler\components\app-nav.tsx:124 :: The uppercase sidebar section-label pattern (nav group headers like 'CREATE') is a shared concept across both products but is implemented with three different values for size, weight, tracking and opacity in each sibling.
    EV: web components/app-nav.tsx:124 `text-[10px] font-semibold tracking-[0.14em] text-sidebar-foreground/35 uppercase` vs studio components/navbar.tsx:178 `text-[11px] font-medium track
    FIX: Codify this as a shared `.nav-group-label` utility (or documented token) with one size/weight/tracking/opacity, and use it verbatim in both app-nav.tsx and navbar.tsx — the approved-decisions doc already treats this as a shared concept that should match label+icon style, not just
[medium] C:\obSID SRL\n8n\edit_factory\frontend\src\app\pipeline\components\step3-preview.tsx:253 :: User complaint 'heavy text thrown around': wizard step headings bury a live dynamic count inside the title string itself instead of a clean title + muted meta line, producing a long, re-flowing heading users have to re-read on every state change.
    EV: step3-preview.tsx:253 `<h2 className="text-2xl font-semibold">Preview & Select Variants ({previewCards.filter(card => selectedVariants.has(card.baseIndex)).length} previews shown)<
    FIX: Split into `<h2 className="font-heading text-2xl font-semibold">Preview & Select Variants</h2>` plus a separate `<p className="text-sm text-muted-foreground">{n} previews shown</p>` meta line below it — same information, clearer hierarchy, and stops the heading text from shifting
[low] C:\obSID SRL\n8n\social-scheduler\app\(app)\channels\page.tsx:124 :: Within the web app alone, the 'section header inside a card' role is split between text-lg (the majority — settings.tsx:540, billing/page.tsx:168/218, automations/page.tsx x3, schedule/page.tsx:298, channels/connect-tile.tsx:61) and text-xl (channels/page.tsx:124,183; dashboard/page.tsx:280; clips/new-run.tsx:71; schedule/post-form.tsx:310) with no visible rule for which sections get the larger size.
    EV: channels/page.tsx:124 `<h2 className="font-heading text-xl font-semibold">` vs settings/page.tsx:540 `<h2 className="mb-5 font-heading text-lg font-semibold">{title}</h2>` — both a
    FIX: Standardize section/card headers on text-lg (the majority pattern) and reserve text-xl for a genuinely larger emphasis case, or fold this into the same PageHeader-adjacent SectionHeader helper suggested above.
[low] C:\obSID SRL\n8n\edit_factory\frontend\src\components\ui\card.tsx:35 :: Studio's shadcn CardTitle never applies font-heading by default (`leading-none font-semibold` only), so every CardTitle across the app renders in the UI body font unless a call site manually adds font-heading — only signup/page.tsx:91,121 and login/reset-password/page.tsx:81 do; the ~40 other CardTitle usages (settings.tsx, usage/page.tsx, schedule/page.tsx, create-image/page.tsx, automations/page.tsx) do not.
    EV: components/ui/card.tsx:35 `className={cn("leading-none font-semibold", className)}` (no font-heading) vs signup/page.tsx:91 `<CardTitle className="font-heading text-2xl font-bold t
    FIX: Bake font-heading into the CardTitle primitive itself (components/ui/card.tsx:35) so every card title inherits the heading font automatically, removing the need to remember it per call site.
  OBS: Both apps share the same design tokens file but not a shared component library — the web app funnels nearly all page/section titles through components/page-header.tsx, while Studio has no equivalent primitive, so its ~20 top-level pages each hand-roll their own h1 (font-heading present in only 6/20, size split ~50/50 between text-2xl and text-3xl). The root cause is structural: Studio's page titles, CardTitle, and Label are all shadcn primitives with no opinionated default for font-heading or size, so every call site re-decides typography from scratch, and drift compounds page by page (45 Label overrides to text-xs vs 14 at text-sm vs 54 left at default). The web app is more disciplined at the page-title layer (via PageHeader) but still forks into a second, undocumented scale for admin/(admin) pages, and its section-header (h2) sizing splits between text-lg/text-xl with no rule. The one 

== DIM: interactive-states (7 confirmed) ==
[high] edit_factory/frontend/src/components/navbar.tsx:420 :: Mobile-header sign-out icon button has zero hover/focus feedback (className is just "flex items-center"), while the identical action rendered in the desktop sidebar (same file, lines 348 and 397) uses the full hover:bg-sidebar-accent hover:text-sidebar-foreground transition-colors treatment.
    EV: className="flex items-center" (line 424) vs. desktop sidebar's className="flex size-9 items-center justify-center rounded-lg text-sidebar-foreground/60 transition-colors hover:bg-s
    FIX: Reuse the same icon-button hover class (rounded-lg + hover:bg-sidebar-accent hover:text-sidebar-foreground + transition-colors) on the mobile header sign-out button so the same action gives the same feedback everywhere.
[high] social-scheduler/app/(app)/media/media-card.tsx:104 :: The thumbnail preview button — a clickable card that opens a dialog — has no hover feedback at all (no hover:opacity, no hover:ring, no overlay), so it reads as static even though it's the primary click target on the card.
    EV: className="flex aspect-video w-full cursor-pointer items-center justify-center bg-muted" (only cursor-pointer, no hover: class)
    FIX: Add a subtle hover cue matching other media/clip tiles in the codebase, e.g. hover:brightness-110 or a group-hover play-icon overlay, plus transition-opacity/transition-colors, so users get feedback before clicking to preview.
[medium] edit_factory/frontend/src/components/navbar.tsx:129 :: Inconsistent hover grammar for the same nav-link component type: Studio's NavLink swaps hover text color to a hardcoded brand color (hover:text-lime), while the mirrored web AppNav renderItem (components/app-nav.tsx:96) and Studio's own admin-nav / app-sidebar / product-switcher all use the semantic hover:text-sidebar-accent-foreground / hover:text-sidebar-foreground token instead.
    EV: text-sidebar-foreground/65 hover:bg-sidebar-accent/60 hover:text-lime (navbar.tsx:129) vs. text-sidebar-foreground/65 hover:bg-sidebar-accent/60 hover:text-sidebar-accent-foregroun
    FIX: Use hover:text-sidebar-accent-foreground (or hover:text-sidebar-foreground, matching the app's other sidebar buttons) instead of a hardcoded hover:text-lime so nav-item hover color follows the same token as every other sidebar hover state in both apps.
[medium] edit_factory/frontend/src/app/automations/page.tsx:327 :: The enabled/paused Badge toggle button has no className at all — no hover background, no cursor styling override, no transition — despite being a real click target that flips automation state.
    EV: <button type="button" onClick={() => void toggleAutomation()} disabled={saving}>\n <Badge variant={selected.enabled ? "default" : "secondary"}>
    FIX: Wrap the Badge in a button with hover:opacity-80 transition-opacity (or hover:brightness-95) so the toggle affordance is visible, matching how other toggle controls (e.g. workflow-row.tsx's enable switch) show state changes.
[low] edit_factory/frontend/src/app/segments/page.tsx:2288 :: Two structurally identical 'selectable list row' patterns in the same file use different hover grammar: the source-video row hovers with a background fill (hover:bg-muted), the segment row directly below/adjacent hovers with only a border tint (hover:border-primary/50), so otherwise-parallel rows give different feedback for the same interaction.
    EV: source video row: selectedVideo?.id === video.id ? "bg-primary/10 border border-primary/50" : "hover:bg-muted" (line 1966) vs. segment row: selectedSegment?.id === segment.id ? "bo
    FIX: Standardize both row types on the same unselected-hover treatment (e.g. hover:bg-muted plus a subtle border tint) so adjacent list rows in the same picker feel like one system.
[low] social-scheduler/app/(admin)/admin/content/pages/[pageId]/block-editor.tsx:83 :: The move-up/move-down/remove buttons on each content block have zero hover feedback (no hover:bg-*, no transition-colors), unlike nearly every other bordered button elsewhere in the app which pairs a border with a hover background.
    EV: className="rounded border border-sidebar-border px-2 py-1 disabled:opacity-30" (lines 87, 95) and className="rounded border border-destructive/40 px-2 py-1 text-destructive" (line 
    FIX: Add hover:bg-muted transition-colors (and hover:bg-destructive/10 for the remove button) to match the rest of the app's bordered-button hover convention.
[low] edit_factory/frontend/src/components/video-segment-player.tsx:1274 :: Playback-rate <select> kills the native focus outline (outline-none) without any focus-visible replacement, so keyboard users get no visible focus indicator on this control — the only truly focus-killed interactive element found in either scoped codebase.
    EV: className="h-7 rounded border border-white/15 bg-black/50 px-1 text-[11px] text-white outline-none hover:bg-black/75"
    FIX: Add a focus-visible:ring-2 focus-visible:ring-primary/50 (or reuse the shared focus-visible:border-ring pattern from ui/input.tsx) instead of a bare outline-none.
  OBS: Both apps' shared primitives (Button, nav links, ProductSwitcher/ProfileSwitcher, the pipeline stepper, workflow-row hover-row) are genuinely well-crafted — transition-colors + a token-based hover fill is the dominant, correct grammar almost everywhere, and it clearly propagated well between the two codebases since they were built by mirroring one another. The drift shows up specifically where a screen was built ad hoc outside the shared component library: a raw <button> or <div onClick> assembled inline (media-card thumbnail, the automations Badge toggle, admin block-editor controls, a mobile-only header variant) skips the hover class that the "canonical" sibling element two lines away already has. The second, subtler pattern is same-concept-different-token: when two nav/list-row implementations exist in parallel (web AppNav vs Studio NavLink; segments source-video row vs segment row; B

== DIM: controls (6 confirmed) ==
[high] components/ui/select.tsx (studio) vs app/(app)/settings/page.tsx etc (web):133 :: Web has NO Input/Select/Textarea primitive in components/ui/ at all (only button.tsx exists there); raw <select>/<input> are hand-rolled in at least 15 files with a different className string each time — different radius (rounded / rounded-md / rounded-lg / rounded-full), different border tokens (border-input / border-sidebar-border / border-border / no border), and several strip the browser's default focus outline v
    EV: app/(app)/settings/page.tsx:133 `className="rounded-lg border border-input bg-card px-2 py-1.5 text-sm text-foreground outline-none"` and app/(app)/schedule/post-form.tsx:716 `clas
    FIX: Add components/ui/input.tsx and components/ui/select.tsx to the web app (port from studio's, they already match the shared token system) and migrate the ~15 ad-hoc select/input strings to them. At minimum restore focus-visible:ring-ring/50 wherever outline-none is used bare.
[medium] app/(app)/media/media-card.tsx:139 :: Raw icon <button> elements with no fixed square footprint (`p-2` only, no height/width), diverging from the Button primitive's icon-sm (size-8) / icon (size-9) scale used elsewhere. 109 raw <button> occurrences exist across 35 web files while only 30 files even import Button — most button-bearing files never touch the shared primitive.
    EV: media-card.tsx:139 `className="rounded-md p-2 text-muted-foreground transition-colors hover:bg-lime/10 hover:text-lime disabled:opacity-50"` (repeats near-identically at lines 151 
    FIX: Replace these three icon buttons with `<Button variant="ghost" size="icon-sm">` so hover/disabled/focus states and the hit-box match every other icon action in the app.
[medium] app/(admin)/admin/affiliates/page.tsx:377 :: Admin page hand-rolls 8 separate ad-hoc buttons, each with its own radius/padding, none matching the Button component's h-8/h-9 size scale, and none carrying any focus-visible ring — keyboard users get no focus indicator on any admin action here.
    EV: Line 138: `className="rounded-md border border-lime/30 px-3 py-2 text-sm font-medium text-lime"`; line 227-228: `className="mt-3 rounded bg-lime px-3 py-1.5 text-sm font-medium tex
    FIX: Route these through <Button variant="outline" size="xs|sm"> (web's button.tsx already has an xs size at h-6) — cuts ~8 divergent className strings down to variant/size props and restores focus-visible rings for free.
[medium] components/ui/button.tsx (web) vs edit_factory/frontend/src/components/ui/button.tsx (studio):22 :: The two Button primitives have drifted on the actual pixel scale behind identical variant/size names: web's `size="default"` is h-8 and `size="lg"` is h-9 with rounded-lg buttons overall (border-radius via the shared `rounded-lg` on the base class); studio's `size="default"` is h-9 and `size="lg"` is h-10 with `rounded-md`. Studio also has a `cta` variant (`bg-lime text-ink`) that has no counterpart in web's variant 
    EV: web button.tsx:23-24 `default: "h-8 gap-1.5 px-2.5 ..."` vs studio button.tsx:29 `default: "h-9 px-4 py-2 has-[>svg]:px-3"`; web:27 `lg: "h-9 gap-1.5 px-2.5 ..."` vs studio:31 `lg:
    FIX: Not necessarily wrong to differ (they're separate codebases), but worth an explicit decision doc since a plain 'default button' is objectively a different height/radius in each product a user may see side by side (e.g. via the product switcher) — pick one canonical h-8/h-9 defaul
[low] app/pipeline/components/step3-preview.tsx:390 :: Icon-sizing grammar is mixed within the same file/control family in studio: some small status icons use the old `h-3 w-3` two-class syntax, others use the modern `size-*` single-class syntax — inconsistent even inside one component. Studio-wide this pattern repeats at scale: 598 `h-N w-N` occurrences across 50 files vs 254 `size-N` occurrences across 41 files, i.e. both syntaxes are in live use simultaneously. Web, b
    EV: step3-preview.tsx:390 `<Loader2 className="h-3 w-3 animate-spin" />`, :396 `<CheckCircle className="h-3 w-3 text-success" />`, :402 `<AlertCircle className="h-3 w-3 text-red-600" /
    FIX: Standardize on `size-*` (matches button.tsx's own `[&_svg:not([class*='size-'])]:size-4` selector, which silently no-ops on h-w icons anyway) and sweep the codebase once with a find/replace.
[low] components/app-sidebar.tsx:49 :: The same conceptual control (sidebar rail toggle chevron) is sized differently depending on collapsed vs expanded state: size-9 when collapsed, size-8 when expanded; the sign-out icon button next to it is also size-8.
    EV: line 53: expand-state button `className="flex size-9 items-center justify-center rounded-lg ..."`; line 99: collapse-state button `className="flex size-8 shrink-0 items-center just
    FIX: Pick one size (size-8 to match Button's icon-sm, or size-9 to match icon) for all rail icon buttons regardless of collapsed state.
  OBS: Web (social-scheduler) only defines a Button primitive in components/ui/ — there is no Input, Select, or Textarea component — so every form control outside the small set of pages that borrow raw <input>/<select> markup gets its styling reinvented per-file (at least 8 distinct locally-scoped className constants found: inputClass, fieldInput, pillSelect, inputCls, and several inline one-offs), each drifting independently on radius, border color token, and — most seriously — several strip outline-none with no focus-visible replacement, a real keyboard-accessibility regression on live admin/settings screens. Studio, by contrast, has a nearly-complete shadcn-style components/ui/ (24 files including input/select/textarea/tabs) and most of its screens correctly consume it, but studio's own consistency is undermined by two things: (1) an entire unused primitive (Tabs) sitting dead while every se

== DIM: radius-borders (8 confirmed) ==
[high] edit_factory/frontend/src/components/ui/button.tsx:8 :: Studio's shared Button primitive is rounded-md (8px, --radius*0.8) while Blipost web's shared Button primitive (components/ui/button.tsx:7) is rounded-lg (10px, the base --radius token). Both repos define the identical --radius scale (--radius-lg = var(--radius) = 0.625rem in both globals.css), so this is a straight authoring divergence, not a token difference: the two apps render the single most common interactive e
    EV: studio: `"inline-flex items-center justify-center gap-2 whitespace-nowrap rounded-md text-sm font-medium..."` (button.tsx:8) vs web: `"group/button inline-flex shrink-0 items-cente
    FIX: Pick one radius for the Button primitive (rounded-lg matches the base --radius token and is what the web app already standardized on) and change studio's buttonVariants base + sm/lg size overrides (button.tsx:30-31, also rounded-md) to match.
[medium] edit_factory/frontend/src/components/ui/card.tsx:10 :: Studio's shared Card bakes in shadow-sm unconditionally, so every one of the ~36 files that import Card gets an elevation shadow by default. Blipost web's card convention (hand-rolled `rounded-lg border bg-card`) almost never adds a shadow (only ~5 of ~60+ card instances use shadow-sm, e.g. dashboard.tsx:277, billing/page.tsx via Button, new-run.tsx:68). This produces a systematically different 'elevation story' betw
    EV: card.tsx:10 includes `shadow-sm` unconditionally; callers override it with `shadow-none` in at least 9 files, e.g. app/pipeline/components/source-videos-card.tsx, app/pipeline/comp
    FIX: Remove shadow-sm from the base Card class and add it only via a variant/className on cards that need elevation (e.g. modals, floating panels), matching web's sparse/no-shadow default for in-page cards.
[medium] app/(app)/dashboard/page.tsx:277 :: Same-file, same-role top-level dashboard `<section>` cards disagree on shadow: the Clip Pipeline section has shadow-sm, but the Upcoming Posts (line 445), Recent Activity (line 502), and Schedule Preview (line 655) sections use the identical `rounded-lg border bg-card p-5` wrapper with no shadow.
    EV: line 277: `<section className="rounded-lg border bg-card p-5 shadow-sm sm:p-6">` vs line 445/502/655: `<section className="rounded-lg border bg-card p-5">`
    FIX: Drop shadow-sm from the one outlier section (or add it consistently to all dashboard sections) so elevation isn't arbitrary within a single page.
[medium] app/(app)/studio/page.tsx:26 :: The BlipStudio-launch error card is the only card-role container in the scoped web app using rounded-2xl, and its icon badge on the next line uses rounded-xl — both diverge from the rounded-lg convention used for ~90+ other section/modal/card containers across affiliate, dashboard, billing, channels, clips, schedule, media, and settings pages.
    EV: line 26: `<section className="w-full max-w-lg rounded-2xl border border-border bg-card p-8 text-center shadow-sm">`; line 27: `<span className="mx-auto flex size-12 items-center ju
    FIX: Change both to rounded-lg to match every other card/badge in the app.
[medium] app/(app)/create/composer.tsx:263 :: The outer Create-hub composer card uses rounded-xl while the tab strip nested one line below it (line 265) and every other card-role element in the same file (lines 469, 473, 497, 521, 534, 548, 559, 592) use rounded-lg — an unmotivated one-off on the single most-visited page in the app.
    EV: line 263: `<section className="rounded-xl border border-border bg-card p-4 sm:p-5">` vs line 265 (2 lines later): `<div className="flex gap-1 rounded-lg border border-border bg-bac
    FIX: Change line 263 to rounded-lg for consistency with the rest of the same component and the app-wide card convention.
[low] app/(app)/billing/page.tsx:178 :: The credit top-up package tile is rounded-md, while every other tile/card-role element on the same page (plan status card, add-credits section wrapper, alert banners) is rounded-lg — same-role divergence within one page.
    EV: line 178: `className="flex items-center justify-between gap-4 rounded-md border p-4"` vs line 166 (parent section, same page): `<section className="rounded-lg border bg-card p-6">`
    FIX: Change to rounded-lg to match the page's other tiles.
[low] app/(app)/schedule/page.tsx:354 :: Icon-buttons for the same role ('small square icon button', size-7/8) render at two different radii depending on file: schedule/page.tsx uses rounded-md at lines 316, 323, 354, 365, 411, and schedule/post-sheet.tsx:44 also uses rounded-md, while dashboard/page.tsx:565 uses rounded-lg for its equivalent icon chip. There's no consistent radius rule for the 'small icon control' role across the app.
    EV: schedule/page.tsx:354: `"flex size-7 items-center justify-center rounded-md border hover:bg-muted"` vs dashboard/page.tsx:565: `"flex size-7 items-center justify-center rounded-lg 
    FIX: Pick one radius for square icon-buttons (rounded-lg matches the Button primitive) and apply it uniformly, or route these through the shared <Button size="icon-sm"> variant instead of hand-rolled classes.
[medium] edit_factory/frontend/src/components/thumbnail-picker.tsx:120 :: Studio's 'selected item' affordance is a decorative ring-2 ring-primary/40 (used repeatedly: thumbnail-picker.tsx:120,165; app/librarie/page.tsx:1451-1452,2101-2104; app/create-image/page.tsx:921; components/video-processing/variant-triage.tsx:126; components/video-processing/subtitle-editor.tsx:611; components/tts/provider-selector.tsx:67), while Blipost web expresses the same 'selected' concept with border+backgrou
    EV: studio: `isSelected ? "border-primary ring-2 ring-primary/40" : "border-transparent"` (thumbnail-picker.tsx:120) vs web: `target === "cloud" ? "border-lime/50 bg-lime/5" : "hover:b
    FIX: Standardize the 'selected' affordance to one pattern across both products — either both use a ring, or both use border+tint — so a user moving between web and desktop sees the same selection language.
  OBS: Neither app has a single canonical "card" component that all pages route through, so radius/border/shadow decisions get re-authored by hand on every page — the web app avoids a shared Card entirely (grep found zero components/ui/card.tsx), while studio has one (components/ui/card.tsx) but the shared Button and Input primitives inside the same kit still disagree with it on radius (rounded-xl vs rounded-md), meaning even studio's "system" isn't self-consistent. Because both repos define the identical --radius token scale (--radius-lg = var(--radius) = 0.625rem in both globals.css), every divergence found is a pure authoring drift, not a legitimate token difference — there is no design reason for studio's Button (8px) to differ from web's Button (10px), or for studio's Card (14px) to differ from its own Button (8px). Shadows show the same root cause from the opposite direction: studio bakes

== DIM: shell-navigation (12 confirmed) ==
[high] C:\obSID SRL\n8n\edit_factory\frontend\src\app\settings\page.tsx:895 :: Studio has no PageHeader component; every page hand-rolls its title. Grep: only 3 studio pages use the canonical web grammar "font-heading text-3xl font-bold tracking-tight" (automations, clipping, media-library — the recently-mirrored ones), 6 pages use plain "text-3xl font-bold" (no font-heading, so they render in Instrument Sans instead of Bricolage Grotesque), and 8 pages use "text-2xl font-bold" (settings, sched
    EV: <h1 className="text-3xl font-bold">Settings</h1> — also schedule/page.tsx:485 "text-2xl font-bold tracking-tight", calendar/page.tsx:19 "text-2xl font-bold mb-6", usage/page.tsx:22
    FIX: Port web's components/page-header.tsx into studio verbatim and replace the ~17 hand-rolled h1s with it. This is the single highest-leverage shell fix.
[high] C:\obSID SRL\n8n\edit_factory\frontend\src\components\navbar.tsx:66 :: "Media Library" is a shared concept (same cloud library, bridged between products) but the icon differs: Images in web, Cloud in studio. This violates the stated rule that shared concepts must match label+icon. Schedule (CalendarClock), Automations (Workflow), Clipping (Scissors) and Settings all match — Media Library is the one drift, visible in the nav on every screen of both apps.
    EV: { label: "Media Library", href: "/media-library", icon: Cloud } vs web app-nav.tsx:49 { href: "/media", label: "Media Library", icon: Images }
    FIX: Pick one icon (Images, matching web, since web is the launch surface) and use it in studio's navbar.tsx.
[medium] C:\obSID SRL\n8n\edit_factory\frontend\src\components\product-switcher.tsx:17 :: The product switcher is the same forked component in both apps, but the descriptions of the SAME two products differ per app. A user who opens the switcher in web sees BlipStudio described as "Turn scripts and footage into videos, in bulk"; in studio the same product says "Turn ideas and footage into videos". BlipCreative's description differs even more. The switcher is the bridging surface — the one place both apps 
    EV: creative: "Create, schedule and publish content" / studio: "Turn ideas and footage into videos" — vs web product-switcher.tsx:14 "Generate images, video & voice" and :22 "Turn scri
    FIX: Make the PRODUCTS array copy identical in both files (pick one description per product) and add it to the mirror-parity checklist so future edits change both.
[medium] C:\obSID SRL\n8n\edit_factory\frontend\src\components\navbar.tsx:129 :: Nav item hover treatment forked: studio hovers turn the label lime, web hovers turn it neutral foreground. Every other class on the nav item is byte-identical between the two apps ("flex shrink-0 items-center gap-2.5 rounded-lg text-sm font-medium transition-colors", active = bg-sidebar-accent), so this is a one-token drift on the most-touched interactive element of the shell.
    EV: : "text-sidebar-foreground/65 hover:bg-sidebar-accent/60 hover:text-lime" vs web app-nav.tsx:96 "... hover:text-sidebar-accent-foreground"
    FIX: Align studio's NavLink hover to web's hover:text-sidebar-accent-foreground (lime hover text is not part of the approved lime-accent list, which covers ui/ controls).
[medium] C:\obSID SRL\n8n\edit_factory\frontend\src\components\navbar.tsx:178 :: Sidebar group-label grammar drifted on four axes: size (11px vs 10px), weight (medium vs semibold), tracking (tracking-wider vs 0.14em), opacity (/40 vs /35), plus top padding (pt-3 vs pt-4). The nav list gap also differs: studio nav uses "flex flex-col gap-1" (navbar.tsx:175) vs web "flex flex-col gap-0.5" (app-nav.tsx:117), making studio's rail visibly looser.
    EV: "px-3 pt-3 pb-1 text-[11px] font-medium tracking-wider text-sidebar-foreground/40 uppercase first:pt-0" vs web app-nav.tsx:124 "px-3 pt-4 pb-1 text-[10px] font-semibold tracking-[0
    FIX: Copy web's group-label class string and gap-0.5 into studio's AppNav — it is a two-line diff.
[medium] C:\obSID SRL\n8n\edit_factory\frontend\src\components\product-switcher.tsx:80 :: Radius grammar fork in the sidebar footer stack: studio upgraded the product-switcher button, credit pill, and user card to rounded-xl while web keeps all three at rounded-lg. Same components, same slot, different corner radius — noticeable when switching products because these elements sit in identical positions.
    EV: "w-full gap-2.5 rounded-xl px-3 py-2.5" vs web product-switcher.tsx:74 "w-full gap-2.5 rounded-lg px-3 py-2.5"; same fork on navbar.tsx:220 (CreditBalance "rounded-xl border border
    FIX: Standardize on rounded-lg (web's choice, closest to --radius 0.625rem) in studio's navbar.tsx and product-switcher.tsx footer cards.
[medium] C:\obSID SRL\n8n\edit_factory\frontend\src\components\navbar.tsx:223 :: The shared "credits balance" concept renders with different iconography and treatment in the same sidebar-footer slot: web uses Zap in lime with a quota progress bar, studio uses a neutral Wallet pill with no lime and no quota. Since studio's balance IS the Blipost platform balance (fetched from /platform/me), a user moving between apps sees the same number branded two different ways.
    EV: <Wallet className="size-4 text-sidebar-foreground/70" /> ... {balance.toLocaleString()} credits — vs web app-sidebar.tsx:135-146 "AI Credits Remaining" + <Zap className="size-3.5 t
    FIX: Reuse web's credit-card visual (Zap + lime + "AI Credits Remaining" label) in studio's CreditBalance; quota bar can be omitted if the API lacks quota, but icon and accent should match.
[medium] C:\obSID SRL\n8n\edit_factory\frontend\src\components\navbar.tsx:59 :: Icon grammar collisions in studio nav: (a) Clapperboard is used twice inside the same "Create" group (Video Pipeline and AI Video) and is simultaneously the BlipStudio product icon in the switcher, so three different things share one glyph; (b) the label "Generate" exists in both apps with different icons and different meanings (web /create hub with Sparkles vs studio context-video generator with Video), muddying the
    EV: { label: "AI Video", href: "/create-video", icon: Clapperboard } duplicates line 55 { label: "Video Pipeline", href: "/pipeline", icon: Clapperboard }; also line 77 { label: "Gener
    FIX: Give AI Video a distinct icon (e.g. lucide Film or Video) and rename studio's context "Generate" (e.g. "Context Video") or align its icon with the web concept it maps to.
[medium] C:\obSID SRL\n8n\edit_factory\frontend\src\app\pipeline\components\step3-preview.tsx:543 :: Studio's sticky toolbars disagree with each other: the pipeline stepper bar is h-14 with backdrop-blur/95-opacity surface, while step3-preview's inner sticky bar is h-10 flat bg-background, and both live on the same screen. Web app pages have no per-page sticky toolbar at all (only the md:hidden mobile header, app/(app)/layout.tsx:73). Context on the known bug: the stepper centers its step indicator with "absolute le
    EV: "sticky top-0 z-10 hidden h-10 items-center border-b bg-background px-4 min-[1280px]:flex" vs pipeline-stepper.tsx:133 "sticky top-0 z-20 flex h-14 ... border-b bg-background/95 px
    FIX: Define one toolbar height/surface for studio (h-14 + backdrop-blur, matching the stepper) and, when fixing the known overlap bug, switch the stepper to a flex layout (context | flex-1 stepper | actions) instead of absolute centering.
[low] C:\obSID SRL\n8n\edit_factory\frontend\src\components\navbar.tsx:315 :: The w-16 collapse rail — otherwise mirrored — drifts in detail: chevron top padding pt-6 vs pt-4, and studio drops the group-divider lines web draws between icon groups, so studio's rail is one undifferentiated icon column (16 icons) while web's is visually chunked. Web's rail also keeps a lime Zap credits shortcut (app-sidebar.tsx:70-76) that studio's rail omits.
    EV: collapsed rail: "flex justify-center px-2 pt-6 pb-2" vs web app-sidebar.tsx:48 "flex justify-center px-2 pt-4 pb-2"; and studio's collapsed AppNav (navbar.tsx:160-171) flatMaps all
    FIX: Add the collapsed-mode group dividers and pt-4 to studio's rail; consider a credits shortcut icon for parity.
[low] C:\obSID SRL\n8n\edit_factory\frontend\src\components\navbar.tsx:234 :: Brand lockup treatment differs: web shows the bare logo at h-11 linking to "/"; studio wraps it in a dark ink pill at h-8 linking to /pipeline. The pill is justified in-code for the light-theme sidebar, but the ~40% size difference makes the header block visibly different when switching products.
    EV: <span className="inline-flex rounded-md bg-ink px-2 py-1.5"> wrapping the logo, sized h-8 (navbar.tsx:360) vs web app-sidebar.tsx:93 <BrandLogo priority className="h-11" /> bare, l
    FIX: Match the rendered logo height (h-11 equivalent inside the pill) so the sidebar header rows align across apps; keep the pill for light-theme contrast.
[low] C:\obSID SRL\n8n\social-scheduler\components\app-nav.tsx:85 :: Active-route detection forked: web uses a bare prefix match while studio uses the boundary-safe variant. No current web routes collide (no href is a prefix of another), but the moment one is added (e.g. /create vs /create-x) web will highlight two nav items at once — a latent divergence in shell behavior between the apps.
    EV: const active = pathname.startsWith(item.href); vs studio navbar.tsx:102-104 pathname === href || pathname.startsWith(href + "/")
    FIX: Adopt studio's boundary-safe matcher in web's app-nav.tsx (one-line change).
  OBS: The shell core proves the parity mechanism works when a component is mirrored wholesale: sidebar widths (w-64/w-16), nav item box (px-3 py-2 text-sm font-medium rounded-lg), active treatment (bg-sidebar-accent text-sidebar-accent-foreground), mobile header, and the collapse cookie/localStorage pattern are byte-for-byte or near-identical between components/app-sidebar.tsx+app-nav.tsx (web) and components/navbar.tsx (studio) — clearly one was copied from the other during a deliberate parity pass. Drift concentrates in two systemic gaps. First, forked-then-edited copies: product-switcher.tsx exists in both repos as the same component, but subsequent edits (descriptions, rounded-xl, hover:text-lime, group-label tweaks) landed in only one side because nothing enforces that these files stay twins. Second, surfaces that never got the shared abstraction: web centralized page titles in PageHeader

== DIM: feedback-states (7 confirmed) ==
[high] package.json (social-scheduler, web app):? :: The web app has no toast/notification library at all — no `sonner` dependency, no `Toaster` mounted anywhere in app/layout.tsx, zero files matching `Toast` in app/ or components/. Studio's components/themed-toaster.tsx wraps sonner's `<Toaster position="top-right" closeButton theme={theme} />` and is used across 17+ pages/dialogs (batch-generate, create-image, create-video, pipeline steps, settings, dialogs/*, etc.) 
    EV: grep -in 'sonner' package.json → no match (web); C:\obSID SRL\n8n\edit_factory\frontend\package.json:50 → "sonner": "^2.0.7"
    FIX: Add sonner to social-scheduler, mount a themed Toaster (copy studio's themed-toaster.tsx pattern) in app/(app)/layout.tsx, and route action results (upload complete/fail, save, publish, workflow run) through toast.success/toast.error instead of ad-hoc inline spans.
[medium] app/(app)/media/upload-zone.tsx:157 :: Upload success/failure is only reflected as a small inline text label appended to the filename row (`uploading…` / `done` / destructive text), never a toast or any prominent confirmation — the only per-action feedback mechanism in the web app for a multi-file async operation.
    EV: {u.status === "uploading" && (<span className="text-muted-foreground">uploading…</span>)} ... {u.status === "done" && <span className="text-success">done</span>}
    FIX: Fire a toast.success(`${file.name} uploaded`) / toast.error(message) once the toaster exists; keep the inline list as a secondary detail view.
[medium] app/(app)/clips/page.tsx, app/(app)/dashboard/page.tsx, app/(app)/media/upload-zone.tsx:108 :: The icon+title+hint empty-state grammar (rounded-lg bg-lime/10 icon badge, font-heading title, muted hint paragraph) is hand-copy-pasted independently in at least 3 files with drifting details (border-dashed bg-card px-6 py-14 in clips/page.tsx vs border-dashed bg-background/40 px-6 py-12 in dashboard/page.tsx) instead of using a shared component. Studio has this exact grammar centralized in components/empty-state.ts
    EV: clips/page.tsx:108 `<div className="flex flex-col items-center gap-3 rounded-lg border border-dashed bg-card px-6 py-14 text-center">` vs dashboard/page.tsx:417 `<div className="mt
    FIX: Extract a components/empty-state.tsx in the web app mirroring studio's API (icon/title/description/action) and swap these 3 hand-rolled blocks — plus any future ones — onto it.
[low] app/(app)/affiliate/page.tsx:402 :: Some list-empty states in the web app are bare centered text with no icon and no card container (`No payouts yet.`), while sibling pages in the same app use the icon+title+hint card grammar for conceptually identical 'no data yet' situations (clips, dashboard) — the app is internally inconsistent about which empty-state visual grammar to use, let alone consistent with studio.
    EV: <p className="px-5 py-8 text-center text-sm text-muted-foreground">No payouts yet.</p> vs. app/(app)/automations/workflow-row.tsx:55 `<span className="text-muted-foreground">No run
    FIX: Once a shared EmptyState component exists (see above), apply it uniformly to affiliate/automations list rows too, or explicitly document a lighter 'inline row' variant of the same component for table-row contexts.
[medium] components/ui/ (social-scheduler):? :: The web app has no Skeleton component and zero skeleton-style loading placeholders (`bg-muted animate-pulse rounded-*` blocks) anywhere in app/ or components/. Studio uses this exact pattern for real content-loading placeholders in components/profile-switcher.tsx:45 (`<div className="w-full h-9 bg-muted animate-pulse rounded-lg" />`) and components/workspace-bar.tsx:141 (`<div className="m-2 h-6 w-40 animate-pulse ro
    EV: grep -rln "animate-pulse" app components → 3 files in web, all icon/dot pulses, vs. studio's profile-switcher.tsx:45 and workspace-bar.tsx:141 using it as a genuine block-level ske
    FIX: Add a components/ui/skeleton.tsx (or port studio's inline pattern) and use it for switcher/nav loading and any client-fetched list (e.g. workspace/profile switcher equivalents, async widgets on dashboard).
[high] components/ui/ (social-scheduler):? :: The web app has no ui/progress.tsx component and zero `<Progress>` bar usage anywhere — despite the product surface (clip rendering, media background-removal, automation runs) having genuinely long-running operations. Studio's progress.tsx (lime-filled bar, per approved decisions) is used in 13+ places: pipeline step2-tts.tsx, step1-script.tsx, batch-generate/page.tsx, create-image/page.tsx, create-video/page.tsx, us
    EV: app/(app)/clips/render-progress.tsx:39-58 renders a `rounded-lg border border-lime/15 bg-card px-3 py-2 text-xs` pill with a status label and `{done}/{total} clips` text — no `<Pro
    FIX: Add a shared ui/progress.tsx (shadcn Progress, lime fill per token) to the web app and use it in RenderProgress (percentage = done/total) and the upload-zone list, matching studio's visual language for long-running operations — this is the single most visible 'cheap' tell for a p
[high] app/(app)/**, app/(auth)/**, app/(admin)/** (social-scheduler):? :: The web app has zero `error.tsx` files anywhere in the App Router tree (no route-level error boundary, no root error.tsx), so an unhandled render/data error in any authenticated page falls through to Next.js's default unstyled error screen. Studio has a themed app/error.tsx at the root with proper dark-theme styling, a destructive-colored error detail box, and Try again/Back home actions.
    EV: find "app/(app)" "app/(auth)" "app/(admin)" -iname "error.tsx" → no results (web); C:\obSID SRL\n8n\edit_factory\frontend\src\app\error.tsx exists with `bg-muted border border-bord
    FIX: Add app/(app)/error.tsx (and one for (admin)) in the web app modeled directly on studio's app/error.tsx — same copy pattern, same destructive-token error box, same Try again/Back home buttons — so a crash reads as an intentional part of the product, not a Next.js default page.
  OBS: The root cause is architectural asymmetry in how the two apps were built: studio grew a small shared vocabulary of feedback primitives early (empty-state.tsx, themed-toaster.tsx wrapping sonner, ui/progress.tsx, a root error.tsx) and every page was written to consume them, so 10-17 call sites stay visually identical by construction. The web app never got that vocabulary — sonner isn't even a dependency, there's no Skeleton or Progress primitive, and no error.tsx exists anywhere in the App Router tree — so every page invents its own micro-solution: a text pill instead of a progress bar, inline uploading/done/error spans instead of a toast, and three near-identical hand-copied empty-state blocks with drifting padding/background instead of one component. This isn't random drift so much as the web app simply skipping a build step studio already took. Because the web app is the one carrying p

===== CRITIC =====
[medium] social-scheduler: no components/ui/dialog.tsx; app/(app)/automations/import-dialog.tsx:70, app/(app)/automations/new-automation.tsx:32, app/(app)/automations/template-wizard.tsx:76, app/(app)/automations/[id]/workflow-editor.tsx:472, app/(app)/create/composer.tsx:469, app/(app)/media/media-card.tsx:172, app/(app)/channels/connect-tile.tsx:158,198:? :: Web has zero shared Dialog/Modal primitive, so every one of its ~7 popup dialogs hand-rolls its own container with a different max-width and padding: max-w-lg p-5 (import-dialog, workflow-editor), max-w-3xl p-5 (new-automation), max-w-md p-5 (template-wizard), max-w-2xl p-5 (composer picker), and two native <dialog> elements at max-w-lg p-5 / max-w-md p-6 (media-card, connect-tile) that additional
    FIX: Introduce a components/ui/dialog.tsx (radix-based, mirroring studio's) and migrate the 7 hand-rolled modals onto it so size/overlay/close-button are governed by one component.
[medium] social-scheduler: no components/ui/badge.tsx (components/ dir only has button.tsx as a shared primitive):? :: Every 'pill/chip/badge' in the web app is a hand-rolled <span>, and the recipe drifts: padding is split between px-2 py-0.5 (dashboard.tsx:342,482; billing/page.tsx:99,220,226,231) and px-2.5 py-0.5/py-1 (media-card.tsx:225; clips/new-run.tsx:76; schedule/post-form.tsx:524; automations/studio-header.tsx:66), text size splits between text-[10px], text-xs, and text-[0.65rem], and font splits between
    FIX: Port studio's components/ui/badge.tsx to web (it's a stock shadcn primitive) and migrate the ~16 hand-rolled pill spans onto it with variant props for the lime/warning/muted tones already in use.
[low] social-scheduler/app/globals.css:174-192 vs app/(app)/automations/[id]/studio-canvas.tsx:568,647; edit_factory/frontend/src/app/globals.css:281-306:? :: Web only styles scrollbars inside one bespoke `.scrollbar-thin-lime` utility class, applied at exactly 2 call sites in one file (the automations node palette dock/run-history dropdown). Every other scrollable region in the web app (media grid, schedule list, automations list, settings sections, admin tables) falls back to the plain OS/browser scrollbar with no dark-theme styling at all. Studio tak
    FIX: Either scope web's scrollbar styling globally like studio (drop the bespoke class, style ::-webkit-scrollbar app-wide) or explicitly document scrollbar-thin-lime as an intentional one-off for dark floating overlays — right now it reads as an abandone
[low] social-scheduler/components/affiliate-consent-banner.tsx:26:? :: This is the only element in the entire web app using an arbitrary z-[100]; every modal/dropdown/sticky-header in the app is built on a consistent z-10/20/30/40/50 ladder (mobile headers z-40, dialogs/dropdowns z-50). The consent banner's z-[100] was presumably added to guarantee it sits above the z-50 dialogs, but that intent isn't documented anywhere near the value, and it's the one spot in the c
    FIX: Either drop to z-50 (same tier as dialogs, order will follow DOM position since the banner is fixed and rendered after dialogs unmount) or add a one-line comment explaining why it must outrank every dialog, so the next author doesn't read z-[100] as 
  OBS: Checked 6 candidate gaps beyond the 8 already-covered dimensions; substantiated 4, ruled out 2. Substantiated (returned as issues): - Dialog/modal grammar (sizes/close-button/overlay-type): web has no shared Dialog primitive at all, so 7 hand-rolled modals + 2 native <dialog> elements drift on max-width (lg/3xl/md/2xl) and padding (p-5/p-6); studio centralizes this in components/ui/dialog.tsx. - Badge/chip grammar: web has zero components/ui/badge.tsx, so every status pill is a hand-rolled span with 3+ drifting padding/text-size/font recipes across 16 files; studio has a real Badge primitive used in 42 files (with only 4 files bypassing it). - Scrollbar styling: web's only scrollbar theming (.scrollbar-thin-lime) is scoped to 2 call sites in 1 file (automations node palette); everywhere else in web falls back to default OS scrollbars. Studio styles ::-webkit-scrollbar globally so every s
===== REFS =====
-- ref --
* Few surfaces, one temperature: Anthropic paints the whole site in one warm family: canvas #faf9f5, surface-secondary #f0eee6, surface-hover #e8e6dc — three tones only a few percent apart, all sharing the same warm hue, with ink #141413 for text. Header, content => Blipost dark mode should use exactly 3 near-black tones from one hue family (e.g. page ~#0b0c0a, card ~#131511, hover/raised ~#1a1c17) plus one hairline border (~white 8-10% alpha). Kill any ad-hoc grays; every surface in the app 
* Band alternation paces the page: Long pages alternate cream and dark full-bleed bands (the 'cream-to-black band alternation' documented in their 22-component system) instead of drawing dividers; each band is a full section with its own generous padding, so sectio => Separate major dashboard/marketing sections by alternating the two darkest surface tones as full-width bands with large vertical padding, instead of <hr>-style borders or heavy card wrappers. Inside the app, one tone for the shell
* Hairline borders, minimal radius scale, near-zero shadows: Cards use a single 1px hairline border in a tone barely darker than the surface, a 4-step radius scale with 8px as the primary radius (16px base for large containers), and effectively no drop shadows — depth comes from the surface => Standardize on two radii (8px controls/cards, 16px large panels/modals), one border token (1px, white/8%), and delete box-shadows on dark surfaces (they're invisible anyway). Elevation on dark = lighter surface tone + hairline, ne
* Two-typeface, ~11-level type scale used with discipline: Styrene/Anthropic Sans for UI, headings and nav; Tiempos/Anthropic Serif for editorial display (90px display down to 24px body serif); Mono only for uppercase metadata labels. Contrast between heading and body comes from size + ty => Blipost keeps one sans for everything UI, at most 5 sizes in-app (12 label, 14 body, 16 emphasized, 20 section, 28-32 page title) and 2 weights (400/600). If you want the 'editorial' feel, use the mono-uppercase-tracked pattern fo
* Muted secondary text does the hierarchy work: They declare a full text ramp — ink #141413, ink-soft #3d3d3a, muted #5e5d59, tertiary #87867f — and lean on it constantly: metadata, captions, and nav are one or two steps grayer than body, so pages read layered without any size  => Define a 3-step dark text ramp (e.g. #f2f2ec primary, ~60% for secondary, ~40% for tertiary) and route ALL de-emphasis through it. Most 'heavy text' feelings on dark UIs come from everything being 100% white; demoting metadata to 
* One accent, spent sparingly: The terracotta/clay #d97757 accent appears almost exclusively on primary CTAs and small highlights; the other 8 accent swatches surface only in special contexts (research charts, index dashboards). A typical page shows the accent  => Ration lime to: primary button, active nav item, focus ring, and live/positive status. Everything else (links, icons, secondary buttons) uses the text ramp + hairline borders. If lime appears more than ~5 times per screen, the scr
* Hover = one surface step, nothing more: Hover states are tokenized as the next surface tone (#f0eee6 → #e8e6dc, a ~3% luminance shift); interactive rows/cards shift background subtly, links shift color or underline. No scale transforms, no shadow pops, no color inversio => One rule: hoverable surfaces move one step up the surface ramp (~4-6% lighter), links/buttons shift toward a slightly brighter tone, transition ~150ms ease-out on background-color/color only. Ban translateY/scale/glow hovers — the
* Centered container, big consistent section rhythm: Layout is a single centered max-width container with an 8-step spacing scale; sections get generous, uniform vertical padding, and there are almost no decorative elements filling the space — whitespace is the decoration. => Fix one container (max-width ~1200px marketing, ~1100px app content), and pull all padding from an 8-step scale (4/8/12/16/24/32/48/64). Section padding in-app: 32-48px vertical, always the same; marketing bands: 96px+. Never eyeb
* Short copy + progressive disclosure beats dense text: Sections are headline-driven: one short H2, one or two sentences, then a link or card list; secondary content hides behind nav dropdowns, grouped comparison tables with checkmarks, and category subheadings — prose paragraphs are r => Cap any card/section at heading + one sentence + one action. Feature/limit lists become checkmark rows grouped under mono-uppercase subheads, with rarely-needed detail behind a disclosure or 'compare' link. In-app empty states and
  SUM: Anthropic.com feels coherent because every visual decision is drawn from a tiny, strictly-rationed token set: three near-identical warm surface tones plus a hairline border do all the layering, two typefaces on a disciplined scale do all the hierarchy, and one clay accent appears only where action is wanted. Depth, hover, and section separation are all expressed as one-step moves within those same ramps (a surface tone step, a text-ramp step), so
-- ref --
* Surface strategy: 2-3 tones, no shadows: openai.com uses one near-black base plus at most one slightly lighter card tone; a token crawl of 33 pages captured 0 box-shadows. Header, content, and cards are separated by tone shift and whitespace only — never elevation. Cards => Blipost should commit to exactly 3 dark surfaces: page base (near-black), raised surface (cards/panels, ~4-6% lighter), and overlay (modals/popovers, one step lighter again). Delete all box-shadows on dark surfaces; use a 1px bord
* One accent, used scarcely: OpenAI's brand teal (#10a37f) appears only on links, key CTAs, and highlights — everything else is a grayscale ramp (graphite #3c3c3c body, slate #6e6e6e secondary, ash #9b9b9b tertiary). The accent has exactly one hover-darkened  => Lime should appear in at most 3 roles per screen: primary CTA, active nav/selection state, and key metric or link. Everything else uses a 3-step gray text ramp (e.g. ~92% white primary, ~64% secondary, ~40% tertiary). Define exact
* Spacing rhythm: 4px base, big section gaps, one container: OpenAI runs a 4px base scale (4/8/12/16/24/32/48/64/96/128), a single ~1200px max-width container with 24px mobile / 48px desktop gutters, and 96-128px vertical spacing between major sections (64px mobile). Grids use a consistent  => Blipost app: one content max-width (~1200px for marketing, ~1400px for the app shell), 24px as the universal card padding and grid gap, and section spacing picked only from 48/64/96. Kill any ad-hoc 10px/18px/30px values — every m
* Type scale: two families of contrast, few sizes: OpenAI pairs an editorial serif (Signifier) for display moments with one sans (Söhne/Inter) for everything else, applies -0.02em tracking on display sizes returning to 0 by 16px, and uses only 2-3 weights. Hierarchy comes from siz => Blipost: one sans family, max 5 sizes (e.g. 13/14/16/22/36) and 2 weights (400/600). Get heading contrast from a big size jump plus tighter tracking (-0.02em above 24px), not from bolding everything. If a display voice is wanted, 
* Hover grammar: quiet, fast, uniform: OpenAI's hovers are 150-220ms color/opacity shifts with cubic-bezier(0.16,1,0.3,1); layout transitions 280-360ms. Only interactive elements (links, buttons, cards that navigate) get hover states, and the state is one thing: a tone => Standardize one transition token: 150ms ease-out on background-color/border-color/color, applied only to clickable things. Card hover = background lifts one tone step (or border brightens); button hover = one darker lime. Ban tran
* Borders and radius: one hairline, two radii: OpenAI uses subtle radii in a narrow band (roughly 8-16px on cards) and very few border treatments overall — 38 border/radius instances across 33 pages, with hairline separators rather than boxed-in sections. Content is mostly div => Blipost: exactly two radii — e.g. 8px for inputs/buttons, 12px for cards/modals (pills for badges only). One border color token (white at ~8-10% opacity) reused everywhere. Prefer whitespace over divider lines inside cards; when a
* Heavy text avoided via card formula + progressive disclosure: Every openai.com card follows a strict formula: eyebrow/category label, one headline, at most one line of description, small metadata ('Product', '7 min read'). The pricing page shows short per-tier feature summaries ('Everything  => Blipost cards and settings rows adopt the same formula: tiny uppercase/muted category, one-line title, one-line muted description, stop. Move exhaustive detail (full feature matrices, advanced options, logs) behind an expander or 
* Emphasis by singling out, not decorating: On the pricing page only one tier (Plus) is highlighted, and OpenAI's hero earns attention with one huge statement and a single image — emphasis is achieved by making one element different while everything around it stays uniform, => Per screen, pick exactly one lime-emphasized element (the recommended plan gets a lime border + small badge; siblings stay identical hairline-gray). In dashboards, one primary action per view is a filled lime button; every other a
  SUM: openai.com feels coherent because it is built from a tiny, ruthlessly reused vocabulary: two or three surface tones with zero shadows, one accent color in one or two states, a 4px spacing scale with big 96-128px section gaps, and a type system of essentially one sans plus a display voice in only 2-3 weights. Every interactive element obeys the same quiet grammar — 150-220ms tone-shift hovers, hairline borders, 8-16px radii — so nothing on any pag
-- ref --
* Two-surface color strategy, not five: The whole site runs on essentially two page tones — eggshell #FDFCFC as the page and 'cream' as the card fill — plus pure black text and one gray (gray-500, used 235 times) for secondary text. Header, content, and cards are separa => Blipost dark equivalent: one near-black page tone + ONE slightly lighter card/panel tone. Kill any third/fourth surface gray. Separate header from content with a 1px hairline border, not a different background. Lime stays a pure a
* Hairline rings instead of borders and shadows: Cards use `ring-0.5 ring-inset ring-black/7.5` (a 0.5px inset ring at 7.5% black) as their edge, applied 41+ times; visible vertical column rules `border-l border-r border-overlay-dark` run the full container height, turning struc => On dark: 1px (or 0.5px) inset ring at white/8–10% on every card, identical everywhere. Drop drop-shadows almost entirely (they read as mud on near-black); if used, keep alpha under 0.3 and 1–4px blur. Consider full-height left/rig
* One container width, tokenized gutters: Everything lives in `--container-width: min(100%, 81.5rem)` (~1304px) with gutters tokenized per breakpoint: `--outer-gutter` 20/40/64px and section padding `--section-px` 0.5rem/1rem/3rem. No per-section ad-hoc widths. => Pick one max-width (~1280–1320px) and 2–3 gutter tokens; every page and section in Blipost uses them. Consistent container edges are half of what reads as 'premium' — audit pages that currently pick their own max-w.
* Tiny-type discipline: small sizes carry the site: Usage counts of the type scale: xs 171×, sm 101×, base 67×, then a cliff, then display sizes (5xl 18×, 6xl 5×). Almost no mid-range headings — the site is small text + occasional huge display, with nothing in between fighting for  => Blipost UI copy should live at 12–14px (xs/sm) with 16px body; reserve one big display size for page/hero titles and delete intermediate heading sizes. The premium feel comes from small, confident text with generous space, not med
* Two font weights only: font-medium 176×, font-normal 29× — that's the whole weight palette (plus rare mono for code). No semibold, no bold. Hierarchy is done with size, color (black vs gray-500), and space instead of weight. => Restrict Blipost to normal + medium (drop 600/700 except maybe display headlines). Secondary text = one muted gray token (their gray-500 equivalent, ~60% white), used everywhere identically.
* Hover grammar: color shift, not decoration: Dominant hovers: `hover:text-gray-500` (132×) — text DARKENS toward muted, `hover:bg-gray-25` (29×) — background shifts one barely-visible step, `active:scale-[0.98]` on buttons. Transitions are only 200ms and 300ms (duration-200  => Blipost: links/nav hover = text color one step (white→gray or gray→white); cards/buttons hover = background one step lighter (e.g. white/4→white/7), plus active:scale-[0.98] on primary buttons. Standardize on exactly two durations
* Radius language: pills + one card radius: rounded-full 286× (buttons, avatars, tags, tab pills) and one signature card radius `rounded-2.5xl` (~20px, 84×), with small `rounded`/`rounded-sm` for inline elements. Essentially three radii total. => Adopt three tokens: full (pill buttons/badges/tabs), one large card radius (16–20px), one small (4–6px) for inputs/inline chips. Delete every other radius value in the codebase. Pill-shaped primary buttons are a cheap, distinctive
* Progressive disclosure kills heavy text: Homepage sections are one short display headline + 1–2 lines of sm text + a visual/demo; detail lives behind tabs (voice categories: Narration/Advertisement/Characters...) and reveal-on-hover captions (`opacity-0 group-hover:opaci => Blipost pages: cap visible copy at headline + one sentence per section; move explanations into tabs, hover captions, or an expandable section below. Pricing/feature pages: simple cards first, dense comparison table second, FAQ las
* Texture and micro-detail as the premium layer: Cards carry a subtle noise texture (`mix-blend-overlay bg-[image:var(--noise-png)]` with resolution-aware sizing) and `text-balance`/`text-pretty` on headings/paragraphs — invisible details that remove the 'flat CSS' feel without  => On near-black, a 2–3% opacity noise overlay on hero/card surfaces plus `text-wrap: balance` on headings and `text-pretty` on paragraphs are near-free polish. This — not gradients or lime glows — is how a dark SaaS avoids looking l
  SUM: ElevenLabs feels coherent because it is radically constrained: two surface tones (eggshell page, cream cards), two font weights, three radii, two transition durations, one container width with tokenized gutters, and one hover verb (shift one color step) — so every screen is recombination of the same few tokens rather than new decisions. Edges are drawn with 0.5px inset rings at ~8% opacity and full-height column rules instead of shadows, which ma