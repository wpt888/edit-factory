# Edit Factory - Python Dependencies
# Install: pip install -r requirements.txt

# Web Framework
fastapi==0.128.0
uvicorn[standard]==0.40.0
python-multipart==0.0.21
slowapi==0.1.9

# Background Tasks
celery==5.6.0
redis==7.1.0

# Video Processing
scenedetect[opencv]==0.6.7.1
opencv-python-headless==4.12.0.88
numpy>=1.24.0,<2.0.0
scipy==1.13.1

# Google Gemini AI
google-genai==1.56.0

# Anthropic Claude AI
anthropic==0.79.0

# Audio Transcription (SRT generation)
openai-whisper==20250625

# TTS & External APIs
httpx==0.28.1
aiofiles==25.1.0
edge-tts==7.2.7

# TTS - Coqui XTTS (voice cloning, 17 languages)
# Note: PyTorch is a transitive dependency. For NVIDIA GPU support, install PyTorch with CUDA:
# pip3 install torch torchvision torchaudio --index-url https://download.pytorch.org/whl/cu121
TTS==0.22.0
librosa==0.11.0
pydub==0.25.0

# TTS - Kokoro (lightweight, fast, requires espeak-ng system dependency)
# IMPORTANT: Kokoro requires espeak-ng system dependency
# Linux: apt-get install espeak-ng
# macOS: brew install espeak-ng
# Windows: https://github.com/espeak-ng/espeak-ng/releases
kokoro==0.9.4
soundfile==0.13.1

# Voice Activity Detection (VAD) - pentru detectarea și mutarea vocilor din video
# Silero VAD necesită PyTorch
torch==2.9.1
torchaudio==2.9.1

# Voice Cloning (optional - requires GPU for best performance)
# Comentat pentru că torch e deja inclus mai sus

# Supabase
supabase==2.27.0

# Google Drive
google-api-python-client==2.187.0
google-auth-oauthlib==1.2.2

# Authentication
PyJWT==2.10.1

# Encryption
cryptography==46.0.3

# Structured JSON Logging
python-json-logger==4.0.0

# Utilities
python-dotenv==1.2.1
tqdm==4.67.1
pydantic==2.12.5
pydantic-settings==2.12.0
tenacity==9.1.2
psutil==7.2.2
sentry-sdk==2.0.0
# MIME type validation via magic-number inspection (not Content-Type header)
# Requires libmagic system library: apt-get install libmagic1 (Linux) | brew install libmagic (macOS)
python-magic==0.4.27

# Subtitle Processing
srt==3.5.3

# XML Feed Parsing (Google Shopping feeds)
lxml==6.0.2

# Testing
pytest==9.0.2
pytest-cov==4.0.0
